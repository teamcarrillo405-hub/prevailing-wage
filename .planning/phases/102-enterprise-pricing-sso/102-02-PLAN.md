---
phase: 102-enterprise-pricing-sso
plan: 02
type: execute
wave: 2
depends_on: [102-01]
files_modified:
  - src/client/pages/PricingPage.tsx
  - src/client/pages/IntegrationsPage.tsx
autonomous: true
requirements: [ENT-01, ENT-02]

must_haves:
  truths:
    - "PricingPage Enterprise card shows SSO/SAML as a feature (not 'roadmap')"
    - "IntegrationsPage has an SSO section with Okta and Azure AD connect stubs"
    - "SSO connect buttons show 'Coming Soon' state with a contact link"
  artifacts:
    - path: "src/client/pages/PricingPage.tsx"
      provides: "Updated Enterprise tier feature list"
      contains: "SSO / SAML"
    - path: "src/client/pages/IntegrationsPage.tsx"
      provides: "SSO integration stub UI"
      contains: "SsoSection"
  key_links:
    - from: "src/client/pages/IntegrationsPage.tsx"
      to: "/contact"
      via: "Contact Sales link on SSO stub"
      pattern: "href.*contact"
---

<objective>
Update PricingPage Enterprise tier to prominently feature SSO, and add an SSO section stub to IntegrationsPage with Okta and Azure AD connect cards.

Purpose: ENT-01 requires the Enterprise pricing card to reflect SSO capability. ENT-02 requires an integration UI stub so enterprise prospects can see the connect surface.
Output: Updated PricingPage.tsx + SSO section in IntegrationsPage.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@src/client/pages/PricingPage.tsx
@src/client/pages/IntegrationsPage.tsx
</context>

<interfaces>
<!-- PricingPage PLANS array (lines 22-73). Enterprise plan features to update: -->
```typescript
// CURRENT Enterprise features in PLANS array:
features: [
  'Everything in Pro',
  'Custom state form requests',
  'DBE / MBE certification tracking',
  'Priority support + SLA',
  'SSO / SAML (roadmap)',      // <-- remove "(roadmap)" suffix
  'SOC 2 Type II reports',
],
```

<!-- PricingPage MATRIX_ROWS — update SSO row if it exists, or add one: -->
```typescript
{ label: 'SSO / SAML',  starter: false, pro: false, enterprise: true },
```

<!-- IntegrationsPage — check current content before adding SSO section. Add SSO section using same card pattern as existing integrations. -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update PricingPage Enterprise tier for SSO</name>
  <files>src/client/pages/PricingPage.tsx</files>
  <action>
Two changes to PricingPage.tsx:

1. In the PLANS array, find the Enterprise plan features array (around line 64). Change:
   - `'SSO / SAML (roadmap)'` → `'SSO / SAML'`
   - Add after that line: `'Dedicated onboarding + CSM'`
   This signals ENT-01: enterprise tier now includes SSO as a shipped/near-shipped feature.

2. In MATRIX_ROWS array, find or add the SSO row:
   - If a row with label 'SSO / SAML' exists: update enterprise value to `true`
   - If no such row: add `{ label: 'SSO / SAML', starter: false, pro: false, enterprise: true }` after the 'MFA' row

No other changes to PricingPage.tsx. Do not change prices, card layout, or other tiers.
  </action>
  <verify>
    <automated>cd /c/Users/glcar/prevailing-wage && grep -n "SSO" src/client/pages/PricingPage.tsx</automated>
  </verify>
  <done>PricingPage.tsx Enterprise features list shows "SSO / SAML" (no "(roadmap)" suffix). MATRIX_ROWS has SSO row with enterprise: true.</done>
</task>

<task type="auto">
  <name>Task 2: Add SSO stub section to IntegrationsPage</name>
  <files>src/client/pages/IntegrationsPage.tsx</files>
  <action>
Read IntegrationsPage.tsx first to understand its current structure and card/section patterns.

Add a new SSO section. The section should:

1. Be placed after the existing integrations content (before the closing of the page's main container).

2. Use this structure:
```tsx
{/* SSO / SAML Section — Phase 102 ENT-02 */}
<section className="mt-12">
  <h2 className="text-xl font-bold text-nav-dark font-headline mb-1">Single Sign-On (SSO)</h2>
  <p className="text-gray-500 text-sm mb-6">
    Connect your identity provider so your team signs in with existing corporate credentials.
    Available on the Enterprise plan.{' '}
    <a href="/contact" className="text-brand-gold hover:underline font-medium">Contact Sales</a> to enable.
  </p>
  <div className="grid grid-cols-1 sm:grid-cols-2 gap-4">
    {[
      { name: 'Okta', logo: 'OK', desc: 'SAML 2.0 via Okta Identity Provider.' },
      { name: 'Azure Active Directory', logo: 'AZ', desc: 'SAML 2.0 / OIDC via Microsoft Entra ID.' },
    ].map(({ name, logo, desc }) => (
      <div key={name} className="bg-white border border-gray-200 rounded-xl p-5 flex items-center gap-4">
        <div className="w-10 h-10 rounded-lg bg-nav-dark text-brand-gold font-bold text-sm flex items-center justify-center flex-shrink-0">
          {logo}
        </div>
        <div className="flex-1">
          <p className="font-semibold text-nav-dark text-sm">{name}</p>
          <p className="text-gray-500 text-xs mt-0.5">{desc}</p>
        </div>
        <span className="text-xs bg-gray-100 text-gray-500 px-2 py-1 rounded-full font-medium whitespace-nowrap">
          Enterprise
        </span>
      </div>
    ))}
  </div>
</section>
```

Use only design tokens (bg-nav-dark, text-brand-gold, etc.). No hardcoded hex. No new imports needed if the page already imports from lucide-react.

Do NOT add any POST routes or server-side logic — this is a UI stub only. The actual SSO connect flow is a future phase.
  </action>
  <verify>
    <automated>cd /c/Users/glcar/prevailing-wage && grep -n "SSO\|sso\|Okta\|Azure" src/client/pages/IntegrationsPage.tsx</automated>
  </verify>
  <done>IntegrationsPage.tsx has SSO section with Okta and Azure AD cards. Each card shows "Enterprise" badge. Contact Sales link points to /contact. No new TS errors.</done>
</task>

</tasks>

<verification>
```bash
cd /c/Users/glcar/prevailing-wage && npx tsc --noEmit 2>&1 | tail -5
```
Must report no new errors.
</verification>

<success_criteria>
- PricingPage Enterprise tier shows "SSO / SAML" (without "(roadmap)")
- PricingPage feature matrix has SSO row with enterprise: true
- IntegrationsPage has SSO section with Okta + Azure AD cards
- Cards show "Enterprise" badge and "Contact Sales" link
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/102-enterprise-pricing-sso/102-02-SUMMARY.md` with:
- Changes made to each file
- SSO feature list position in pricing
- TS compile result
</output>
