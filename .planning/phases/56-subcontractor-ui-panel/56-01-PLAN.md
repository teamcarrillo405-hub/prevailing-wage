---
phase: 56-subcontractor-ui-panel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/client/lib/cprStatus.ts
  - src/client/pages/ProjectDetailPage.tsx
  - tests/lib/cprStatus.test.ts
autonomous: false
requirements:
  - SUB-05

must_haves:
  truths:
    - "ProjectDetailPage has a Subcontractors section listing all subs for the project"
    - "Each sub row shows name, license number, and contact info with Edit and Remove controls"
    - "Expanding a sub row shows its CPR week table with status badges"
    - "Status badge is Received — Compliant (green) when receivedDate is set and isCompliant === 1"
    - "Status badge is Received — Non-Compliant (red) when receivedDate is set and isCompliant !== 1"
    - "Status badge is Overdue (amber) when receivedDate is null and weekEndingDate is more than 7 days ago"
    - "Status badge is Not Received (gray) when receivedDate is null and weekEndingDate is within 7 days"
    - "User can add a sub via an inline form (name required, optional fields)"
    - "User can edit a sub via an inline form and save changes"
    - "User can remove a sub with an inline confirmation prompt"
    - "User can add a CPR week record for a sub with weekEndingDate, receivedDate, isCompliant, notes"
    - "User can mark an existing CPR week as received or update compliance status"
    - "Subcontractor data is never mixed into GC worker counts, compliance rollups, or WorkflowProgress"
  artifacts:
    - path: "src/client/lib/cprStatus.ts"
      provides: "getCprStatus pure function + STATUS_BADGE map + Subcontractor/CprWeek types"
      exports: ["getCprStatus", "STATUS_BADGE", "Subcontractor", "CprWeek", "CprStatus"]
    - path: "src/client/pages/ProjectDetailPage.tsx"
      provides: "SubcontractorsPanel inline component + full CRUD surface + expandable CPR week table"
      contains: "SubcontractorsPanel"
    - path: "tests/lib/cprStatus.test.ts"
      provides: "Unit tests for badge logic, overdue boundary, three-state isCompliant"
      contains: "getCprStatus"
  key_links:
    - from: "SubcontractorsPanel"
      to: "/api/projects/:id/subcontractors"
      via: "useQuery(['subcontractors', id])"
      pattern: "queryKey.*subcontractors.*id"
    - from: "CprWeekTable (inline)"
      to: "/api/projects/:id/subcontractors/:subId/cpr-weeks"
      via: "useQuery(['cpr-weeks', id, subId]) enabled only when expanded"
      pattern: "enabled.*expandedSubId.*sub\\.id"
    - from: "getCprStatus"
      to: "Badge variant"
      via: "STATUS_BADGE lookup"
      pattern: "STATUS_BADGE\\[status\\]"
---

<objective>
Build the Subcontractors panel on ProjectDetailPage — the final piece of the SUB-05 requirement. All seven API routes (Phase 55) are live. This plan implements the complete client-side panel: list subs, add/edit/remove subs, expandable per-sub CPR week table with status badges, and the "Mark Received/Compliant" action surface.

Purpose: Prime contractors bear strict liability for sub prevailing wage violations. This panel makes CPR receipt and compliance status visible and actionable without leaving the project view.
Output: src/client/lib/cprStatus.ts (pure badge logic), updated ProjectDetailPage.tsx (SubcontractorsPanel inline component), tests/lib/cprStatus.test.ts (unit tests).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-subcontractor-ui-panel/56-RESEARCH.md
@.planning/phases/56-subcontractor-ui-panel/56-VALIDATION.md

<interfaces>
<!-- Key types and API contracts the executor needs. No codebase exploration required. -->

<!-- API response shapes (Phase 55 implementation) -->
GET  /api/projects/:id/subcontractors         → { data: { subcontractors: Subcontractor[] } }
POST /api/projects/:id/subcontractors         → { data: { subcontractor: Subcontractor } } 201
PATCH /api/projects/:id/subcontractors/:subId → { data: { subcontractor: Subcontractor } }
DELETE /api/projects/:id/subcontractors/:subId → { data: { deleted: true } }
GET  /api/projects/:id/subcontractors/:subId/cpr-weeks       → { data: { cprWeeks: CprWeek[] } }
POST /api/projects/:id/subcontractors/:subId/cpr-weeks       → { data: { cprWeek: CprWeek } } 201; 409 on duplicate weekEndingDate
PATCH /api/projects/:id/subcontractors/:subId/cpr-weeks/:weekId → { data: { cprWeek: CprWeek } }

<!-- Data shapes -->
interface Subcontractor {
  id: string;
  projectId: string;
  name: string;
  licenseNumber: string | null;
  contactName: string | null;
  contactEmail: string | null;
  address: string | null;
  createdAt: string;
}

interface CprWeek {
  id: string;
  subcontractorId: string;
  weekEndingDate: string;        // YYYY-MM-DD text
  receivedDate: string | null;   // YYYY-MM-DD text, null = not received
  isCompliant: number | null;    // null=unassessed, 0=non-compliant, 1=compliant (THREE-STATE — never coerce to boolean)
  notes: string | null;
  createdAt: string;
}

<!-- Badge component (src/client/components/ui/Badge.tsx) -->
type BadgeVariant = 'compliant' | 'violation' | 'warning' | 'neutral';
<!-- DO NOT add new variants. These four cover all four CPR statuses. -->

<!-- Button component (src/client/components/ui/Button.tsx) -->
<!-- NO asChild prop. Copy secondary classes directly to <a> for links. -->
<!-- variants: 'primary' | 'secondary' | 'ghost' -->

<!-- Card component -->
<!-- padding="default" | "sm" | "none" -->

<!-- Existing TanStack Query key conventions in ProjectDetailPage.tsx -->
['projects', id]          ← project detail query
['workers', id]           ← workers query
['payroll-weeks', id]     ← payroll weeks query

<!-- New query keys (use exactly these for invalidation consistency) -->
['subcontractors', id]             ← subs list for project
['cpr-weeks', id, subId]           ← CPR weeks for one sub (lazy, enabled only when expanded)

<!-- Design tokens (from CLAUDE.md — never hardcode hex) -->
bg-nav-dark, border-brand-gold, text-brand-gold, bg-brand-gold
bg-surface-card, bg-surface-page
font-headline (Oswald), font-body (Inter)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract getCprStatus to src/client/lib/cprStatus.ts and write unit tests (Wave 0 + RED/GREEN)</name>
  <files>src/client/lib/cprStatus.ts, tests/lib/cprStatus.test.ts</files>
  <behavior>
    - getCprStatus(week: CprWeek): CprStatus returns 'overdue' when receivedDate is null AND weekEndingDate is more than 7 days ago
    - getCprStatus returns 'not-received' when receivedDate is null AND weekEndingDate is within 7 days (inclusive)
    - getCprStatus returns 'received-compliant' when receivedDate is non-null AND isCompliant === 1
    - getCprStatus returns 'received-non-compliant' when receivedDate is non-null AND isCompliant === 0
    - getCprStatus returns 'received-non-compliant' when receivedDate is non-null AND isCompliant === null (unassessed = non-compliant for badge purposes)
    - Overdue boundary: exactly 7 days ago → 'not-received'; exactly 8 days ago → 'overdue'
    - Date parsing: weekEndingDate + 'T00:00:00' forces local time (prevents UTC midnight offset bug)
    - STATUS_BADGE maps 'received-compliant' → variant: 'compliant', label: 'Received — Compliant'
    - STATUS_BADGE maps 'received-non-compliant' → variant: 'violation', label: 'Received — Non-Compliant'
    - STATUS_BADGE maps 'not-received' → variant: 'neutral', label: 'Not Received'
    - STATUS_BADGE maps 'overdue' → variant: 'warning', label: 'Overdue'
  </behavior>
  <action>
    TDD cycle — RED then GREEN:

    RED: Create tests/lib/cprStatus.test.ts first. Import getCprStatus and STATUS_BADGE from src/client/lib/cprStatus.ts (file does not exist yet — import will fail, confirming RED). Write tests covering all behavior items above. Use vi.setSystemTime() to control Date.now() for boundary tests — set system time to a known date, then construct weekEndingDate strings at 7 and 8 days prior. Run `npx vitest run tests/lib/cprStatus.test.ts` — must fail.

    GREEN: Create src/client/lib/cprStatus.ts:

    ```typescript
    import type { BadgeVariant } from '../components/ui/Badge';

    export interface Subcontractor {
      id: string;
      projectId: string;
      name: string;
      licenseNumber: string | null;
      contactName: string | null;
      contactEmail: string | null;
      address: string | null;
      createdAt: string;
    }

    export interface CprWeek {
      id: string;
      subcontractorId: string;
      weekEndingDate: string;
      receivedDate: string | null;
      isCompliant: number | null;
      notes: string | null;
      createdAt: string;
    }

    export type CprStatus = 'overdue' | 'received-compliant' | 'received-non-compliant' | 'not-received';

    export function getCprStatus(week: CprWeek): CprStatus {
      if (!week.receivedDate) {
        const weekMs = new Date(week.weekEndingDate + 'T00:00:00').getTime();
        const daysAgo = Math.floor((Date.now() - weekMs) / 86_400_000);
        return daysAgo > 7 ? 'overdue' : 'not-received';
      }
      return week.isCompliant === 1 ? 'received-compliant' : 'received-non-compliant';
    }

    export const STATUS_BADGE: Record<CprStatus, { variant: BadgeVariant; label: string }> = {
      'received-compliant':     { variant: 'compliant', label: 'Received — Compliant' },
      'received-non-compliant': { variant: 'violation', label: 'Received — Non-Compliant' },
      'not-received':           { variant: 'neutral',   label: 'Not Received' },
      'overdue':                { variant: 'warning',   label: 'Overdue' },
    };
    ```

    Run `npx vitest run tests/lib/cprStatus.test.ts` — must pass GREEN.

    CRITICAL CONSTRAINTS:
    - isCompliant === 1 check must use strict equality, NOT truthy coercion (isCompliant could be 0)
    - weekEndingDate + 'T00:00:00' is mandatory (UTC midnight trap — see research Pitfall 2)
    - BadgeVariant import must be `import type` to avoid circular dependency
    - Do not export a BadgeVariant type from this file — import it from Badge.tsx
  </action>
  <verify>
    <automated>cd C:/Users/glcar/prevailing-wage && npx vitest run tests/lib/cprStatus.test.ts</automated>
  </verify>
  <done>All getCprStatus and STATUS_BADGE tests pass green. src/client/lib/cprStatus.ts and tests/lib/cprStatus.test.ts both exist.</done>
</task>

<task type="auto">
  <name>Task 2: Implement SubcontractorsPanel inline in ProjectDetailPage.tsx</name>
  <files>src/client/pages/ProjectDetailPage.tsx</files>
  <action>
    Add a SubcontractorsPanel inline component to ProjectDetailPage.tsx (same file, not a separate file — single-use component pattern matching WorkflowProgress). Render it inside the `{project && (...)}` block, after the notification panel and before the sub-page navigation links.

    IMPORTS to add at top of file:
    - ChevronRight from 'lucide-react' (add to existing Workflow, Settings import)
    - { getCprStatus, STATUS_BADGE } from '../lib/cprStatus'
    - { Subcontractor, CprWeek } from '../lib/cprStatus' (type imports)

    INLINE SubcontractorsPanel COMPONENT (add above ProjectDetailPage function):

    Component signature:
    ```typescript
    function SubcontractorsPanel({ projectId }: { projectId: string }) { ... }
    ```

    STATE in SubcontractorsPanel:
    - expandedSubId: string | null — which sub row is expanded
    - addingNew: boolean — show/hide add form
    - editingSubId: string | null — which sub is in edit mode
    - deletingSubId: string | null — which sub shows inline delete confirmation
    - addForm: { name: string; licenseNumber: string; contactName: string; contactEmail: string; address: string }
    - editForm: same shape as addForm

    QUERIES:
    - Subs list: queryKey ['subcontractors', projectId], enabled: !!projectId
      → api.get<{ data: { subcontractors: Subcontractor[] } }>(`/projects/${projectId}/subcontractors`)

    MUTATIONS (in SubcontractorsPanel):
    - addSubMutation: POST /projects/:id/subcontractors — body: { name, licenseNumber?, contactName?, contactEmail?, address? }
      onSuccess: invalidate ['subcontractors', projectId], setAddingNew(false), reset addForm
      Guard: if (!addForm.name.trim()) return — name is required
    - editSubMutation: PATCH /projects/:id/subcontractors/:subId — body same as add
      onSuccess: invalidate ['subcontractors', projectId], setEditingSubId(null)
    - deleteSubMutation: DELETE /projects/:id/subcontractors/:subId
      onSuccess: invalidate ['subcontractors', projectId], setDeletingSubId(null), setExpandedSubId(prev => prev === deletedId ? null : prev)

    INLINE CprWeekTable COMPONENT (nested inside SubcontractorsPanel body, not a separate export):
    - Props: { projectId: string; subId: string }
    - Query: queryKey ['cpr-weeks', projectId, subId], enabled: !!subId
      → api.get<{ data: { cprWeeks: CprWeek[] } }>(`/projects/${projectId}/subcontractors/${subId}/cpr-weeks`)
    - addCprWeekMutation: POST /projects/:id/subcontractors/:subId/cpr-weeks
      Body: { weekEndingDate: string; receivedDate?: string; isCompliant?: 0|1; notes?: string }
      onError: show 409 message ("A record for this week ending date already exists.")
      onSuccess: invalidate ['cpr-weeks', projectId, subId], reset add week form
    - updateCprWeekMutation: PATCH /projects/:id/subcontractors/:subId/cpr-weeks/:weekId
      Body: Partial<{ receivedDate: string; isCompliant: 0|1|null; notes: string }>
      onSuccess: invalidate ['cpr-weeks', projectId, subId]
    - For each CPR week row: show Badge with getCprStatus result → STATUS_BADGE[status]
    - Action buttons per row:
      - If !receivedDate: "Mark Received" button → PATCH with receivedDate = today's ISO date (YYYY-MM-DD), isCompliant = null
      - If receivedDate and isCompliant !== 1: "Mark Compliant" button → PATCH with isCompliant = 1
      - If receivedDate and isCompliant === 1: "Mark Non-Compliant" button → PATCH with isCompliant = 0
    - Show "Add CPR Week" form inline below the table (not a modal): date input (type="date"), receivedDate input (type="date", optional), isCompliant select (options: — (null), Compliant (1), Non-Compliant (0)), notes textarea (optional)

    EXPAND TOGGLE:
    ```typescript
    onClick={() => setExpandedSubId(prev => prev === sub.id ? null : sub.id)}
    ```
    Use ChevronRight with `rotate-90` class when expanded (same icon rotation pattern as NotifPanel).

    LAYOUT:
    - Section heading: "Subcontractors" with "Add Subcontractor" button on the right
    - Sub list: each sub in a Card row with name (bold), licenseNumber (muted), contactName, contactEmail
    - Per-row actions: expand toggle, Edit button (shows editForm inline replacing row content), Remove button (shows inline "Confirm remove?" text with Confirm/Cancel)
    - Expanded area: full-width CprWeekTable below sub info row, inside same card
    - If subs list is empty and not addingNew: EmptyState with heading="No subcontractors" message="Track CPR receipt and compliance status for each subcontractor on this project."

    DESIGN TOKEN RULES (from CLAUDE.md — strictly enforced):
    - Use bg-surface-card, bg-surface-page, text-brand-gold, border-brand-gold, font-headline, font-body
    - NEVER hardcode #hex values
    - Section heading: font-headline text-lg text-gray-900
    - Input classes: border border-border-default rounded px-2 py-1 text-sm bg-surface-page (matches existing notif panel inputs)
    - Expand chevron button: text-sm font-medium text-gray-700 hover:text-brand-gold transition-colors

    CRITICAL: Do NOT pass sub CPR data to WorkflowProgress or any compliance rollup. The SubcontractorsPanel is display-only tracking — subs are never mixed into GC counts.
  </action>
  <verify>
    <automated>cd C:/Users/glcar/prevailing-wage && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>TypeScript compiles with no new errors. SubcontractorsPanel is rendered in ProjectDetailPage inside {project && (...)} block. All mutations use correct query key arrays for invalidation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of SubcontractorsPanel</name>
  <what-built>
    Full SubcontractorsPanel in ProjectDetailPage with add/edit/remove subs, expandable per-sub CPR week table, status badges, and mark received/compliant actions. getCprStatus pure function extracted to src/client/lib/cprStatus.ts with unit tests passing.
  </what-built>
  <how-to-verify>
    1. Start the dev server: `npm run dev` (server port 4099)
    2. Open http://localhost:4099 and log in, then open any active project
    3. Scroll below the Notifications button — confirm "Subcontractors" section appears
    4. Click "Add Subcontractor" — confirm inline form appears with Name (required), License Number, Contact Name, Contact Email, Address fields
    5. Add a test sub named "Test Sub Co" with license "LIC-001" — confirm it appears in the list
    6. Click the sub row expand chevron — confirm it rotates and a CPR week table area opens
    7. In the CPR week area, add a record for a week ending date 10 days ago (no receivedDate) — confirm the badge shows "Overdue" in amber
    8. Add another record for today's date (no receivedDate) — confirm badge shows "Not Received" in gray
    9. Click "Mark Received" on the Overdue row — confirm receivedDate populates and badge changes to "Received — Non-Compliant" in red
    10. Click "Mark Compliant" — confirm badge changes to "Received — Compliant" in green
    11. Click Edit on the sub — confirm edit form prefills with existing values, save updates the row
    12. Click Remove on the sub — confirm inline "Confirm remove?" prompt appears, then confirm deletion removes the sub
    13. Confirm WorkflowProgress indicator (steps at top of page) does NOT change when subs are added/removed
    14. Run full test suite: `npm test` — confirm all tests pass (no regressions)
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
  <action>Human verifies the panel visually per the steps above. No automated action — all automation completed in Tasks 1 and 2.</action>
  <verify>
    <automated>cd C:/Users/glcar/prevailing-wage && npm test 2>&1 | tail -5</automated>
  </verify>
  <done>All four badge states confirmed in browser, add/edit/remove flows work, WorkflowProgress unchanged, full test suite green.</done>
</task>

</tasks>

<verification>
`npx vitest run tests/lib/cprStatus.test.ts` — all getCprStatus unit tests pass
`npx tsc --noEmit` — no TypeScript errors introduced by SubcontractorsPanel
`npm test` — full suite green (no regressions in existing tests)
Human verification: SubcontractorsPanel visible on ProjectDetailPage with all badge states confirmed working
</verification>

<success_criteria>
1. ProjectDetailPage has a Subcontractors section listing all subs with add/edit/remove controls
2. Each sub is expandable; CPR week rows show correct badge: Received+Compliant (green), Received+Non-Compliant (red), Not Received (gray), Overdue (amber)
3. Overdue badge fires correctly when weekEndingDate > 7 days ago and CPR not received
4. User can mark a week as Received and toggle between Compliant/Non-Compliant
5. Subcontractor data never appears in WorkflowProgress, GC totals, or compliance rollups
6. All 4+ getCprStatus unit tests pass green
7. Full test suite remains green
</success_criteria>

<output>
After completion, create `.planning/phases/56-subcontractor-ui-panel/56-01-SUMMARY.md`
</output>
