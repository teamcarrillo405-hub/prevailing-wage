---
phase: 114-svg-compliance-map
plan: 02
type: execute
wave: 2
depends_on:
  - 114-01
files_modified:
  - src/client/pages/LandingPage.tsx
autonomous: false
requirements:
  - UI-17
must_haves:
  truths:
    - "StateCoverageSection in LandingPage.tsx is replaced by UsComplianceMap component"
    - "The section heading '8 States — More Coming' is preserved above the map"
    - "The abbreviation-box grid (flex flex-wrap of 2-char boxes) is gone"
    - "The map renders correctly in the browser at 375px, 768px, and 1280px"
  artifacts:
    - path: "src/client/pages/LandingPage.tsx"
      provides: "StateCoverageSection replaced with UsComplianceMap"
      contains: "UsComplianceMap"
  key_links:
    - from: "src/client/pages/LandingPage.tsx StateCoverageSection"
      to: "src/client/components/UsComplianceMap.tsx"
      via: "import UsComplianceMap; render <UsComplianceMap />"
      pattern: "UsComplianceMap"
---

<objective>
Replace the StateCoverageSection abbreviation-box grid in LandingPage.tsx with the UsComplianceMap component, then visually verify the result at three breakpoints.

Purpose: The abbreviation boxes (w-12 h-12 squares with state codes) are the weak element identified in the ROADMAP. The SVG map is the replacement — this plan wires it in and confirms visual correctness.

Output: LandingPage.tsx StateCoverageSection body replaced with UsComplianceMap; visual checkpoint passed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md

<interfaces>
<!-- Current StateCoverageSection in LandingPage.tsx (lines 403-474): -->
The existing StateCoverageSection function renders:
1. A section wrapper: `<section className="bg-white py-20 px-6">`
2. A heading: "8 States — More Coming"
3. A subheading: "Native certified payroll forms..."
4. A "State-specific forms available now" subsection with flex-wrap boxes (8 active states as w-12 h-12 bg-nav-dark text-brand-gold boxes)
5. A "Coming soon" subsection with flex-wrap boxes (gray boxes)
6. A footer note about WH-347

The replacement keeps the section wrapper, heading, and subheading — but replaces items 4, 5, and 6 with a single `<UsComplianceMap />` render.

Import to add at the top of LandingPage.tsx:
```typescript
import { UsComplianceMap } from '../components/UsComplianceMap.js';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire UsComplianceMap into LandingPage StateCoverageSection</name>
  <files>src/client/pages/LandingPage.tsx</files>
  <action>
    Open `src/client/pages/LandingPage.tsx`.

    1. Add import at the top (with the other component imports):
       ```typescript
       import { UsComplianceMap } from '../components/UsComplianceMap.js';
       ```

    2. Find the `StateCoverageSection` function (around line 404). Replace its entire return value with:

    ```tsx
    return (
      <section className="bg-white py-20 px-6">
        <div className="max-w-5xl mx-auto text-center">
          <h2 className="text-3xl font-bold text-nav-dark font-headline mb-4">
            8 States — More Coming
          </h2>
          <p className="text-gray-600 mb-10 max-w-2xl mx-auto">
            Native certified payroll forms for every active state.
            Federal WH-347 supported for all 50 states.
          </p>
          <UsComplianceMap />
        </div>
      </section>
    );
    ```

    Remove the entire body of the old StateCoverageSection (the activeStates array, comingSoonStates array, the two flex-wrap div blocks, and the footer note paragraph). Keep the function signature and the new return above.

    The function's internal const declarations (`activeStates`, `comingSoonStates`) are now unused — delete them to keep the file clean and avoid TypeScript unused-variable warnings.

    After the edit, verify the build:
    ```bash
    cd /c/Users/glcar/prevailing-wage && npm run build 2>&1 | grep -E "error TS" | head -10
    ```
  </action>
  <verify>
    <automated>cd /c/Users/glcar/prevailing-wage && npm run build 2>&1 | grep -c "error TS" | xargs -I{} test {} -eq 0 && echo "BUILD_CLEAN" || echo "HAS_ERRORS"</automated>
  </verify>
  <done>
    - LandingPage.tsx imports UsComplianceMap
    - StateCoverageSection renders UsComplianceMap instead of abbreviation boxes
    - No activeStates or comingSoonStates arrays remain in the function
    - TypeScript build exits 0
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Replaced the state abbreviation-box grid in the Landing Page "8 States — More Coming" section with an inline SVG choropleth map. Active states (WA, CA, NY, MA, NJ, IL, MN, VA) appear dark with gold stroke; all others appear light gray. A tooltip shows state name and form status on hover. A legend sits below the map.
  </what-built>
  <how-to-verify>
    1. Start the dev server: `cd /c/Users/glcar/prevailing-wage && npm run dev`
    2. Open http://localhost:5173 (Vite port) in a browser
    3. Scroll to the "8 States — More Coming" section
    4. Confirm: the old abbreviation boxes (w-12 h-12 squares) are GONE
    5. Confirm: a geographic USA SVG map is visible, dark states clearly distinct from light states
    6. Hover over California (west coast), New York (northeast), Illinois (midwest) — tooltip should appear with state name and form name (e.g., "California — A-1-131")
    7. Hover over a gray state (e.g., Texas) — tooltip should say "Texas — Federal WH-347 only"
    8. Resize browser to 375px width — map should scale down without horizontal scroll or overflow
    9. Check that Alaska and Hawaii appear as insets in the lower-left of the map
    10. Check that the legend ("State-specific form available" / "Federal WH-347 only") renders below the map
  </how-to-verify>
  <resume-signal>Type "approved" if the map renders correctly at all three checks. Describe any visual issues (wrong colors, missing states, overflow at mobile) if not approved.</resume-signal>
</task>

</tasks>

<verification>
```bash
cd /c/Users/glcar/prevailing-wage && npm run build 2>&1 | tail -5
cd /c/Users/glcar/prevailing-wage && grep "UsComplianceMap" src/client/pages/LandingPage.tsx
```
LandingPage.tsx references UsComplianceMap; build passes.
</verification>

<success_criteria>
- LandingPage.tsx uses UsComplianceMap — no abbreviation boxes remain in StateCoverageSection
- Map renders in browser with correct dark/light state distinction
- Tooltip appears on hover with correct state name and form label
- Map responsive at 375px, 768px, 1280px (no horizontal overflow)
- TypeScript build exits 0
- Human verifier approves the visual result
</success_criteria>

<output>
After completion, create `.planning/phases/114-svg-compliance-map/114-02-SUMMARY.md`
</output>
