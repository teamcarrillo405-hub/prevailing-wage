---
phase: 114-svg-compliance-map
plan: 01
type: execute
wave: 1
depends_on:
  - 113-01
files_modified:
  - src/client/components/UsComplianceMap.tsx
autonomous: true
requirements:
  - UI-17
must_haves:
  truths:
    - "UsComplianceMap.tsx renders 50 state paths as inline SVG with no external map library"
    - "Active states (WA, CA, NY, MA, NJ, IL, MN, VA) are filled with bg-nav-dark color and have a gold stroke"
    - "Inactive states are filled #f3f4f6 with a light gray stroke"
    - "Alaska and Hawaii appear as inset boxes in the lower-left corner"
    - "Hovering a state shows a tooltip with state name and form status"
    - "Component is responsive: fills its container at 375px, 768px, and 1280px"
  artifacts:
    - path: "src/client/components/UsComplianceMap.tsx"
      provides: "50-state SVG choropleth component"
      exports: ["UsComplianceMap"]
  key_links:
    - from: "src/client/components/UsComplianceMap.tsx"
      to: "src/client/pages/LandingPage.tsx StateCoverageSection"
      via: "Used in plan 114-02 to replace StateCoverageSection"
      pattern: "UsComplianceMap"
---

<objective>
Create the UsComplianceMap React component with 50 inline SVG state paths.

Purpose: The existing StateCoverageSection shows abbreviation boxes — visually weak compared to competitors. A proper geographic choropleth immediately communicates "these states are live" without the user having to recognize abbreviations.

Output: src/client/components/UsComplianceMap.tsx — self-contained, no external deps.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md

<interfaces>
<!-- Design token context -->
Brand tokens:
- Active state fill: use inline style `fill: '#1a1a1a'` (bg-nav-dark hex) — CSS variables don't work inside SVG fill attributes
- Active state stroke: '#F5C518' (brand-gold hex)
- Active state stroke-width: 1.5
- Inactive state fill: '#f3f4f6' (gray-100)
- Inactive state stroke: '#d1d5db' (gray-300)
- Inactive state stroke-width: 0.5

Tooltip pattern (no library — pure React state):
```tsx
const [tooltip, setTooltip] = useState<{ x: number; y: number; label: string } | null>(null);
// On path mouseEnter: setTooltip({ x: e.clientX, y: e.clientY, label: `${stateName} — ${formLabel}` })
// On path mouseLeave: setTooltip(null)
// Render: tooltip && <div style={{position:'fixed', left:tooltip.x+12, top:tooltip.y-8}} ...>{tooltip.label}</div>
```

Active states and their form labels:
- WA → "F700-065-000"
- CA → "A-1-131"
- NY → "PW-12"
- MA → "MA Certified Payroll"
- NJ → "NJ Certified Payroll"
- IL → "IL Certified Transcript"
- MN → "MN Certified Payroll"
- VA → "VA Certified Payroll"

Inactive states label: "Federal WH-347 only"

SVG viewBox: "0 0 960 600" (standard US map dimensions)
Component should be:
- A named export `UsComplianceMap`
- Accepts no required props (all data is internal constants)
- Returns a div wrapper with className="relative w-full" and max-width set inline
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create UsComplianceMap.tsx with 50-state SVG paths</name>
  <files>src/client/components/UsComplianceMap.tsx</files>
  <action>
    Create `src/client/components/UsComplianceMap.tsx`.

    Use a compact but geographically accurate set of SVG path data for all 50 US states. The recommended approach is the standard US Albers projection path data that is widely available in the public domain (originally from the US Census TIGER data, simplified for web use). The final SVG file must be under 80 KB.

    **Structure of the component:**

    ```tsx
    import { useState } from 'react';

    // Active states with certified payroll forms
    const ACTIVE_STATES: Record<string, string> = {
      WA: 'F700-065-000',
      CA: 'A-1-131',
      NY: 'PW-12',
      MA: 'MA Certified Payroll',
      NJ: 'NJ Certified Payroll',
      IL: 'IL Certified Transcript',
      MN: 'MN Certified Payroll',
      VA: 'VA Certified Payroll',
    };

    // State abbreviation to full name
    const STATE_NAMES: Record<string, string> = {
      AL: 'Alabama', AK: 'Alaska', AZ: 'Arizona', AR: 'Arkansas', CA: 'California',
      CO: 'Colorado', CT: 'Connecticut', DE: 'Delaware', FL: 'Florida', GA: 'Georgia',
      HI: 'Hawaii', ID: 'Idaho', IL: 'Illinois', IN: 'Indiana', IA: 'Iowa',
      KS: 'Kansas', KY: 'Kentucky', LA: 'Louisiana', ME: 'Maine', MD: 'Maryland',
      MA: 'Massachusetts', MI: 'Michigan', MN: 'Minnesota', MS: 'Mississippi', MO: 'Missouri',
      MT: 'Montana', NE: 'Nebraska', NV: 'Nevada', NH: 'New Hampshire', NJ: 'New Jersey',
      NM: 'New Mexico', NY: 'New York', NC: 'North Carolina', ND: 'North Dakota', OH: 'Ohio',
      OK: 'Oklahoma', OR: 'Oregon', PA: 'Pennsylvania', RI: 'Rhode Island', SC: 'South Carolina',
      SD: 'South Dakota', TN: 'Tennessee', TX: 'Texas', UT: 'Utah', VT: 'Vermont',
      VA: 'Virginia', WA: 'Washington', WV: 'West Virginia', WI: 'Wisconsin', WY: 'Wyoming',
    };

    interface StatePathProps {
      abbr: string;
      d: string;
      isActive: boolean;
      onHover: (abbr: string, e: React.MouseEvent) => void;
      onLeave: () => void;
    }

    function StatePath({ abbr, d, isActive, onHover, onLeave }: StatePathProps) {
      return (
        <path
          d={d}
          fill={isActive ? '#1a1a1a' : '#f3f4f6'}
          stroke={isActive ? '#F5C518' : '#d1d5db'}
          strokeWidth={isActive ? 1.5 : 0.5}
          style={{ cursor: 'default', transition: 'opacity 0.15s' }}
          onMouseEnter={(e) => onHover(abbr, e)}
          onMouseLeave={onLeave}
          onMouseMove={(e) => onHover(abbr, e)}
        />
      );
    }

    export function UsComplianceMap() {
      const [tooltip, setTooltip] = useState<{ x: number; y: number; label: string } | null>(null);

      function handleHover(abbr: string, e: React.MouseEvent) {
        const formLabel = ACTIVE_STATES[abbr] ?? 'Federal WH-347 only';
        setTooltip({
          x: e.clientX,
          y: e.clientY,
          label: `${STATE_NAMES[abbr] ?? abbr} — ${formLabel}`,
        });
      }

      return (
        <div className="relative w-full">
          <svg
            viewBox="0 0 960 600"
            style={{ width: '100%', height: 'auto', maxHeight: '480px' }}
            aria-label="US state compliance coverage map"
          >
            {/* 50 state paths — see path data below */}
            {STATE_PATHS.map(({ abbr, d }) => (
              <StatePath
                key={abbr}
                abbr={abbr}
                d={d}
                isActive={abbr in ACTIVE_STATES}
                onHover={handleHover}
                onLeave={() => setTooltip(null)}
              />
            ))}
          </svg>

          {/* Tooltip */}
          {tooltip && (
            <div
              style={{
                position: 'fixed',
                left: tooltip.x + 14,
                top: tooltip.y - 36,
                pointerEvents: 'none',
                zIndex: 9999,
              }}
              className="bg-nav-dark text-white text-xs px-3 py-1.5 rounded shadow-lg whitespace-nowrap font-body"
            >
              {tooltip.label}
            </div>
          )}

          {/* Legend */}
          <div className="flex items-center gap-6 mt-4 justify-center">
            <div className="flex items-center gap-2">
              <div className="w-4 h-4 rounded-sm bg-nav-dark border border-brand-gold" />
              <span className="text-xs text-gray-600 font-body">State-specific form available</span>
            </div>
            <div className="flex items-center gap-2">
              <div className="w-4 h-4 rounded-sm bg-gray-100 border border-gray-300" />
              <span className="text-xs text-gray-600 font-body">Federal WH-347 only</span>
            </div>
          </div>
        </div>
      );
    }
    ```

    For the `STATE_PATHS` array: use compact Albers USA projection path data. The paths below are simplified from the public domain Natural Earth / Census data. Alaska and Hawaii are repositioned as insets (bottom-left of the viewBox at 0 0 960 600 — Alaska near x=80,y=460, Hawaii near x=200,y=520).

    Use the following approach: define a `STATE_PATHS` array of `{ abbr: string; d: string }` objects. The path data should be the standard simplified US state boundaries commonly used in D3.js examples (these are public domain). If you have access to a reliable set of simplified US state SVG paths (e.g., from the `us-atlas` npm package's topojson, converted to SVG paths), use those. Otherwise, construct simplified rectangular/polygon approximations that are geographically recognizable — the requirement is that the map is visually readable, not pixel-perfect.

    **Minimum viable path data strategy** (if exact paths are not available in context):
    Use a grid-aligned approximation where each state is a polygon at its approximate geographic position within a 960x600 viewBox. The paths do NOT need to be perfect outlines — they need to be visually identifiable at the state level (correct relative position, rough shape). This is acceptable per the ROADMAP: "simplified path data or a compact SVG."

    Alaska inset: small polygon at bottom-left (approx x: 50-160, y: 440-560)
    Hawaii inset: small polygon to the right of Alaska (approx x: 180-240, y: 500-560)

    The file must be under 80 KB when written to disk.

    After writing the file, verify it compiles:
    ```bash
    cd /c/Users/glcar/prevailing-wage && npx tsc --noEmit 2>&1 | grep "UsComplianceMap" | head -10
    ```
  </action>
  <verify>
    <automated>cd /c/Users/glcar/prevailing-wage && test -f src/client/components/UsComplianceMap.tsx && wc -c src/client/components/UsComplianceMap.tsx | awk '{print ($1 < 81920) ? "SIZE_OK" : "TOO_LARGE: " $1}'</automated>
  </verify>
  <done>
    - UsComplianceMap.tsx exists and is under 80 KB
    - File exports UsComplianceMap named export
    - Contains STATE_PATHS array with entries for all 50 states (grep: count of abbr: entries = 50)
    - Active states (WA, CA, NY, MA, NJ, IL, MN, VA) use fill '#1a1a1a'
    - TypeScript finds no errors in the file
  </done>
</task>

</tasks>

<verification>
```bash
cd /c/Users/glcar/prevailing-wage && grep -c "abbr:" src/client/components/UsComplianceMap.tsx
cd /c/Users/glcar/prevailing-wage && npx tsc --noEmit 2>&1 | grep "UsComplianceMap"
```
50 state entries present; no TypeScript errors.
</verification>

<success_criteria>
- UsComplianceMap.tsx under 80 KB
- 50 state path entries in STATE_PATHS
- Active states (WA, CA, NY, MA, NJ, IL, MN, VA) filled #1a1a1a with gold stroke
- Hover tooltip shows state name and form label
- Legend rendered below map
- No external map library imported (no mapbox, leaflet, d3-geo, etc.)
- TypeScript compiles without error
</success_criteria>

<output>
After completion, create `.planning/phases/114-svg-compliance-map/114-01-SUMMARY.md`
</output>
