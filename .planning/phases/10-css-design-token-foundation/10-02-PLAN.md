---
phase: 10-css-design-token-foundation
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - src/client/components/wages/ManualWageEntryForm.tsx
  - src/client/components/wages/WageClassificationsTable.tsx
  - src/client/pages/AdminStateWagePage.tsx
  - src/client/pages/WageLookupPage.tsx
  - src/client/pages/ReportsPage.tsx
autonomous: false
requirements: [DESIGN-03]
must_haves:
  truths:
    - "No JSX file contains style={{ backgroundColor: '#F5C518' }} or style={{ fontFamily: 'Oswald' }}"
    - "All 7 former inline-style locations render the same visual result using className utilities"
    - "WageClassificationsTable header row shows gold background in browser (tr element background renders correctly)"
    - "ReportsPage section headings render in Oswald font via font-headline className"
  artifacts:
    - path: "src/client/components/wages/ManualWageEntryForm.tsx"
      provides: "bg-brand-gold className replacing inline style"
    - path: "src/client/components/wages/WageClassificationsTable.tsx"
      provides: "bg-brand-gold on <tr> element — verified in browser"
    - path: "src/client/pages/ReportsPage.tsx"
      provides: "font-headline className replacing 3 inline fontFamily styles; focus:outline-hidden replacing 1 focus:outline-none instance"
  key_links:
    - from: "WageClassificationsTable.tsx"
      to: "bg-brand-gold CSS class"
      via: "className attribute on <tr>"
      pattern: "className=\".*bg-brand-gold"
---

<objective>
Replace all 7 hardcoded inline brand values in JSX with design token utility classes, and migrate the 1 focus:outline-none instance in ReportsPage.tsx to focus:outline-hidden.

Purpose: Phase 10 success criterion 3 requires zero remaining `style={{ backgroundColor: '#F5C518' }}` or `style={{ fontFamily: 'Oswald' }}` inline values. This plan executes the migration across 5 files (7 instances total). ReportsPage.tsx's focus:outline-none is also handled here to consolidate all ReportsPage edits in one plan and avoid parallel write conflicts with Plan 03.
Output: 5 modified TSX files where brand values reference tokens, not hex literals or font string literals. ReportsPage.tsx has no remaining focus:outline-none.
</objective>

<execution_context>
@C:/Users/glcar/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/glcar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-css-design-token-foundation/10-RESEARCH.md
@.planning/phases/10-css-design-token-foundation/10-01-SUMMARY.md

<interfaces>
<!-- Exact inline style instances to replace — sourced from live codebase audit (10-RESEARCH.md) -->

Instance 1 — ManualWageEntryForm.tsx ~line 169:
  BEFORE: style={{ backgroundColor: '#F5C518' }}
  AFTER:  className="bg-brand-gold"
  Context: Likely a button or badge element

Instance 2 — WageClassificationsTable.tsx ~line 23:
  BEFORE: style={{ backgroundColor: '#F5C518' }}
  AFTER:  className="bg-brand-gold"
  Context: <tr> element — requires browser verify (Pitfall 2 in RESEARCH.md)
  Fallback if bg-brand-gold fails on <tr>: use style={{ backgroundColor: 'var(--color-brand-gold)' }}

Instance 3 — AdminStateWagePage.tsx ~line 121:
  BEFORE: style={{ backgroundColor: '#F5C518' }}
  AFTER:  className="bg-brand-gold"

Instance 4 — WageLookupPage.tsx ~line 81:
  BEFORE: style={{ backgroundColor: '#F5C518' }}
  AFTER:  className="bg-brand-gold"

Instances 5, 6, 7 — ReportsPage.tsx ~lines 140, 169, 242:
  BEFORE: style={{ fontFamily: 'Oswald, sans-serif' }} (or similar fontFamily variant)
  AFTER:  className="font-headline"
  Note: After @layer base in Plan 01, h1-h4 headings already get Oswald. If these elements
  are already heading elements (h1-h4), the className is still correct but redundant — apply it
  anyway for explicit intent. Do NOT skip the migration on the assumption @layer base covers it.

Instance 8 — ReportsPage.tsx (1 instance of focus:outline-none):
  BEFORE: focus:outline-none (in a className string)
  AFTER:  focus:outline-hidden
  Note: This is a TailwindCSS v4 rename. Mechanical string replacement only — do not change
  any other classes on the element. This is bundled here to avoid concurrent file writes
  with Plan 03 which handles the same migration across other files.

SCOPE BOUNDARY — do NOT touch these:
  - className="...[#F5C518]..." — arbitrary value className instances (65 total, Phase 11-14 scope)
  - Any style={{}} that does not contain '#F5C518', 'Oswald', or 'Inter'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate 4 backgroundColor inline styles to bg-brand-gold</name>
  <files>
    src/client/components/wages/ManualWageEntryForm.tsx,
    src/client/components/wages/WageClassificationsTable.tsx,
    src/client/pages/AdminStateWagePage.tsx,
    src/client/pages/WageLookupPage.tsx
  </files>
  <action>
Read each of the 4 files. Locate the exact `style={{ backgroundColor: '#F5C518' }}` attribute.

For each instance, replace the inline style prop with a className addition:
- If the element already has a className prop: add "bg-brand-gold" to the existing className string
- If the element has no className prop: add `className="bg-brand-gold"`
- Remove the `style={{ backgroundColor: '#F5C518' }}` prop entirely

For conditional inline styles like `style={{ backgroundColor: loading ? undefined : '#F5C518' }}`, convert to:
`className={loading ? '' : 'bg-brand-gold'}`

Do NOT touch any className attributes that contain `[#F5C518]` — those are out of Phase 10 scope.
Do NOT change any other styling, logic, or structure in these files.

After editing all 4 files, run the grep check:
`grep -rn "backgroundColor.*F5C518" src/client/components/wages/ src/client/pages/AdminStateWagePage.tsx src/client/pages/WageLookupPage.tsx`
Expected result: 0 matches.
  </action>
  <verify>
    <automated>grep -rn "backgroundColor.*F5C518" C:/Users/glcar/prevailing-wage/src/client/components/wages/ C:/Users/glcar/prevailing-wage/src/client/pages/AdminStateWagePage.tsx C:/Users/glcar/prevailing-wage/src/client/pages/WageLookupPage.tsx 2>/dev/null | wc -l</automated>
  </verify>
  <done>grep returns 0 results for backgroundColor + F5C518 across all 4 files. Each former inline style location now has bg-brand-gold in its className. npm test suite passes (regression guard).</done>
</task>

<task type="auto">
  <name>Task 2: Migrate 3 fontFamily inline styles in ReportsPage to font-headline, then migrate 1 focus:outline-none to focus:outline-hidden</name>
  <files>src/client/pages/ReportsPage.tsx</files>
  <action>
Read ReportsPage.tsx. Perform two separate migration operations on this file:

**Operation A — fontFamily inline styles (3 instances):**
Locate the 3 instances at approximately lines 140, 169, and 242 that contain `style={{ fontFamily: 'Oswald, sans-serif' }}` or `style={{ fontFamily: "Oswald, sans-serif" }}` or any variant with the string "Oswald" in an inline style prop.

For each instance:
- Add "font-headline" to the element's className (or create className="font-headline" if none exists)
- Remove the `style={{ fontFamily: ... }}` prop entirely

After Operation A, run:
`grep -n "fontFamily.*Oswald\|fontFamily.*Inter" src/client/pages/ReportsPage.tsx`
Expected result: 0 matches.

**Operation B — focus:outline-none migration (1 instance):**
Locate the 1 instance of `focus:outline-none` in ReportsPage.tsx.
Replace it with `focus:outline-hidden`. This is a mechanical string replacement — do not change any other classes on the element.

After Operation B, run:
`grep -n "focus:outline-none" src/client/pages/ReportsPage.tsx`
Expected result: 0 matches.

Do NOT touch className attributes containing [#F5C518] in this file — out of scope.
Do NOT change any other styling or logic.
  </action>
  <verify>
    <automated>grep -n "fontFamily.*Oswald\|fontFamily.*Inter\|focus:outline-none" C:/Users/glcar/prevailing-wage/src/client/pages/ReportsPage.tsx | wc -l</automated>
  </verify>
  <done>grep returns 0 results for fontFamily+Oswald/Inter inline styles and focus:outline-none in ReportsPage.tsx. All 3 fontFamily instances now use className="font-headline". The 1 focus:outline-none instance is now focus:outline-hidden. npm test suite passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify all gold elements render correctly in browser</name>
  <action>
Open the app in browser and verify the following. For the WageClassificationsTable tr case: if bg-brand-gold does not render on the tr element, apply the fallback — change className="bg-brand-gold" on the tr to style={{ backgroundColor: 'var(--color-brand-gold)' }} (CSS variable instead of hex literal, which still references the token).

Steps:
1. Start dev server if not running: `npm run dev`
2. Open http://localhost:4099 in browser
3. Navigate to wage classifications or wage lookup table — verify WageClassificationsTable header row shows GOLD background
4. Navigate to Reports page — verify section headings render in Oswald (visually distinct from Inter body text)
5. Navigate to Admin State Wage and Wage Lookup pages — verify gold buttons/badges still show gold
6. Run shell confirm: `grep -rn "backgroundColor.*F5C518|fontFamily.*Oswald|fontFamily.*Inter" src/client --include="*.tsx"` — expect 0 results
  </action>
  <verify>
    <automated>grep -rn "backgroundColor.*F5C518\|fontFamily.*Oswald\|fontFamily.*Inter" C:/Users/glcar/prevailing-wage/src/client --include="*.tsx" 2>/dev/null | wc -l</automated>
  </verify>
  <done>User confirms all gold elements render correctly. WageClassificationsTable header row shows gold background (via bg-brand-gold or the CSS variable fallback). ReportsPage headings render in Oswald. Shell grep returns 0 inline brand style instances.</done>
  <what-built>
    Migrated all 7 inline brand style instances across 5 files, plus 1 focus:outline-none in ReportsPage:
    - ManualWageEntryForm.tsx (1 backgroundColor → bg-brand-gold)
    - WageClassificationsTable.tsx (1 backgroundColor on tr → bg-brand-gold)
    - AdminStateWagePage.tsx (1 backgroundColor → bg-brand-gold)
    - WageLookupPage.tsx (1 backgroundColor → bg-brand-gold)
    - ReportsPage.tsx (3 fontFamily → font-headline; 1 focus:outline-none → focus:outline-hidden)

    CRITICAL: WageClassificationsTable uses bg-brand-gold on a tr element — tr background-color has CSS specificity quirks in some browsers. Must verify in browser before signing off.
  </what-built>
  <how-to-verify>
    Navigate to WageClassificationsTable in browser. The header row must show gold background. If white/no background: apply the CSS variable fallback (see action above).
  </how-to-verify>
  <resume-signal>Type "approved" if all gold elements render correctly, or describe any rendering issue (especially the WageClassificationsTable tr case) so the fallback can be applied.</resume-signal>
</task>

</tasks>

<verification>
Shell check — run after all tasks complete:
```
grep -rn "style={{" src/client --include="*.tsx" | grep -E "F5C518|Oswald|Inter"
```
Expected: 0 results — confirms all 7 inline brand style instances are gone.

```
grep -n "focus:outline-none" src/client/pages/ReportsPage.tsx
```
Expected: 0 results — confirms ReportsPage focus migration is complete.

Then run full regression suite:
```
npm run test -- --run
```
Expected: 181 tests pass — confirms no server-side regressions from TSX edits.
</verification>

<success_criteria>
- Zero `style={{ backgroundColor: '#F5C518' }}` instances remaining in any TSX file
- Zero `style={{ fontFamily: 'Oswald' }}` or `style={{ fontFamily: "Oswald" }}` instances remaining
- Zero `focus:outline-none` instances remaining in ReportsPage.tsx
- All 5 affected pages render visually identical to before (gold colors still gold, Oswald still renders)
- WageClassificationsTable header row shows gold background in browser (either via bg-brand-gold className OR var(--color-brand-gold) fallback)
- npm test suite: 181 passing
</success_criteria>

<output>
After completion, create `.planning/phases/10-css-design-token-foundation/10-02-SUMMARY.md`
</output>
