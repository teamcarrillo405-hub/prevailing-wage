---
phase: 112-phase-b-gate-v8
plan: 01
type: execute
wave: 1
depends_on:
  - 111-02
files_modified:
  - .planning/phases/112-phase-b-gate-v8/112-SCORE.md
autonomous: true
requirements:
  - ENT-03
  - ENT-04
  - ENT-05
must_haves:
  truths:
    - "112-SCORE.md exists with 6 gate criteria graded C1-C6"
    - "All 6 criteria pass (Y) or total score >= 9.1/10"
    - "GATE_PASS or GATE_FAIL is explicitly declared in the score file"
  artifacts:
    - path: ".planning/phases/112-phase-b-gate-v8/112-SCORE.md"
      provides: "Phase B gate evaluation with 6 criteria"
      contains: "GATE_PASS"
  key_links:
    - from: "112-SCORE.md criteria"
      to: "src/server/routes/sso.ts"
      via: "automated grep checks for route handlers"
      pattern: "GET.*metadata|POST.*acs|GET.*login"
---

<objective>
Run automated evidence checks for Phase B's 6 gate criteria and write 112-SCORE.md declaring GATE_PASS or GATE_FAIL.

Purpose: The ROADMAP.md Phase B Watchdog gate requires score >= 9.1/10 before Phase C (production hardening) can begin. This plan codifies the check so it is repeatable and auditable.

Output: `.planning/phases/112-phase-b-gate-v8/112-SCORE.md` with 6 criteria, scores, and gate declaration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run gate criterion checks and write 112-SCORE.md</name>
  <files>.planning/phases/112-phase-b-gate-v8/112-SCORE.md</files>
  <action>
    Run each check using bash commands, then write the score file.

    **C1 — ENT-03: sso_configs table exists in schema.ts**
    ```bash
    cd /c/Users/glcar/prevailing-wage && grep -c "ssoConfigs" src/server/db/schema.ts
    ```
    Pass if count > 0.

    **C2 — ENT-03: GET /api/sso/metadata route exists in sso.ts**
    ```bash
    cd /c/Users/glcar/prevailing-wage && grep -c "'/metadata'" src/server/routes/sso.ts
    ```
    Pass if count > 0.

    **C3 — ENT-04: POST /api/sso/acs route exists in sso.ts**
    ```bash
    cd /c/Users/glcar/prevailing-wage && grep -c "'/acs'" src/server/routes/sso.ts
    ```
    Pass if count > 0.

    **C4 — ENT-04: Replay protection (seenAssertionIds Map) exists in sso.ts**
    ```bash
    cd /c/Users/glcar/prevailing-wage && grep -c "seenAssertionIds" src/server/routes/sso.ts
    ```
    Pass if count > 0.

    **C5 — ENT-05: SsoConfigPage.tsx exists**
    ```bash
    cd /c/Users/glcar/prevailing-wage && test -f src/client/pages/SsoConfigPage.tsx && echo "PASS" || echo "FAIL"
    ```

    **C6 — Full test suite green**
    ```bash
    cd /c/Users/glcar/prevailing-wage && npm run test 2>&1 | tail -5
    ```
    Pass if output contains "passed" and no "failed" tests.

    Score each criterion 0 or 1.67 (6 criteria × 1.67 = ~10). Any criterion scoring 0 is a blocker.

    Write `.planning/phases/112-phase-b-gate-v8/112-SCORE.md` with this structure:

    ```markdown
    # Phase B Watchdog Gate — v8.0 SAML SSO

    **Date:** 2026-04-27
    **Required:** >= 9.1/10
    **Result:** [GATE_PASS | GATE_FAIL]

    ## Criteria

    | ID | Requirement | Check | Result | Score |
    |----|-------------|-------|--------|-------|
    | C1 | ENT-03: sso_configs table in schema.ts | grep ssoConfigs schema.ts | [Y/N] | [1.67/0] |
    | C2 | ENT-03: GET /api/sso/metadata route | grep '/metadata' sso.ts | [Y/N] | [1.67/0] |
    | C3 | ENT-04: POST /api/sso/acs route | grep '/acs' sso.ts | [Y/N] | [1.67/0] |
    | C4 | ENT-04: Replay protection (seenAssertionIds) | grep seenAssertionIds sso.ts | [Y/N] | [1.67/0] |
    | C5 | ENT-05: SsoConfigPage.tsx exists | file check | [Y/N] | [1.67/0] |
    | C6 | Full test suite green | npm run test | [Y/N] | [1.67/0] |

    **Total: [X]/10**

    ## Manual Verify Note
    Live Okta end-to-end test requires an Okta developer account. The SP metadata URL must be imported
    into the Okta admin panel and a test user must successfully log in via SSO. This manual step is
    performed by the developer before closing Phase B.

    ## Declaration
    **[GATE_PASS | GATE_FAIL]** — Phase C (production hardening) [may | may NOT] begin.
    ```

    If any criterion fails, still write the file with GATE_FAIL and list which criteria need fixing before re-running this plan.

    If all 6 pass (score = 10/10 or ≥ 9.1), write GATE_PASS.
  </action>
  <verify>
    <automated>cd /c/Users/glcar/prevailing-wage && test -f .planning/phases/112-phase-b-gate-v8/112-SCORE.md && grep -c "GATE_" .planning/phases/112-phase-b-gate-v8/112-SCORE.md</automated>
  </verify>
  <done>
    - 112-SCORE.md exists with all 6 criteria graded
    - GATE_PASS or GATE_FAIL declared
    - If GATE_FAIL: specific failing criteria identified so executor can re-run after fixes
  </done>
</task>

</tasks>

<verification>
```bash
cd /c/Users/glcar/prevailing-wage && cat .planning/phases/112-phase-b-gate-v8/112-SCORE.md
```
Score file readable; GATE_PASS or GATE_FAIL present; all 6 criteria rows filled in.
</verification>

<success_criteria>
- 112-SCORE.md written at correct path
- All 6 criteria evaluated against actual codebase state
- Score >= 9.1 → GATE_PASS declared
- Score < 9.1 → GATE_FAIL declared with specific failing criteria identified
</success_criteria>

<output>
After completion, create `.planning/phases/112-phase-b-gate-v8/112-01-SUMMARY.md`
</output>
