---
phase: 47-state-foundations-tx-certified-payroll
plan: 04
type: execute
wave: 3
depends_on: [47-02, 47-03]
files_modified:
  - src/client/pages/PayrollWeekDetailPage.tsx
autonomous: true
requirements: [TX-02, NFR-06]

must_haves:
  truths:
    - "TX projects show an LCPtracker informational callout on PayrollWeekDetailPage with a link to lcp123.com"
    - "The callout mentions Texas Chapter 2258 and electronic submission requirement"
    - "The callout includes a link to the TxDOT contractor compliance page"
    - "Non-TX projects do not show the LCPtracker callout"
  artifacts:
    - path: "src/client/pages/PayrollWeekDetailPage.tsx"
      provides: "TX LCPtracker HelpCallout component gated on isTX"
      contains: "LCPtracker"
  key_links:
    - from: "isTX boolean"
      to: "HelpCallout component"
      via: "{isTX && <HelpCallout ...>}"
      pattern: "isTX.*HelpCallout|isTX.*LCPtracker"
---

<objective>
Add TX LCPtracker informational callout to PayrollWeekDetailPage per TX-02.

Purpose: Texas contractors see guidance that electronic certified payroll submission is required via LCPtracker for TxDOT projects, with direct links to the portal and compliance page.
Output: HelpCallout component gated on isTX in PayrollWeekDetailPage.tsx.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-state-foundations-tx-certified-payroll/47-RESEARCH.md
@.planning/phases/47-state-foundations-tx-certified-payroll/47-02-SUMMARY.md

<interfaces>
From src/client/components/ui/HelpCallout.tsx:
```typescript
interface HelpCalloutProps {
  icon: LucideIcon;
  title: string;
  body: React.ReactNode;
  className?: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add TX LCPtracker HelpCallout to PayrollWeekDetailPage</name>
  <files>src/client/pages/PayrollWeekDetailPage.tsx</files>
  <read_first>
    src/client/pages/PayrollWeekDetailPage.tsx (lines 1-30 — imports, confirm HelpCallout and ExternalLink/AlertCircle are imported)
    src/client/pages/PayrollWeekDetailPage.tsx (search for "isTX" — confirm boolean exists from Plan 02)
    src/client/pages/PayrollWeekDetailPage.tsx (lines 1425-1450 — WA PWIA panel for placement reference)
    src/client/pages/PayrollWeekDetailPage.tsx (lines 1055-1070 — existing HelpCallout usage for pattern reference)
    src/client/components/ui/HelpCallout.tsx (full file — confirm props interface)
  </read_first>
  <action>
    Add a TX LCPtracker informational callout after the WA PWIA panel section (around line 1430+), gated on `isTX`:

    ```tsx
    {!isLoading && !isError && isTX && (
      <HelpCallout
        icon={ExternalLink}
        title="Texas LCPtracker Electronic Submission Required"
        body={<>
          Texas Chapter 2258 requires electronic submission of certified payroll
          records via LCPtracker for TxDOT and other public works contracts.
          Submit your WH-347 through the{' '}
          <a href="https://lcp123.com" target="_blank" rel="noopener noreferrer"
            className="text-brand-gold underline">
            LCPtracker portal (lcp123.com)
          </a>
          . Refer to the{' '}
          <a href="https://www.txdot.gov/business/contractors/labor-compliance.html"
            target="_blank" rel="noopener noreferrer"
            className="text-brand-gold underline">
            TxDOT contractor compliance page
          </a>
          {' '}for submission requirements.
        </>}
      />
    )}
    ```

    If `ExternalLink` is not already imported from lucide-react, add it to the existing lucide-react import line. Check the imports first — `AlertCircle` or `ExternalLink` may already be there. Use whichever icon is already imported that best fits (ExternalLink preferred since it references external portals; fall back to `AlertCircle` or `Info` if ExternalLink is not imported and you want to avoid adding a new import).

    Placement: After the WA PWIA panel block. The research confirmed the WA panel is around line 1428. Place the TX callout in the same section, after WA's panel but before the next major section.

    Do NOT:
    - Modify any existing panels or callouts
    - Remove or change the isTX boolean (added in Plan 02)
    - Add TX submission tracking row here (that's a separate concern for a future plan if needed)
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "LCPtracker" src/client/pages/PayrollWeekDetailPage.tsx && grep -c "lcp123.com" src/client/pages/PayrollWeekDetailPage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - PayrollWeekDetailPage.tsx contains string `LCPtracker` (at least 2 occurrences — title + body text)
    - PayrollWeekDetailPage.tsx contains string `lcp123.com`
    - PayrollWeekDetailPage.tsx contains string `txdot.gov/business/contractors/labor-compliance`
    - PayrollWeekDetailPage.tsx contains string `Texas Chapter 2258`
    - PayrollWeekDetailPage.tsx contains `isTX && (` or `isTX &&` gating the callout
    - PayrollWeekDetailPage.tsx contains `<HelpCallout` with `title="Texas LCPtracker` (or similar)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>TX LCPtracker callout renders for TX projects with links to lcp123.com and TxDOT compliance page; non-TX projects unaffected</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `grep "LCPtracker" src/client/pages/PayrollWeekDetailPage.tsx` shows callout text
- `grep "lcp123.com" src/client/pages/PayrollWeekDetailPage.tsx` shows portal link
- `grep "txdot.gov" src/client/pages/PayrollWeekDetailPage.tsx` shows compliance page link
- `npx vitest run --exclude ".claude/**"` — full suite passes (no regressions)
</verification>

<success_criteria>
TX projects display an LCPtracker informational callout with links to lcp123.com and TxDOT contractor compliance page. Non-TX projects show no TX-related UI. TypeScript compiles cleanly.
</success_criteria>

<output>
After completion, create `.planning/phases/47-state-foundations-tx-certified-payroll/47-04-SUMMARY.md`
</output>
