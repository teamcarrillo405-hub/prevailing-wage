---
phase: 47-state-foundations-tx-certified-payroll
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/client/pages/PayrollWeekDetailPage.tsx
  - src/client/pages/PayrollEntryPage.tsx
  - src/client/pages/WorkersPage.tsx
  - src/server/routes/export.ts
  - tests/routes/export.test.ts
autonomous: true
requirements: [STATE-13]

must_haves:
  truths:
    - "A project stored as lowercase 'ca' passes the CA A-1-131 state gate on the server"
    - "A project stored as lowercase 'wa' passes the WA F700 state gate on the server"
    - "A project stored as lowercase 'ca' passes the CA eCPR XML state gate on the server"
    - "A project stored as lowercase 'wa' passes the WA CPR XML state gate on the server"
    - "Client-side isCA/isWA booleans evaluate true for lowercase state values"
  artifacts:
    - path: "src/server/routes/export.ts"
      provides: "Normalized state gates for a1131, f700, ecpr-xml, wa-cpr-xml"
      contains: "project.state?.toUpperCase()"
    - path: "src/client/pages/PayrollWeekDetailPage.tsx"
      provides: "Normalized isCA and isWA booleans"
      contains: ".toUpperCase() === 'CA'"
    - path: "tests/routes/export.test.ts"
      provides: "STATE-13 integration tests"
      contains: "STATE-13"
  key_links:
    - from: "src/server/routes/export.ts"
      to: "project.state column"
      via: ".toUpperCase() normalization"
      pattern: "project\\.state\\?\\.toUpperCase\\(\\)"
---

<objective>
Normalize all state comparisons to `.toUpperCase()` across client and server per STATE-13.

Purpose: Prevents silent failures when project state is stored in lowercase (e.g., 'ca' instead of 'CA') before adding TX/FL/MA/NJ states in subsequent phases.
Output: 7 one-line changes across 4 files + 2 integration tests in export.test.ts confirming normalization works.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-state-foundations-tx-certified-payroll/47-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize state comparisons to .toUpperCase() in 4 files</name>
  <files>
    src/client/pages/PayrollWeekDetailPage.tsx
    src/client/pages/PayrollEntryPage.tsx
    src/client/pages/WorkersPage.tsx
    src/server/routes/export.ts
  </files>
  <read_first>
    src/client/pages/PayrollWeekDetailPage.tsx (lines 460-465 — isCA/isWA declarations)
    src/client/pages/PayrollEntryPage.tsx (lines 70-75 — isCA declaration)
    src/client/pages/WorkersPage.tsx (lines 178-183 — isWA declaration)
    src/server/routes/export.ts (lines 275-280, 385-390, 553-558, 734-740 — four state gates)
  </read_first>
  <action>
    Make exactly 7 one-line changes per STATE-13:

    1. PayrollWeekDetailPage.tsx line 461:
       BEFORE: `const isCA = projectData?.data?.project?.state === 'CA';`
       AFTER:  `const isCA = projectData?.data?.project?.state?.toUpperCase() === 'CA';`

    2. PayrollWeekDetailPage.tsx line 462:
       BEFORE: `const isWA = projectData?.data?.project?.state === 'WA';`
       AFTER:  `const isWA = projectData?.data?.project?.state?.toUpperCase() === 'WA';`

    3. PayrollEntryPage.tsx line 72:
       BEFORE: `const isCA = projectData?.data?.project?.state === 'CA';`
       AFTER:  `const isCA = projectData?.data?.project?.state?.toUpperCase() === 'CA';`

    4. WorkersPage.tsx line 180:
       BEFORE: `const isWA = projectData?.data?.project?.state === 'WA';`
       AFTER:  `const isWA = projectData?.data?.project?.state?.toUpperCase() === 'WA';`

    5. export.ts line ~278 (a1131 route):
       BEFORE: `if (project.state !== 'CA')`
       AFTER:  `if (project.state?.toUpperCase() !== 'CA')`

    6. export.ts line ~388 (f700 route):
       BEFORE: `if (project.state !== 'WA')`
       AFTER:  `if (project.state?.toUpperCase() !== 'WA')`

    7. export.ts line ~556 (ecpr-xml route):
       BEFORE: `if (project.state !== 'CA')`
       AFTER:  `if (project.state?.toUpperCase() !== 'CA')`

    8. export.ts line ~737 (wa-cpr-xml route):
       BEFORE: `if (project.state !== 'WA')`
       AFTER:  `if (project.state?.toUpperCase() !== 'WA')`

    Do NOT change:
    - PayrollWeekDetailPage.tsx lines 463-464 (isNY, isIL) — already use .toUpperCase()
    - PayrollEntryPage.tsx line 73 (isIL) — already uses .toUpperCase()
    - WorkersPage.tsx line 181 (isIL) — already uses .toUpperCase()
    - ProjectForm.tsx lines 54-57 — already normalized
    - complianceService.ts line 57 — already normalized
    - export.ts pw12 route (line 911) and il-transcript route (line 1123) — already normalized
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - PayrollWeekDetailPage.tsx contains `state?.toUpperCase() === 'CA'` on the isCA line
    - PayrollWeekDetailPage.tsx contains `state?.toUpperCase() === 'WA'` on the isWA line
    - PayrollEntryPage.tsx contains `state?.toUpperCase() === 'CA'`
    - WorkersPage.tsx contains `state?.toUpperCase() === 'WA'`
    - export.ts contains `project.state?.toUpperCase() !== 'CA'` in the a1131 route
    - export.ts contains `project.state?.toUpperCase() !== 'WA'` in the f700 route
    - export.ts contains `project.state?.toUpperCase() !== 'CA'` in the ecpr-xml route
    - export.ts contains `project.state?.toUpperCase() !== 'WA'` in the wa-cpr-xml route
    - Zero occurrences of `project.state !== 'CA'` or `project.state !== 'WA'` (exact match without toUpperCase) in export.ts
    - Zero occurrences of `?.state === 'CA'` or `?.state === 'WA'` (exact match without toUpperCase) in any client page file
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>All 7 state comparisons normalized; no exact-match CA/WA comparisons remain in the 4 files</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add STATE-13 integration tests to export.test.ts</name>
  <files>
    tests/routes/export.test.ts
  </files>
  <read_first>
    tests/routes/export.test.ts (read first 100 lines for imports/helpers, then search for existing describe blocks to find insertion point)
  </read_first>
  <behavior>
    - Test 1: POST /api/projects with state='ca' (lowercase), then GET /api/export/a1131/:weekId returns 400 (TX project, not CA — proves gate normalizes). Create project with state 'tx' lowercase, hit a1131 — should get 400 because tx != CA.
    - Test 2: POST /api/projects with state='ca' (lowercase), then GET /api/export/f700/:weekId returns 400 (CA project, not WA — proves gate normalizes). Create project with state 'ca' lowercase, hit f700 — should get 400 because ca != WA.
    - Test 3: POST /api/projects with state='ca' (lowercase), then GET /api/export/a1131/:weekId returns 200 (or at least not 400 state-gate error). This proves a lowercase 'ca' project passes the CA state gate.
  </behavior>
  <action>
    Add a new describe block `STATE-13: case normalization on export routes` in tests/routes/export.test.ts.

    Use the existing test helper pattern from the file (registerUser, createProject, createPayrollWeek, createPayrollEntry, createWorkerWithClassification — read the file to confirm exact signatures).

    Test 1 — non-CA project blocked by a1131 gate:
    ```typescript
    it('should return 400 for non-CA project on a1131 route', async () => {
      // Create a TX project with lowercase state
      const cookie = await registerUser('state13-a1131');
      const projectId = await createProject(cookie, 'tx');
      const weekId = await createPayrollWeek(cookie, projectId);
      const res = await supertest(app).get(`/api/export/a1131/${weekId}`).set('Cookie', cookie);
      expect(res.status).toBe(400);
    });
    ```

    Test 2 — non-WA project blocked by f700 gate:
    ```typescript
    it('should return 400 for non-WA project on f700 route', async () => {
      const cookie = await registerUser('state13-f700');
      const projectId = await createProject(cookie, 'ca');
      const weekId = await createPayrollWeek(cookie, projectId);
      const res = await supertest(app).get(`/api/export/f700/${weekId}`).set('Cookie', cookie);
      expect(res.status).toBe(400);
    });
    ```

    Test 3 — lowercase CA passes the a1131 gate (does NOT get 400):
    ```typescript
    it('should not return state-gate 400 for lowercase ca project on a1131', async () => {
      const cookie = await registerUser('state13-ca-lower');
      const projectId = await createProject(cookie, 'ca');  // lowercase
      const { workerId, classificationId } = await createWorkerWithClassification(cookie, projectId);
      const weekId = await createPayrollWeek(cookie, projectId);
      await createPayrollEntry(cookie, weekId, workerId, classificationId);
      const res = await supertest(app).get(`/api/export/a1131/${weekId}`).set('Cookie', cookie);
      // Should NOT be 400 (state gate) — may be 200 or other error, but not the state gate
      expect(res.status).not.toBe(400);
    });
    ```

    Adapt helper function signatures to match what actually exists in the file (read_first). If `createProject` takes a different signature, adjust accordingly.
  </action>
  <verify>
    <automated>npx vitest run tests/routes/export.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - export.test.ts contains `describe('STATE-13` or `describe("STATE-13`
    - export.test.ts contains at least 2 test cases with `state13` in their setup
    - `npx vitest run tests/routes/export.test.ts` exits 0
    - All STATE-13 tests pass (no failures in verbose output)
  </acceptance_criteria>
  <done>STATE-13 integration tests pass, confirming case normalization works on export routes</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes (TypeScript compilation)
- `npx vitest run tests/routes/export.test.ts` passes (all existing + new STATE-13 tests)
- `grep -rn "\.state === 'CA'" src/client/pages/` returns 0 results
- `grep -rn "\.state === 'WA'" src/client/pages/` returns 0 results
- `grep -n "project.state !== 'CA'" src/server/routes/export.ts` returns 0 results
- `grep -n "project.state !== 'WA'" src/server/routes/export.ts` returns 0 results
</verification>

<success_criteria>
All state comparisons in PayrollWeekDetailPage.tsx, PayrollEntryPage.tsx, WorkersPage.tsx, and export.ts use .toUpperCase() consistently. Integration tests confirm lowercase state values pass the correct gates.
</success_criteria>

<output>
After completion, create `.planning/phases/47-state-foundations-tx-certified-payroll/47-01-SUMMARY.md`
</output>
