---
phase: 13-landing-page-routing
plan: 03
type: execute
wave: 3
depends_on: [13-02]
files_modified:
  - src/client/pages/LandingPage.tsx
autonomous: false
requirements: [LANDING-04, LANDING-05, LANDING-06]
must_haves:
  truths:
    - "Feature highlights section renders 4-6 benefit cards using Card primitive"
    - "Trust signals section renders with compliance currency language ('January 2025 WH-347')"
    - "CTA close section renders 'Create Free Account' button linking to /register"
    - "Footer renders with login link and HCC brand"
    - "All six landing page sections present: hero, problem, how-it-works, feature highlights, trust signals, CTA close + footer"
  artifacts:
    - path: "src/client/pages/LandingPage.tsx"
      provides: "Complete six-section landing page"
      exports: ["LandingPage"]
      contains: "FeatureHighlightsSection, TrustSignalsSection, CTACloseSection, LandingFooter"
  key_links:
    - from: "CTACloseSection"
      to: "/register"
      via: "<Link to='/register'> wrapping <Button variant='primary'>"
      pattern: "to=\"/register\""
    - from: "LandingFooter"
      to: "/login"
      via: "<Link to='/login'>"
      pattern: "to=\"/login\""
---

<objective>
Complete LandingPage.tsx by appending four remaining sections: feature highlights, trust signals, CTA close, and footer. Then run a final human checkpoint to verify all six sections render and routing is correct.

Purpose: Completes LANDING-04 through LANDING-06. The trust signals section is the conversion closer — it names specific compliance artifacts (January 2025 WH-347 form) that prove product depth to skeptical contractors.
Output: Complete LandingPage.tsx with all six sections. Human sign-off on all Phase 13 success criteria.
</objective>

<execution_context>
@C:/Users/glcar/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/glcar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-landing-page-routing/13-RESEARCH.md
@.planning/phases/13-landing-page-routing/13-02-SUMMARY.md
</context>

<interfaces>
<!-- Key contracts. No codebase exploration required. -->

From src/client/components/ui/Button.tsx:
```tsx
export function Button({ variant = 'primary', size = 'md', className, children, ...props }: ButtonProps)
// variant: 'primary' | 'secondary' | 'ghost'
// size: 'sm' | 'md'
```

From src/client/components/ui/Card.tsx:
```tsx
export function Card({ children, padding = 'default', className }: CardProps)
// padding: 'default' | 'sm' | 'none'
```

Available lucide-react icons (already installed from Plan 01):
- Shield, CheckCircle, Clock, FileText, Database, AlertTriangle, TrendingUp, Lock
- Import: `import { Shield, CheckCircle, ... } from 'lucide-react';`

Available design tokens:
- bg-nav-dark, bg-surface-page, bg-surface-card, bg-brand-gold
- text-brand-gold, text-text-primary, text-text-secondary, text-white
- border-brand-gold, border-border-default
- font-headline (Oswald), shadow-card, rounded-card
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Append feature highlights, trust signals, CTA close, and footer to LandingPage.tsx</name>
  <files>src/client/pages/LandingPage.tsx</files>
  <action>
Read the current LandingPage.tsx (written in Plan 02). Add four new inline section functions above the `LandingPage` export, then update the `LandingPage` return to include all sections.

Add these lucide-react icon imports to the existing import line (merge, don't duplicate the import):
```tsx
import { FolderPlus, ClipboardList, FileCheck, Shield, CheckCircle, Clock, FileText, Database } from 'lucide-react';
```

**Section 5 — FeatureHighlightsSection (LANDING-04):**
- Background: `bg-surface-page`
- Padding: `py-20 px-4`
- Heading (Oswald, centered): "Everything You Need for Davis-Bacon Compliance"
- Subhead (text-text-secondary, centered): "Built for general contractors who can't afford compliance mistakes"
- 6 feature cards in a responsive grid (`grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 max-w-6xl mx-auto mt-12`)
- Each card uses `<Card>` with an icon, bold benefit title, and 1-sentence description
- Frame as BENEFITS (what the user gains), not FEATURES (what the system does):
  1. Icon: `<Database />` — "Wage Rates Auto-Populated" — "Pull current Davis-Bacon rates from SAM.gov by project location — no manual lookup, no spreadsheet imports."
  2. Icon: `<FileText />` — "WH-347 in One Click" — "Generate a complete, pre-filled January 2025 certified payroll form ready to submit to your contracting officer."
  3. Icon: `<Shield />` — "Violation Detection Before You File" — "The system flags underpayment and CWHSSA overtime violations before you generate the WH-347 — not after a DOL audit."
  4. Icon: `<CheckCircle />` — "Apprentice Compliance Built In" — "Track program name, registration number, and J/RA ratio for apprentice workers — required on the 2025 form."
  5. Icon: `<Clock />` — "Minutes, Not Hours" — "Contractors report completing weekly certified payroll in under 10 minutes once project wage rates are loaded."
  6. Icon: `<FileText />` — "Full Audit Trail" — "Every payroll entry is timestamped and stored. Retrieve any week's data instantly if a project is audited."
- Icon styling: `className="w-6 h-6 text-brand-gold mb-3"`

**Section 6 — TrustSignalsSection (LANDING-05):**
- Background: `bg-nav-dark text-white`
- Padding: `py-16 px-4`
- Max width: `max-w-4xl mx-auto`
- Layout: centered, two visual blocks stacked or side-by-side
- Block 1 — Compliance currency statement (key conversion signal):
  - Gold left-border accent: `border-l-4 border-brand-gold pl-6`
  - Heading (Oswald, text-white): "Current as of January 2025"
  - Body: "The January 2025 WH-347 form revision is the only version accepted by the Department of Labor. HCC Prevailing Wage generates this exact form — not a generic PDF approximation."
- Block 2 — Regulatory alignment:
  - Three short trust statements, each with a gold checkmark icon (`<CheckCircle className="w-5 h-5 text-brand-gold" />`) and text
  - Statements:
    - "Davis-Bacon and Related Acts (DBRA) compliance for all federal contracts over $2,000"
    - "CWHSSA overtime detection for contracts over $100,000 with 40-hour workweeks"
    - "SAM.gov wage determination data — the same source contracting officers use"
- No testimonials (not in scope). No stock photos. Copy specificity is the trust signal.

**Section 7 — CTACloseSection + LandingFooter (LANDING-06):**
Build as two separate inline functions: `CTACloseSection` and `LandingFooter`.

CTACloseSection:
- Background: `bg-brand-gold`
- Padding: `py-16 px-4 text-center`
- Heading (Oswald, text-nav-dark, large): "Ready to File Your First Certified Payroll?"
- Subhead (text-nav-dark/80): "Free to start. No credit card required."
- CTA button: `<Link to="/register"><Button variant="primary" size="md" className="bg-nav-dark text-brand-gold hover:bg-nav-dark/90">Create Free Account</Button></Link>`
  - Note: override Button colors for gold-background context — dark button on gold bg reads clearly

LandingFooter:
- Background: `bg-nav-dark text-gray-400`
- Padding: `py-8 px-6`
- Layout: `flex justify-between items-center flex-wrap gap-4`
- Left: "HCC Prevailing Wage" in `text-white font-headline text-sm`
- Center/right nav links (text-gray-400, hover:text-brand-gold, text-sm):
  - `<Link to="/login">Log In</Link>`
  - `<a href="mailto:support@hcc.com">Contact</a>` (static email placeholder)
- Bottom micro-copy: `text-xs text-gray-600 text-center mt-4` — "Davis-Bacon and Related Acts compliance software for federal construction contractors."

**Final LandingPage export — include ALL eight section renders:**
```tsx
export function LandingPage() {
  return (
    <div className="min-h-screen">
      <LandingNav />
      <HeroSection />
      <ProblemSection />
      <HowItWorksSection />
      <FeatureHighlightsSection />
      <TrustSignalsSection />
      <CTACloseSection />
      <LandingFooter />
    </div>
  );
}
```

Do NOT use hardcoded hex values. Use only design tokens. Do NOT add motion/animations. Do NOT wrap in Layout.tsx.
  </action>
  <verify>
    <automated>npm run test -- --run 2>&1 | tail -5</automated>
  </verify>
  <done>LandingPage.tsx compiles. Dev server shows all four new sections below the How It Works section. CTACloseSection "Create Free Account" links to /register. Footer has login link. 181-test suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Browser verification — all Phase 13 success criteria</name>
  <files>none — human verification only</files>
  <what-built>
Complete LandingPage.tsx with all six sections (hero, problem, how-it-works, feature highlights, trust signals, CTA close + footer), PublicRoute guard, RegisterPage, and auth-aware App.tsx routing.
  </what-built>
  <how-to-verify>
Start the dev server: `npm run dev` (server on port 4099 per project decision).

1. LOGGED-OUT routing:
   - Navigate to "/" — landing page renders (not a redirect)
   - Verify hero text contains "WH-347", "Davis-Bacon", and "SAM.gov" visible without scrolling at 1280x800
   - Verify "Create Free Account" (primary gold button) is visible in hero
   - Click "See How It Works" anchor link — page scrolls to How It Works section
   - Click "Create Free Account" — URL changes to /register
   - /register page shows a registration form (not login form)
   - Navigate to "/nonexistent-page" — redirects to "/"

2. LOGGED-IN routing:
   - Log in with a valid account
   - Navigate to "/" — immediately redirects to /dashboard
   - Navigate to "/nonexistent-page" — immediately redirects to /dashboard

3. FULL PAGE scroll verification:
   - Scroll through all sections: hero → problem (3 cards) → how it works (3 steps with gold icons) → feature highlights (6 cards) → trust signals (compliance currency + 3 checkmarks) → CTA close (gold bg, dark button) → footer (login link)

4. Run `npm run test -- --run` — all 181 tests pass
  </how-to-verify>
  <action>Human browser verification — no automated action. See how-to-verify above.</action>
  <verify>
    <automated>MISSING — this is a checkpoint:human-verify task. Run `npm run test -- --run 2>&1 | tail -10` to confirm full suite still green before presenting to user.</automated>
  </verify>
  <done>User types "approved" after confirming all Phase 13 routing and visual success criteria in browser.</done>
  <resume-signal>Type "approved" to complete Phase 13, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Full Phase 13 verification checklist:
- [ ] "/" logged out → landing page renders (not redirect)
- [ ] "/" logged in → /dashboard redirect
- [ ] "/register" → RegisterPage with form
- [ ] Unknown URL logged out → "/"
- [ ] Unknown URL logged in → /dashboard
- [ ] Hero contains WH-347, Davis-Bacon, SAM.gov above fold (1280x800)
- [ ] "Create Free Account" CTA → /register
- [ ] "See How It Works" → scrolls to #how-it-works
- [ ] All 6 sections render top to bottom
- [ ] No hardcoded hex values in LandingPage.tsx, PublicRoute.tsx, RegisterPage.tsx
- [ ] npm run test -- --run: 181 tests green
</verification>

<success_criteria>
- All six landing page sections render in order without errors
- "January 2025 WH-347" appears in trust signals copy
- Both "Create Free Account" CTAs (hero and CTA close) navigate to /register
- Footer login link navigates to /login
- All routing success criteria verified by human in browser
- All 181 existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/13-landing-page-routing/13-03-SUMMARY.md`
</output>
