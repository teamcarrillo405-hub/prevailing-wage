---
phase: 116-phase-d-gate-v8-ship
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/116-phase-d-gate-v8-ship/116-SCORE.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [OPS-04]

must_haves:
  truths:
    - "116-SCORE.md exists with one row per criterion (C1-C10) showing PASS or FAIL with evidence command"
    - "A numeric score is computed deterministically: 1.0 point per criterion, 10 points total"
    - "GATE_PASS or GATE_FAIL is declared based on score >= 9.2"
    - "If GATE_PASS: git tag v8.0.0 is created and ROADMAP.md phases 107-116 are marked complete"
    - "If GATE_FAIL: each failing criterion has a specific fix description with file path"
    - "Full test suite run result is recorded with pass count and any failures"
  artifacts:
    - path: ".planning/phases/116-phase-d-gate-v8-ship/116-SCORE.md"
      provides: "Evidence record with per-criterion verdicts and final score"
      contains: "GATE_PASS or GATE_FAIL"
  key_links:
    - from: "116-SCORE.md"
      to: "phases 107-115 artifacts"
      via: "grep/test/vitest/git commands as evidence"
      pattern: "PASS|FAIL"
    - from: "GATE_PASS verdict"
      to: "git tag v8.0.0"
      via: "git tag -a command"
      pattern: "v8.0.0"
---

<objective>
Run the v8.0 Watchdog Gate: execute all 10 acceptance criterion checks spanning phases 107-115, compute a score, run the full test suite, and write 116-SCORE.md declaring GATE_PASS (score >= 9.2/10) or GATE_FAIL.

If GATE_PASS: create git tag v8.0.0, push tag, and mark ROADMAP.md phases 107-116 complete.

Purpose: Final gate enforcement — v8.0 does not ship until every feature is verified and on record.

Output: 116-SCORE.md with final verdict, git tag v8.0.0 on GATE_PASS, updated ROADMAP.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run all 10 gate criterion checks and integrity suite</name>
  <files>.planning/phases/116-phase-d-gate-v8-ship/116-SCORE.md</files>
  <action>
Run each check below in sequence from the repo root. Use `cd /c/Users/glcar/prevailing-wage` before each command block. Record PASS or FAIL. Do NOT interpret ambiguous output as PASS.

---

**Phase 107 — DBE Classification Flag on Subcontractors (C1-C2)**

C1 — dbeClassification column exists on subcontractors table:
```bash
grep -n "dbeClassification\|dbe_classification" src/server/db/schema.ts
```
PASS if output contains `dbe_classification`. FAIL if empty.

C2 — subcontractorId FK column exists on payroll_entries table:
```bash
grep -n "subcontractorId\|subcontractor_id" src/server/db/schema.ts
```
PASS if output contains `subcontractor_id`. FAIL if empty.

---

**Phase 108 — DBE Flag on Payroll Line Items (C3-C4)**

C3 — GET /api/projects/:id/reports/dbe-participation route registered:
```bash
grep -rn "dbe-participation" src/server/routes/
```
PASS if output contains `dbe-participation`. FAIL if empty.

C4 — DbeParticipationCard or DbeParticipation component exists in client:
```bash
grep -rn "DbeParticipation\|dbe-participation" src/client/
```
PASS if any file path returned. FAIL if empty.

---

**Phase 110 + 111 — SAML SSO (C5-C6)**

C5 — SAML ACS route registered (samlCallback or /auth/saml or /sso/acs):
```bash
grep -rn "saml\|/acs\|SamlStrategy\|passport-saml\|@node-saml" src/server/routes/ src/server/index.ts 2>/dev/null
```
PASS if any SAML route handler found. FAIL if empty.

C6 — SsoConfigPage component exists:
```bash
grep -rn "SsoConfigPage\|/settings/sso" src/client/ 2>/dev/null
```
PASS if found. FAIL if empty.

---

**Phase 113 — Render Health Check + Deploy Runbook (C7-C8)**

C7 — render.yaml contains healthCheckPath:
```bash
grep "healthCheckPath" render.yaml 2>/dev/null
```
PASS if output contains `healthCheckPath`. FAIL if file missing or no match.

C8 — DEPLOY.md exists at repo root:
```bash
test -f DEPLOY.md && echo "PASS" || echo "FAIL"
```

---

**Phase 115 — Per-Seat Billing Quotas (C9-C10)**

C9 — GET /api/billing/usage route registered:
```bash
grep -n "billing/usage\|router.get.*usage" src/server/routes/billing.ts
```
PASS if found. FAIL if empty.

C10 — BillingPage usage bars exist (UsageBar or billing-usage queryKey):
```bash
grep -n "billing-usage\|UsageBar\|usageBar\|projectCount\|workerCount" src/client/pages/BillingPage.tsx
```
PASS if any match. FAIL if empty.

---

**Integrity Checks**

TypeScript check (pre-existing known errors are exempt):
```bash
npx tsc --noEmit 2>&1 | grep -v "workers.ts(108\|115)\|stripeService.*Stripe" | grep "error TS" | wc -l
```
Record the count. PASS if count = 0. FAIL if count > 0 (list the errors).

Full test suite:
```bash
npx vitest run 2>&1 | tail -5
```
Record the pass count. PASS if >= 850 tests passing, 0 failures. FAIL if any failures or count < 850.

---

**Score Calculation**

| # | Criterion | Phase | Result |
|---|-----------|-------|--------|
| C1 | dbeClassification column on subcontractors | 107 | [PASS/FAIL] |
| C2 | subcontractorId FK on payroll_entries | 107 | [PASS/FAIL] |
| C3 | GET /api/.../dbe-participation route | 108 | [PASS/FAIL] |
| C4 | DbeParticipationCard component | 108 | [PASS/FAIL] |
| C5 | SAML ACS route registered | 110+111 | [PASS/FAIL] |
| C6 | SsoConfigPage component | 110+111 | [PASS/FAIL] |
| C7 | render.yaml healthCheckPath | 113 | [PASS/FAIL] |
| C8 | DEPLOY.md at repo root | 113 | [PASS/FAIL] |
| C9 | GET /api/billing/usage route | 115 | [PASS/FAIL] |
| C10 | BillingPage usage bars | 115 | [PASS/FAIL] |

Score = SUM(PASS count) / 10 × 10.0

Write all results to `.planning/phases/116-phase-d-gate-v8-ship/116-SCORE.md` using the template in Task 2.
  </action>
  <verify>
    <automated>test -f /c/Users/glcar/prevailing-wage/.planning/phases/116-phase-d-gate-v8-ship/116-SCORE.md && echo "SCORE.md created" || echo "MISSING"</automated>
  </verify>
  <done>116-SCORE.md exists with all 10 criterion rows, integrity check results, and a computed numeric score.</done>
</task>

<task type="auto">
  <name>Task 2: Write 116-SCORE.md, declare gate verdict, tag and update ROADMAP on PASS</name>
  <files>.planning/phases/116-phase-d-gate-v8-ship/116-SCORE.md, .planning/ROADMAP.md</files>
  <action>
Write 116-SCORE.md using this exact template (fill in evidence and results from Task 1):

```markdown
# 116-SCORE.md — v8.0 Watchdog Gate

**Date:** [ISO date]
**Phase:** 116 — Phase D Gate + v8.0 Ship
**Threshold:** 9.2 / 10.0

## Criterion Results

| Criterion | Description | Phase | Evidence Command | Result |
|-----------|-------------|-------|-----------------|--------|
| C1 | dbeClassification column on subcontractors | 107 | `grep -n "dbe_classification" src/server/db/schema.ts` | PASS/FAIL |
| C2 | subcontractorId FK on payroll_entries | 107 | `grep -n "subcontractor_id" src/server/db/schema.ts` | PASS/FAIL |
| C3 | GET /api/.../dbe-participation route | 108 | `grep -rn "dbe-participation" src/server/routes/` | PASS/FAIL |
| C4 | DbeParticipationCard component | 108 | `grep -rn "DbeParticipation" src/client/` | PASS/FAIL |
| C5 | SAML ACS route registered | 110+111 | `grep -rn "saml" src/server/routes/ src/server/index.ts` | PASS/FAIL |
| C6 | SsoConfigPage component | 110+111 | `grep -rn "SsoConfigPage" src/client/` | PASS/FAIL |
| C7 | render.yaml healthCheckPath | 113 | `grep "healthCheckPath" render.yaml` | PASS/FAIL |
| C8 | DEPLOY.md at repo root | 113 | `test -f DEPLOY.md` | PASS/FAIL |
| C9 | GET /api/billing/usage route | 115 | `grep -n "billing/usage" src/server/routes/billing.ts` | PASS/FAIL |
| C10 | BillingPage usage bars | 115 | `grep -n "billing-usage" src/client/pages/BillingPage.tsx` | PASS/FAIL |

## Integrity Checks

| Check | Result |
|-------|--------|
| TypeScript (new errors beyond 2 known) | [0 errors / N errors] |
| Vitest full suite | [N passing / N failing] |

## Score

**Pass Count:** N / 10
**Score:** N.N / 10.0
**Threshold:** 9.2 / 10.0

## Verdict

[GATE_PASS or GATE_FAIL]

[If GATE_FAIL: list each failing criterion with specific file path to fix and what change is needed]

[If GATE_PASS: confirm v8.0.0 tag created]
```

---

After writing SCORE.md:

**If GATE_PASS (score >= 9.2):**

1. Create and push the git tag:
```bash
cd /c/Users/glcar/prevailing-wage
git tag -a v8.0.0 -m "v8.0 Production-Ready + DBE Parity + Enterprise SAML"
git push origin v8.0.0
```

2. Update ROADMAP.md: Find the v8.0 Progress table and change all Not Started rows for phases 107-116 to Complete with today's date (2026-04-27). Find the v8.0 phase list items and change `- [ ]` to `- [x]` for each of phases 107-116.

3. Git commit the SCORE.md and ROADMAP.md updates:
```bash
git add .planning/phases/116-phase-d-gate-v8-ship/116-SCORE.md .planning/ROADMAP.md
git commit -m "feat(v8.0): GATE_PASS — v8.0.0 shipped; 116-SCORE.md on record"
```

**If GATE_FAIL (score < 9.2):**

1. Write GATE_FAIL in SCORE.md verdict section.
2. List each failing criterion with: which file to edit, what grep to run to confirm the fix, and what code/config change is needed.
3. Do NOT create the v8.0.0 tag.
4. Do NOT update ROADMAP.md.
5. Return the SCORE.md contents to the user with clear guidance on which phases to revisit.
  </action>
  <verify>
    <automated>grep -q "GATE_PASS\|GATE_FAIL" /c/Users/glcar/prevailing-wage/.planning/phases/116-phase-d-gate-v8-ship/116-SCORE.md && echo "Verdict found" || echo "Verdict missing"</automated>
  </verify>
  <done>116-SCORE.md declares GATE_PASS or GATE_FAIL with numeric score. On GATE_PASS: v8.0.0 tag exists, ROADMAP.md phases 107-116 are marked Complete, and a git commit captures the evidence. On GATE_FAIL: each failing criterion has a fix path.</done>
</task>

</tasks>

<verification>
# Confirm SCORE.md exists with verdict
grep "GATE_PASS\|GATE_FAIL" /c/Users/glcar/prevailing-wage/.planning/phases/116-phase-d-gate-v8-ship/116-SCORE.md

# On GATE_PASS — confirm tag
git -C /c/Users/glcar/prevailing-wage tag | grep v8.0.0

# Confirm ROADMAP updated
grep "116.*Complete" /c/Users/glcar/prevailing-wage/.planning/ROADMAP.md
</verification>

<success_criteria>
- 116-SCORE.md exists with all 10 criterion rows, evidence commands, and a numeric score
- Full Vitest run result (pass count) recorded in SCORE.md
- TypeScript new-error count recorded (0 = PASS)
- GATE_PASS declared if score >= 9.2/10
- On GATE_PASS: git tag v8.0.0 created and pushed, ROADMAP.md phases 107-116 marked Complete
- On GATE_FAIL: each failing criterion documents the exact file + change needed; no tag created
</success_criteria>

<output>
After completion, create `.planning/phases/116-phase-d-gate-v8-ship/116-01-SUMMARY.md` with:
- Final score (N/10)
- GATE_PASS or GATE_FAIL
- Which criteria passed and which failed
- Tag status
- ROADMAP update status
</output>
