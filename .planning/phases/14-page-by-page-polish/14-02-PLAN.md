---
phase: 14-page-by-page-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/client/pages/WorkersPage.tsx
  - src/client/pages/PayrollEntryPage.tsx
  - src/client/pages/ProjectDetailPage.tsx
autonomous: true
requirements:
  - PAGE-02
  - PAGE-03
  - PAGE-04

must_haves:
  truths:
    - "WorkersPage missing-data warning uses Badge component with variant='warning' — not inline bg-amber-100 span"
    - "WorkersPage 'Save Changes' and 'Add Worker' buttons use Button primitive instead of hardcoded bg-[#F5C518] buttons"
    - "WorkersPage header uses PageHeader primitive instead of ad-hoc flex/h1 div"
    - "PayrollEntryPage empty state (no workers) uses EmptyState primitive — not custom rounded-lg border-dashed div"
    - "PayrollEntryPage 'Try with sample workers' button uses Button primary variant"
    - "PayrollEntryPage header uses PageHeader primitive"
    - "ProjectDetailPage funding type badge uses Badge primitive with variant='neutral' — not inline bg-[#F5C518] dd element"
    - "ProjectDetailPage nav links use secondary button styling classes (border-brand-gold text-brand-gold) — not inline-block rounded border bg-white links"
  artifacts:
    - path: "src/client/pages/WorkersPage.tsx"
      provides: "Badge for missing-data warnings, Button for form actions, PageHeader for title"
      contains: "Badge"
    - path: "src/client/pages/PayrollEntryPage.tsx"
      provides: "EmptyState for no-workers case, Button for sample workers CTA, PageHeader for title"
      contains: "EmptyState"
    - path: "src/client/pages/ProjectDetailPage.tsx"
      provides: "Badge for funding type, Button for nav links"
      contains: "Badge"
  key_links:
    - from: "src/client/pages/WorkersPage.tsx"
      to: "src/client/components/ui/Badge.tsx"
      via: "import { Badge }"
      pattern: "import.*Badge"
    - from: "src/client/pages/PayrollEntryPage.tsx"
      to: "src/client/components/ui/EmptyState.tsx"
      via: "import { EmptyState }"
      pattern: "import.*EmptyState"
    - from: "src/client/pages/ProjectDetailPage.tsx"
      to: "src/client/components/ui/Badge.tsx"
      via: "import { Badge }"
      pattern: "import.*Badge"
---

<objective>
Replace ad-hoc inline styling on WorkersPage, PayrollEntryPage, and ProjectDetailPage with Card, Badge, Button, PageHeader, and EmptyState primitives.

Purpose: PAGE-02, PAGE-03, PAGE-04 each require specific primitive adoption. All three pages are independent (no shared file mutations) so they execute in the same wave.

Output: Three pages with consistent primitive usage — no hardcoded brand hex values, no custom badge spans, no ad-hoc button styles on primary CTAs.
</objective>

<execution_context>
@C:/Users/glcar/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/glcar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/glcar/prevailing-wage/.planning/PROJECT.md
@C:/Users/glcar/prevailing-wage/.planning/ROADMAP.md
@C:/Users/glcar/prevailing-wage/src/client/pages/WorkersPage.tsx
@C:/Users/glcar/prevailing-wage/src/client/pages/PayrollEntryPage.tsx
@C:/Users/glcar/prevailing-wage/src/client/pages/ProjectDetailPage.tsx
</context>

<interfaces>
<!-- Key component contracts. No codebase exploration required. -->

From src/client/components/ui/Badge.tsx:
```typescript
type BadgeVariant = 'compliant' | 'violation' | 'warning' | 'neutral';
interface BadgeProps {
  variant: BadgeVariant;
  children: React.ReactNode;
  className?: string;
}
export function Badge({ variant, children, className }: BadgeProps)
// warning variant: bg-status-warning/10 text-status-warning border border-status-warning/30
// neutral variant: bg-gray-100 text-gray-600 border border-gray-300
```

From src/client/components/ui/Button.tsx:
```typescript
type ButtonVariant = 'primary' | 'secondary' | 'ghost';
type ButtonSize = 'sm' | 'md';
export function Button({ variant = 'primary', size = 'md', ...props }: ButtonProps)
// primary: bg-brand-gold text-nav-dark
// secondary: bg-transparent text-brand-gold border border-brand-gold hover:bg-brand-gold/10
// ghost: bg-transparent text-text-secondary border border-transparent hover:bg-gray-100
```

From src/client/components/ui/EmptyState.tsx:
```typescript
interface EmptyStateProps {
  heading: string;
  message: string;
  action?: React.ReactNode;
  className?: string;
}
export function EmptyState({ heading, message, action, className }: EmptyStateProps)
```

From src/client/components/ui/PageHeader.tsx:
```typescript
interface PageHeaderProps {
  title: string;
  subtitle?: string;
  action?: React.ReactNode;
  className?: string;
}
export function PageHeader({ title, subtitle, action, className }: PageHeaderProps)
```

From src/client/components/ui/Card.tsx:
```typescript
// Already imported in WorkersPage — no new import needed there
interface CardProps {
  children: React.ReactNode;
  padding?: 'default' | 'sm' | 'none';
  className?: string;
}
export function Card({ children, padding = 'default', className }: CardProps)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: WorkersPage — Badge for missing-data warning, Button for form actions, PageHeader for title</name>
  <files>src/client/pages/WorkersPage.tsx</files>
  <action>
    Add imports at top of file (after existing imports):
    ```tsx
    import { Badge } from '../components/ui/Badge';
    import { Button } from '../components/ui/Button';
    import { PageHeader } from '../components/ui/PageHeader';
    ```

    **Header section (lines 223-229):** Replace the ad-hoc flex div + h1 with PageHeader. Remove the back-button from the header row — keep it as a standalone button above PageHeader OR keep the existing pattern of embedding it. Best approach: keep the back button as its own line above PageHeader:
    ```tsx
    <button onClick={() => navigate(`/projects/${projectId}`)} className="text-sm text-gray-500 hover:text-gray-900 transition-colors mb-4 inline-block">
      &larr; Back to Project
    </button>
    <PageHeader title="Workers" />
    ```
    Remove the old `<div className="mb-6 flex items-center gap-4">` wrapper and its `<h1>` child.

    **Missing-data warning (line 334-336):** Replace the inline `<span className="inline-block text-xs bg-amber-100 text-amber-700 px-2 py-0.5 rounded mt-1">` with:
    ```tsx
    <Badge variant="warning" className="mt-1">Missing data — WH-347 blocked</Badge>
    ```

    **"Save Changes" button in edit form (line 297-299):** Replace:
    ```tsx
    <button onClick={() => handleEditSave(w.id)} disabled={updateWorker.isPending}
      className="px-4 py-2 bg-[#F5C518] text-gray-900 text-sm font-medium rounded hover:bg-yellow-400 disabled:opacity-50 transition-colors">
      {updateWorker.isPending ? 'Saving...' : 'Save Changes'}
    </button>
    ```
    With:
    ```tsx
    <Button onClick={() => handleEditSave(w.id)} disabled={updateWorker.isPending}>
      {updateWorker.isPending ? 'Saving...' : 'Save Changes'}
    </Button>
    ```

    **"Cancel" ghost button in edit form (line 301-303):** Replace raw button with:
    ```tsx
    <Button variant="ghost" onClick={() => { setEditingId(null); setEditError(''); }}>Cancel</Button>
    ```

    **"+ Add Worker" submit button at bottom (lines 568-572):** Replace:
    ```tsx
    <button onClick={handleSubmit} disabled={addWorker.isPending}
      className="mt-5 px-5 py-2 bg-[#F5C518] text-gray-900 text-sm font-medium rounded hover:bg-yellow-400 disabled:opacity-50 transition-colors"
    >
      {addWorker.isPending ? 'Saving...' : '+ Add Worker'}
    </button>
    ```
    With:
    ```tsx
    <Button onClick={handleSubmit} disabled={addWorker.isPending} className="mt-5">
      {addWorker.isPending ? 'Saving...' : '+ Add Worker'}
    </Button>
    ```

    Do NOT change: Card usage, input focus rings, select styling, delete/edit action buttons (those are intentionally secondary/ghost style and can be left as raw buttons for now — they are not primary CTAs), classification rows, WD status banners.
  </action>
  <verify>
    <automated>cd C:/Users/glcar/prevailing-wage && npm run test -- --run 2>&1 | tail -5</automated>
  </verify>
  <done>181 tests pass. WorkersPage has no `bg-[#F5C518]` on buttons, uses Badge for missing-data warning, uses Button primary for "Save Changes" and "+ Add Worker", uses PageHeader for the page title.</done>
</task>

<task type="auto">
  <name>Task 2: PayrollEntryPage — EmptyState for no-workers, Button for sample CTA, PageHeader for title</name>
  <files>src/client/pages/PayrollEntryPage.tsx</files>
  <action>
    Add imports at top of file (after existing imports):
    ```tsx
    import { EmptyState } from '../components/ui/EmptyState';
    import { Button } from '../components/ui/Button';
    import { PageHeader } from '../components/ui/PageHeader';
    ```

    **Header section (lines 68-76):** Replace the ad-hoc flex div + back button + h1 with:
    ```tsx
    <button
      onClick={() => navigate(`/projects/${projectId}/payroll`)}
      className="text-sm text-gray-500 hover:text-gray-900 transition-colors mb-4 inline-block"
    >
      &larr; Back to Payroll Weeks
    </button>
    <PageHeader title="New Payroll Week" />
    ```
    Remove the old `<div className="mb-6 flex items-center gap-4">` wrapper.

    **Empty state block (lines 86-99):** Replace the entire `<div className="rounded-lg border border-dashed border-gray-300 bg-gray-50 p-8 text-center">` block with:
    ```tsx
    {!isLoading && !isError && workerRows.length === 0 && !useMock && (
      <EmptyState
        heading="No workers assigned yet"
        message="Add workers to this project first, or run a quick check with sample data to preview calculations."
        action={
          <Button onClick={() => setUseMock(true)}>
            Try with sample workers
          </Button>
        }
      />
    )}
    ```

    Keep the SamplePayrollForm and PayrollWeekForm render paths (lines 101-113) exactly as-is.
  </action>
  <verify>
    <automated>cd C:/Users/glcar/prevailing-wage && npm run test -- --run 2>&1 | tail -5</automated>
  </verify>
  <done>181 tests pass. PayrollEntryPage imports EmptyState and uses it for the no-workers branch. No `bg-[#F5C518]` remains in this file. PageHeader used for page title.</done>
</task>

<task type="auto">
  <name>Task 3: ProjectDetailPage — Badge for funding type, Button for nav links</name>
  <files>src/client/pages/ProjectDetailPage.tsx</files>
  <action>
    Add imports (Badge and Button not yet imported in this file; Card and PageHeader already are):
    ```tsx
    import { Badge } from '../components/ui/Badge';
    import { Button } from '../components/ui/Button';
    ```

    **Funding type `<dd>` (line 72-75):** Replace:
    ```tsx
    <dd className="inline-block text-xs font-medium px-2 py-0.5 bg-[#F5C518] text-gray-900 rounded">
      {FUNDING_TYPE_LABELS[project.fundingType] ?? project.fundingType}
    </dd>
    ```
    With:
    ```tsx
    <dd>
      <Badge variant="neutral">
        {FUNDING_TYPE_LABELS[project.fundingType] ?? project.fundingType}
      </Badge>
    </dd>
    ```

    **Nav link buttons (lines 92-123):** Replace each `<Link className="inline-block rounded border border-gray-200 bg-white px-4 py-2 text-sm font-medium text-gray-700 hover:bg-gray-50 transition-colors">` with `<Link>` wrapping a `<Button variant="secondary" size="sm">`. Use `asChild`-style pattern: since Button renders a `<button>` and Link renders an `<a>`, the cleanest approach is to use the Button's className directly on the Link. Apply the secondary button classes to the Link's className prop instead of wrapping:

    ```tsx
    <Link
      to={`/projects/${project.id}/workers`}
      className="inline-flex items-center justify-center text-xs px-3 py-1.5 font-semibold rounded-sm border border-brand-gold text-brand-gold hover:bg-brand-gold/10 transition-colors duration-150"
    >
      Workers
    </Link>
    ```
    Apply this same className pattern to all 5 nav links (Workers, Payroll Weeks, OT Scenario Planner, Variance, Reports). This matches Button secondary+sm styling without requiring asChild support.

    Do NOT change: Card, PageHeader, dl/dt rows, LoadingSpinner, error state.
  </action>
  <verify>
    <automated>cd C:/Users/glcar/prevailing-wage && npm run test -- --run 2>&1 | tail -5</automated>
  </verify>
  <done>181 tests pass. ProjectDetailPage has no `bg-[#F5C518]`. Funding type uses `&lt;Badge variant="neutral"&gt;`. Nav links use secondary button styling classes. No inline hardcoded hex values remain.</done>
</task>

</tasks>

<verification>
- `grep -rn "bg-\[#F5C518\]" src/client/pages/WorkersPage.tsx src/client/pages/PayrollEntryPage.tsx src/client/pages/ProjectDetailPage.tsx` returns no output
- `grep -n "Badge" src/client/pages/WorkersPage.tsx` returns at least 2 matches (import + usage)
- `grep -n "EmptyState" src/client/pages/PayrollEntryPage.tsx` returns at least 2 matches (import + usage)
- `grep -n "Badge" src/client/pages/ProjectDetailPage.tsx` returns at least 2 matches (import + usage)
- All 181 tests pass
</verification>

<success_criteria>
- WorkersPage: PageHeader for title, Badge for missing-data warning, Button primary for Save Changes / Add Worker
- PayrollEntryPage: PageHeader for title, EmptyState for no-workers branch, Button primary for sample workers CTA
- ProjectDetailPage: Badge neutral for funding type, secondary button styling for nav links
- No `bg-[#F5C518]` remains in any of the three files
- 181/181 tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/14-page-by-page-polish/14-02-SUMMARY.md`
</output>
