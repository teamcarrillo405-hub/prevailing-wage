---
phase: 14-page-by-page-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/client/pages/DashboardPage.tsx
  - src/client/pages/LoginPage.tsx
autonomous: true
requirements:
  - PAGE-01
  - PAGE-07

must_haves:
  truths:
    - "Empty dashboard shows EmptyState component with heading 'No projects yet' and action-prompt copy — not a raw div with inline text"
    - "LoginPage renders with bg-surface-page background class (not bg-gray-50)"
    - "LoginPage title border uses border-brand-gold token (not hardcoded #F5C518)"
    - "LoginPage is login-only — no mode state, no RegisterForm import, no toggle button; a link to /register exists instead"
  artifacts:
    - path: "src/client/pages/DashboardPage.tsx"
      provides: "EmptyState adoption for zero-projects state"
      contains: "<EmptyState"
    - path: "src/client/pages/LoginPage.tsx"
      provides: "Token-clean login-only page"
      contains: "border-brand-gold"
  key_links:
    - from: "src/client/pages/DashboardPage.tsx"
      to: "src/client/components/ui/EmptyState.tsx"
      via: "import { EmptyState }"
      pattern: "import.*EmptyState"
    - from: "src/client/pages/LoginPage.tsx"
      to: "/register route"
      via: "Link to='/register'"
      pattern: "to=.*register"
---

<objective>
Adopt the EmptyState primitive on DashboardPage and clean up LoginPage's hardcoded tokens + defunct mode-toggle pattern.

Purpose: PAGE-01 requires the empty dashboard to use EmptyState. PAGE-07 requires LoginPage to use design tokens and a consistent form layout. Both are small, independent changes that fit cleanly in Wave 1.

Output: DashboardPage uses `<EmptyState>` for the zero-projects case. LoginPage drops RegisterForm entirely, removes hardcoded `#F5C518` and `bg-gray-50`, uses token classes, and links to `/register` with a plain anchor.
</objective>

<execution_context>
@C:/Users/glcar/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/glcar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/glcar/prevailing-wage/.planning/PROJECT.md
@C:/Users/glcar/prevailing-wage/.planning/ROADMAP.md
@C:/Users/glcar/prevailing-wage/src/client/pages/DashboardPage.tsx
@C:/Users/glcar/prevailing-wage/src/client/pages/LoginPage.tsx
</context>

<interfaces>
<!-- Key component contracts the executor needs. No codebase exploration required. -->

From src/client/components/ui/EmptyState.tsx:
```typescript
interface EmptyStateProps {
  heading: string;
  message: string;
  action?: React.ReactNode;
  className?: string;
}
export function EmptyState({ heading, message, action, className }: EmptyStateProps)
```

From src/client/components/ui/Button.tsx:
```typescript
type ButtonVariant = 'primary' | 'secondary' | 'ghost';
type ButtonSize = 'sm' | 'md';
interface ButtonProps extends React.ButtonHTMLAttributes<HTMLButtonElement> {
  variant?: ButtonVariant;
  size?: ButtonSize;
}
export function Button({ variant = 'primary', size = 'md', ... }: ButtonProps)
// primary variant: bg-brand-gold text-nav-dark
```

Design tokens relevant to LoginPage:
- bg-surface-page  (replaces bg-gray-50 on the root div)
- border-brand-gold  (replaces border-[#F5C518])
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: DashboardPage — adopt EmptyState for zero-projects state</name>
  <files>src/client/pages/DashboardPage.tsx</files>
  <action>
    1. Add `import { EmptyState } from '../components/ui/EmptyState';` alongside existing imports.
    2. Replace lines 64-71 (the inline text div for empty state) with:
       ```tsx
       <EmptyState
         heading="No projects yet"
         message='Click "New Project" to create your first prevailing wage project.'
         action={
           <Button onClick={() => setShowForm(true)}>New Project</Button>
         }
       />
       ```
    3. The `Button` import already exists on line 9 — do not add a duplicate import.
    4. Keep all other markup unchanged. The grid and ProjectCard render path (lines 73-79) stays exactly as-is.
    5. Do NOT alter the modal, PageHeader, or error state sections.
  </action>
  <verify>
    <automated>cd C:/Users/glcar/prevailing-wage && npm run test -- --run 2>&1 | tail -5</automated>
  </verify>
  <done>181 tests pass. DashboardPage imports EmptyState and the empty-state branch uses `&lt;EmptyState heading="No projects yet" ...&gt;` — no raw `div className="text-center py-16 text-gray-500"` remains.</done>
</task>

<task type="auto">
  <name>Task 2: LoginPage — token cleanup and login-only simplification</name>
  <files>src/client/pages/LoginPage.tsx</files>
  <action>
    Rewrite LoginPage.tsx completely. The new file must:

    1. Remove the `useState` import (no more mode state).
    2. Remove the `RegisterForm` import (RegisterPage is now a separate route at /register).
    3. Add `import { Link } from 'react-router-dom';` for the register link.
    4. Replace `bg-gray-50` on the root div with `bg-surface-page`.
    5. Replace `border-[#F5C518]` on the h1 with `border-brand-gold`.
    6. Remove the `mode` conditional on the subtitle — replace with a static string: "Sign in to your account".
    7. Remove the conditional `{mode === 'login' ? <LoginForm /> : <RegisterForm />}` block — replace with just `<LoginForm />`.
    8. Replace the toggle button block with a single `<Link>`:
       ```tsx
       <div className="mt-4 text-center">
         <Link
           to="/register"
           className="text-sm text-gray-500 hover:text-gray-800 underline"
         >
           No account? Register instead
         </Link>
       </div>
       ```
    9. The outer wrapper, max-w-sm, card div, border, shadow, and padding all stay identical — only the token swap + mode removal changes.

    Final file should be ~30 lines (down from 46), with no `useState`, no `RegisterForm`, no `mode` variable.
  </action>
  <verify>
    <automated>cd C:/Users/glcar/prevailing-wage && npm run test -- --run 2>&1 | tail -5</automated>
  </verify>
  <done>181 tests pass. LoginPage.tsx has no `useState`, no `RegisterForm` import, no `mode` variable, no `border-[#F5C518]`, no `bg-gray-50`. Uses `border-brand-gold`, `bg-surface-page`, and a `&lt;Link to="/register"&gt;` for registration navigation.</done>
</task>

</tasks>

<verification>
- `grep -n "border-\[#F5C518\]" src/client/pages/LoginPage.tsx` returns no output
- `grep -n "bg-gray-50" src/client/pages/LoginPage.tsx` returns no output
- `grep -n "RegisterForm" src/client/pages/LoginPage.tsx` returns no output
- `grep -n "EmptyState" src/client/pages/DashboardPage.tsx` returns a match
- `grep -n "text-center py-16 text-gray-500" src/client/pages/DashboardPage.tsx` returns no output
- All 181 tests pass
</verification>

<success_criteria>
- DashboardPage zero-projects branch renders `<EmptyState heading="No projects yet" ...>` with action button
- LoginPage uses `bg-surface-page` and `border-brand-gold`
- LoginPage is login-only with a `<Link to="/register">` instead of embedded RegisterForm toggle
- 181/181 tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/14-page-by-page-polish/14-01-SUMMARY.md`
</output>
