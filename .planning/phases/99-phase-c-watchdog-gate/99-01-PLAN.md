---
phase: 99-phase-c-watchdog-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/99-phase-c-watchdog-gate/99-SCORE.md
autonomous: true
requirements:
  - MOB-16
  - MOB-17
  - MOB-18
  - MOB-19
  - MOB-20
  - MOB-21
  - MOB-22

must_haves:
  truths:
    - "Every Phase C acceptance criterion is verified by an automated command"
    - "A SCORE.md file exists with a pass/fail row per criterion and a numeric total"
    - "The numeric score is calculated deterministically from pass counts"
    - "GATE_PASS or GATE_FAIL is declared based on score >= 8.90"
  artifacts:
    - path: ".planning/phases/99-phase-c-watchdog-gate/99-SCORE.md"
      provides: "Evidence record with per-criterion verdicts and final score"
      contains: "GATE_PASS or GATE_FAIL"
  key_links:
    - from: "99-SCORE.md"
      to: "phases 94-98 artifacts"
      via: "grep/test/file-exists commands"
      pattern: "PASS|FAIL"
---

<objective>
Run the Phase C Watchdog Gate: execute every acceptance criterion check for phases 94–98,
compute a score (each criterion worth 1.0 point, 10 total), and write SCORE.md declaring
GATE_PASS (score >= 8.90) or GATE_FAIL with remediation pointers.

Purpose: Gate enforcement — Phase 100 must not begin until this score is on record.
Output: 99-SCORE.md with a final verdict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/94-offline-payroll-entry-queue/VALIDATION.md
@.planning/phases/95-background-sync/VALIDATION.md
@.planning/phases/96-photo-verification/VALIDATION.md
@.planning/phases/97-mobile-nav-redesign/VALIDATION.md
@.planning/phases/98-offline-compliance-checklists/VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run all automated gate checks and collect raw results</name>
  <files>.planning/phases/99-phase-c-watchdog-gate/99-SCORE.md</files>
  <action>
Run each check below in sequence from the repo root (`cd /c/Users/glcar/prevailing-wage`).
Record PASS or FAIL for each criterion. Do NOT interpret ambiguous output as a pass.

**Phase 94 — Offline Payroll Entry Queue (MOB-16, MOB-17)**

C1 — payrollQueue.ts exists and exports the required IDB operations:
```
grep -c "enqueuePayrollEntry\|getPayrollQueue\|clearPayrollEntry\|markSyncedElsewhere" src/client/lib/payrollQueue.ts
```
PASS if count >= 3. (At least 3 of the 4 exported functions present.)

C2 — useOfflineEntryMutation hook exists and routes to IDB queue when offline:
```
test -f src/client/components/payrollWizard/useOfflineEntryMutation.ts &&
grep -c "enqueuePayrollEntry\|navigator.onLine" src/client/components/payrollWizard/useOfflineEntryMutation.ts
```
PASS if file exists AND count >= 1. (Hook imports the IDB enqueue function and checks online status.)

**Phase 95 — Background Sync (MOB-18)**

C3 — Service worker has payroll-queue-replay Background Sync handler:
```
grep -c "payroll-queue-replay\|replayPayrollQueue" src/client/sw.ts
```
PASS if count >= 1.

C4 — SyncStatusIndicator component and useSyncStatus hook exist:
```
test -f src/client/hooks/useSyncStatus.ts &&
test -f src/client/components/ui/SyncStatusIndicator.tsx &&
grep -c "SyncStatusIndicator" src/client/components/shared/Layout.tsx
```
PASS if both files exist AND Layout imports/renders the indicator (count >= 1).

**Phase 96 — Photo Verification (MOB-19, MOB-20)**

C5 — Photo and signature migrations exist in the DB migration folder:
```
ls src/server/db/migrations/0035_photo_verification.sql 2>/dev/null || ls src/server/db/migrations/*photo_verification*.sql 2>/dev/null
```
PASS if any photo_verification migration file exists.

Also check schema has contractorSignatures and projectPhotos tables:
```
grep -c "contractorSignatures\|projectPhotos" src/server/db/schema.ts
```
PASS if count >= 1. Count C5 as PASS if both the migration file check and schema check pass.

C6 — SignaturePad and PhotoGallery components exported from their files:
```
test -f src/client/components/ui/SignaturePad.tsx &&
grep -c "export.*SignaturePad\|SignaturePad" src/client/components/ui/SignaturePad.tsx
```
PASS if file exists AND exports SignaturePad (count >= 1).

Also check PhotoGallery component (it may be in the same file or a separate one):
```
grep -rn "PhotoGallery\|export.*PhotoGallery" src/client/components/ 2>/dev/null | wc -l
```
Count C6 as PASS if SignaturePad exists AND PhotoGallery is present somewhere in components.

**Phase 97 — Mobile Nav Redesign (MOB-21)**

C7 — BottomTabBar component exported and imported in Layout:
```
test -f src/client/components/shared/BottomTabBar.tsx &&
grep -c "BottomTabBar" src/client/components/shared/Layout.tsx
```
PASS if BottomTabBar.tsx exists AND Layout references it (count >= 1).

C8 — Bottom tab bar is mobile-only (hidden on md+) in BottomTabBar or Layout:
```
grep -c "md:hidden\|md:flex\|768" src/client/components/shared/BottomTabBar.tsx
```
PASS if count >= 1. (Responsive hide class or breakpoint reference confirming mobile-only behavior.)

Also check swipe routing is implemented:
```
grep -c "touchstart\|swipe\|onTouchStart" src/client/components/shared/BottomTabBar.tsx src/client/components/shared/Layout.tsx 2>/dev/null
```
Count C8 as PASS if the mobile-only hide AND swipe handler checks both pass (count >= 1 for each).

**Phase 98 — Offline Compliance Checklists (MOB-22)**

C9 — checklistDb.ts exports the required IDB helpers:
```
grep -c "openChecklistDb\|saveChecklist\|getChecklists\|markSynced" src/client/lib/checklistDb.ts
```
PASS if count >= 3. (At least 3 of the 4 required exports present.)

C10 — OfflineChecklistPage exists, exported, and routed in App.tsx:
```
test -f src/client/pages/OfflineChecklistPage.tsx &&
grep -c "OfflineChecklistPage\|checklists" src/client/App.tsx
```
PASS if file exists AND App.tsx registers the route (count >= 1).

**Full test suite (gate integrity check)**

Run:
```
npx vitest run --exclude ".worktrees/**" 2>&1 | tail -5
```
Not a scored criterion. If the suite has failures (not just todos/skips), deduct 0.5 from
the final score. Note the test suite count — Phase 94's payrollQueue.test.ts should be
present and passing.

**TypeScript check (gate integrity check)**

Run:
```
npx tsc --noEmit 2>&1 | grep -v "workers.ts" | grep "error TS" | head -10
```
If there are TS errors beyond the pre-existing workers.ts implicit-any, deduct 0.5.
  </action>
  <verify>
    <automated>cd /c/Users/glcar/prevailing-wage && echo "Raw check pass — proceed to Task 2 to write SCORE.md"</automated>
  </verify>
  <done>All 10 criterion checks have been run and raw PASS/FAIL results are recorded in working memory, ready for SCORE.md authoring.</done>
</task>

<task type="auto">
  <name>Task 2: Write 99-SCORE.md with verdicts and final declaration</name>
  <files>.planning/phases/99-phase-c-watchdog-gate/99-SCORE.md</files>
  <action>
Using the raw results collected in Task 1, write the 99-SCORE.md file with this exact structure:

```markdown
---
phase: 99-phase-c-watchdog-gate
scored_at: {ISO timestamp}
score_target: 8.90
---

# Phase C Watchdog Gate — Score Report

## Criteria Results

| ID  | Phase | Requirement | Description | Result | Points |
|-----|-------|-------------|-------------|--------|--------|
| C1  | 94 | MOB-16 | payrollQueue.ts exports 3+ IDB operations | PASS/FAIL | 1.0/0.0 |
| C2  | 94 | MOB-17 | useOfflineEntryMutation routes to IDB queue when offline | PASS/FAIL | 1.0/0.0 |
| C3  | 95 | MOB-18 | sw.ts has payroll-queue-replay Background Sync handler | PASS/FAIL | 1.0/0.0 |
| C4  | 95 | MOB-18 | SyncStatusIndicator + useSyncStatus mounted in Layout | PASS/FAIL | 1.0/0.0 |
| C5  | 96 | MOB-19 | Photo verification migration exists + schema has projectPhotos/signatures | PASS/FAIL | 1.0/0.0 |
| C6  | 96 | MOB-20 | SignaturePad component exported + PhotoGallery present in components | PASS/FAIL | 1.0/0.0 |
| C7  | 97 | MOB-21 | BottomTabBar.tsx exported + imported in Layout | PASS/FAIL | 1.0/0.0 |
| C8  | 97 | MOB-21 | Bottom tab bar is mobile-only (md:hidden) + swipe handler present | PASS/FAIL | 1.0/0.0 |
| C9  | 98 | MOB-22 | checklistDb.ts exports 3+ IDB helpers | PASS/FAIL | 1.0/0.0 |
| C10 | 98 | MOB-22 | OfflineChecklistPage exists + route registered in App.tsx | PASS/FAIL | 1.0/0.0 |

## Integrity Checks (deductions)

| Check | Result | Deduction |
|-------|--------|-----------|
| Full test suite (all tests green, payrollQueue.test.ts included) | PASS/FAIL | 0.0 / -0.5 |
| TypeScript (no new errors beyond workers.ts implicit-any) | PASS/FAIL | 0.0 / -0.5 |

## Score Calculation

- Base score: {sum of passing criteria} / 10
- Deductions: {sum of integrity deductions}
- **Final score: {base - deductions} / 10**

## Verdict

<!-- Replace exactly one of the two lines below -->
**GATE_PASS** — Score {X.XX} >= 8.90. Phase 100 may begin.
**GATE_FAIL** — Score {X.XX} < 8.90. Remediation required before Phase 100.

## Failed Criteria (if any)

{For each FAIL row, one bullet with the file path and a specific fix description}

## Raw Command Evidence

| ID  | Command | Output Summary |
|-----|---------|----------------|
| C1  | grep payrollQueue exports | {actual output} |
| C2  | test useOfflineEntryMutation + grep enqueue | {actual output} |
| C3  | grep payroll-queue-replay sw.ts | {actual output} |
| C4  | test useSyncStatus + test SyncStatusIndicator + grep Layout | {actual output} |
| C5  | ls photo_verification migration + grep schema | {actual output} |
| C6  | test SignaturePad + grep PhotoGallery | {actual output} |
| C7  | test BottomTabBar + grep Layout | {actual output} |
| C8  | grep md:hidden + grep touchstart/swipe | {actual output} |
| C9  | grep checklistDb exports | {actual output} |
| C10 | test OfflineChecklistPage + grep App.tsx | {actual output} |

## Notes

- Scored: {date}
- Test suite result: {pass count} passed, {skip count} skipped, {fail count} failed
- TypeScript: {clean / known workers.ts implicit-any only / new errors found}
- payrollQueue.test.ts present in suite: {yes/no — required for MOB-16 TDD coverage}
- Manual-only criteria deferred: offline payroll submit with network throttle, Background Sync
  firing within 30s of connectivity restoration, signature capture PNG blob verified — cannot
  be verified without live browser + DevTools network throttling session
```

Scoring rules:
- 10 criteria × 1.0 point each = 10.0 maximum
- Integrity deductions applied after summing criteria
- Minimum score to declare GATE_PASS: 8.90
- Score is a single decimal-rounded number (e.g., 9.00, 8.50)
  </action>
  <verify>
    <automated>cd /c/Users/glcar/prevailing-wage && test -f .planning/phases/99-phase-c-watchdog-gate/99-SCORE.md && grep -q "GATE_PASS\|GATE_FAIL" .planning/phases/99-phase-c-watchdog-gate/99-SCORE.md && grep -q "Final score" .planning/phases/99-phase-c-watchdog-gate/99-SCORE.md && echo "99-SCORE.md OK"</automated>
  </verify>
  <done>99-SCORE.md is written, all 10 rows have PASS or FAIL, the final score is calculated, and exactly one of GATE_PASS or GATE_FAIL is declared.</done>
</task>

</tasks>

<verification>
Gate passes when:
1. 99-SCORE.md exists in `.planning/phases/99-phase-c-watchdog-gate/`
2. All 10 criteria rows have a verdict
3. Final score >= 8.90 → GATE_PASS declared
4. Full test suite is green (no new failures, payrollQueue.test.ts passing)
5. No new TypeScript errors beyond the pre-existing workers.ts implicit-any

Gate fails when:
- Final score < 8.90 → GATE_FAIL declared with remediation bullets pointing to the
  specific file and fix for each failed criterion
</verification>

<success_criteria>
- 99-SCORE.md exists with 10 scored rows
- Final numeric score computed and displayed
- GATE_PASS or GATE_FAIL declared unambiguously
- If GATE_FAIL: each failing criterion has a specific file path and fix description
- If GATE_PASS: Phase 100 entry in ROADMAP.md may be unlocked
- payrollQueue.test.ts presence in test suite documented (TDD coverage for MOB-16)
</success_criteria>

<output>
After completion, create `.planning/phases/99-phase-c-watchdog-gate/99-01-SUMMARY.md` with:
- Final gate verdict (GATE_PASS / GATE_FAIL)
- Final score
- Count of passing criteria out of 10
- Any failing criteria listed by ID
- Test suite result (X passing, Y failing)
- Whether Phase 100 is unblocked
- Note on payrollQueue.test.ts presence
</output>
