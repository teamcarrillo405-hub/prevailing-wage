---
phase: 100-roi-calculator-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/client/pages/RoiCalculatorPage.tsx
  - src/client/App.tsx
autonomous: true
requirements:
  - TRUST-04
must_haves:
  truths:
    - "Visiting /roi shows the calculator without logging in"
    - "Sliders for project count and worker count update savings estimate in real time"
    - "Hours-saved and dollar-value totals update on every slider change"
    - "URL params ?projects=N&workers=M pre-fill the sliders on load"
    - "Email input + CTA button are present below the results"
  artifacts:
    - path: "src/client/pages/RoiCalculatorPage.tsx"
      provides: "Full ROI calculator page — sliders, live calc, email CTA"
      exports: ["RoiCalculatorPage"]
    - path: "src/client/App.tsx"
      provides: "Route registration for /roi (public, no auth wrapper)"
      contains: "path=\"/roi\""
  key_links:
    - from: "src/client/pages/RoiCalculatorPage.tsx"
      to: "/api/roi-leads"
      via: "fetch POST on form submit"
      pattern: "fetch.*api/roi-leads"
    - from: "src/client/App.tsx"
      to: "RoiCalculatorPage"
      via: "React.lazy import + <Route path=\"/roi\">"
      pattern: "path=\"/roi\""
---

<objective>
Build the /roi public marketing page: a live ROI calculator with project/worker sliders, a real-time savings display, and an email capture form that POSTs to /api/roi-leads.

Purpose: Convert landing-page visitors into leads by showing concrete dollar savings before asking for an email. Implements TRUST-04 in the market credibility roadmap.

Output:
- src/client/pages/RoiCalculatorPage.tsx — complete React page
- src/client/App.tsx — /roi route added (public, before wildcard)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

Brand reference: src/client/pages/LandingPage.tsx — use the same navy gradient hero pattern
  (bg-cover gradient 135deg #1a2744 → #2d4a8a → #1a3a5c), brand-gold (#F5C518 via token),
  bg-nav-dark, font-headline (Oswald), font-body (Inter), Card primitive from ui/Card.

Routing reference: src/client/App.tsx — add /roi in the "Public pages — no auth required" block,
  following the same React.lazy() import pattern used by CaseStudyPage, PricingPage, etc.

<interfaces>
<!-- Key types and patterns the executor needs. Extracted from codebase. -->

From src/client/pages/LandingPage.tsx (brand patterns):
  - Hero: <section className="relative min-h-[70vh] flex items-center justify-center overflow-hidden">
  - Background: style={{ backgroundImage: "linear-gradient(135deg, #1a2744 0%, #2d4a8a 50%, #1a3a5c 100%)" }}
  - Dark overlay: <div className="absolute inset-0 bg-nav-dark/40" />
  - Floating nav: text-brand-gold font-headline text-xl font-bold
  - CTA button: bg-brand-gold text-nav-dark font-semibold px-8 py-4 rounded-lg

From src/client/App.tsx (routing pattern):
  const RoiCalculatorPage = React.lazy(() =>
    import('./pages/RoiCalculatorPage').then(m => ({ default: m.RoiCalculatorPage }))
  );
  // In "Public pages — no auth required" block:
  <Route path="/roi" element={<RoiCalculatorPage />} />

From src/client/components/ui/Card.tsx (import pattern):
  import { Card } from '../components/ui/Card';
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RoiCalculatorPage — sliders + live formula + email CTA</name>
  <files>src/client/pages/RoiCalculatorPage.tsx, src/client/pages/RoiCalculatorPage.test.tsx</files>
  <behavior>
    - calcRoi({ projects, workers }) returns { annualHours, annualSavings }
      where annualHours = workers × 2.5 × 52 and annualSavings = annualHours × 45
    - calcRoi({ projects: 1, workers: 10 }) → { annualHours: 1300, annualSavings: 58500 }
    - calcRoi({ projects: 5, workers: 50 }) → { annualHours: 6500, annualSavings: 292500 }
    - Projects slider range: 1–50 (default 5)
    - Workers slider range: 1–200 (default 20)
    - URL param ?projects=3&workers=15 pre-fills sliders to those values (clamped to ranges)
    - Email submit calls POST /api/roi-leads with { email, projectCount, workerCount, estimatedSavings }
    - Submit shows "Sending..." while in flight, "Report sent!" on success, inline error on failure
    - Submit button is disabled while email field is empty or in flight
  </behavior>
  <action>
    Write RoiCalculatorPage.test.tsx FIRST — unit tests for the pure calcRoi() function
    and interaction tests for slider → display updates and form submit behavior (mock fetch).

    Then implement RoiCalculatorPage.tsx:

    CONSTANTS (at file top, outside component):
      const HOURS_PER_WORKER_PER_WEEK = 2.5;
      const WEEKS_PER_YEAR = 52;
      const HOURLY_RATE = 45;
      function calcRoi(projects: number, workers: number) {
        const annualHours = workers * HOURS_PER_WORKER_PER_WEEK * WEEKS_PER_YEAR;
        const annualSavings = annualHours * HOURLY_RATE;
        return { annualHours, annualSavings };
      }
      // Export calcRoi for testing
      export { calcRoi };

    URL PARAM SEEDING:
      Use useSearchParams() from react-router-dom.
      Parse ?projects and ?workers on mount, clamp to slider ranges.
      const [projects, setProjects] = useState(() => {
        const p = parseInt(searchParams.get('projects') ?? '5', 10);
        return Math.min(50, Math.max(1, isNaN(p) ? 5 : p));
      });

    LAYOUT — match LandingPage hero visual language:
    1. Compact hero banner (min-h-[40vh]) with same navy gradient + dark overlay
       - Floating nav: "HCC Prevailing Wage" (brand-gold, font-headline) + Log In link
       - Headline: "Calculate Your ROI" (clamp, white, font-headline)
       - Subhead: "See exactly how much time and money you save on prevailing wage compliance"
    2. White content card (max-w-2xl mx-auto, -mt-16 relative z-10, rounded-2xl shadow-xl, p-8)
       - Section: "Your Project Profile" (font-headline text-nav-dark)
         - Projects slider (1–50): range input + live value badge
         - Workers slider (1–200): range input + live value badge
       - Divider
       - Section: "Your Estimated Annual Savings" (font-headline text-brand-gold)
         - Big number: annualHours formatted "X,XXX hours saved"  (text-5xl font-headline text-nav-dark)
         - Big number: annualSavings formatted "$X,XXX" (text-5xl font-headline text-brand-gold)
         - Fine print: "Based on 2.5 hrs/worker/week × 52 weeks × $45/hr compliance labor"
       - Divider
       - Section: "Get Your Full ROI Report"
         - Email input (type="email", placeholder="your@email.com")
         - Submit button (bg-brand-gold text-nav-dark, full-width): "Send My Free Report"
         - Status messaging: "Sending..." / "Report sent! Check your inbox." / inline error text

    STYLING RULES (per PROJECT.md constraints):
    - No hardcoded hex values — use bg-nav-dark, bg-brand-gold, text-brand-gold, text-nav-dark
    - All brand font classes: font-headline (Oswald), font-body or default (Inter)
    - Range input: accent-[theme(colors.brand.gold)] for the thumb color
    - Card: use the Card component from ui/Card if it accepts className; otherwise use a plain div
      with className="bg-white rounded-2xl shadow-xl p-8"

    EMAIL SUBMIT handler:
      async function handleSubmit(e: React.FormEvent) {
        e.preventDefault();
        setStatus('sending');
        try {
          const res = await fetch('/api/roi-leads', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({ email, projectCount: projects, workerCount: workers,
                                   estimatedSavings: calcRoi(projects, workers).annualSavings }),
          });
          if (!res.ok) throw new Error(await res.text());
          setStatus('sent');
        } catch {
          setStatus('error');
        }
      }

    Export: export function RoiCalculatorPage() { ... }
    (named export to match React.lazy() pattern in App.tsx)
  </action>
  <verify>
    <automated>cd /c/Users/glcar/prevailing-wage && npx vitest run src/client/pages/RoiCalculatorPage.test.tsx</automated>
  </verify>
  <done>
    All vitest tests pass. /roi renders without crash in dev. Sliders update both numbers in real time. URL params pre-fill sliders. Email form submits to /api/roi-leads. No hardcoded hex values in the file.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire /roi route into App.tsx</name>
  <files>src/client/App.tsx</files>
  <action>
    Add the lazy import and route for RoiCalculatorPage.

    1. After the GovernmentPage lazy import line (line ~52), add:
       const RoiCalculatorPage = React.lazy(() =>
         import('./pages/RoiCalculatorPage').then(m => ({ default: m.RoiCalculatorPage }))
       );

    2. In the "Public pages — no auth required" block (after the GovernmentPage route, before
       the accept-invite route), add:
       <Route path="/roi" element={<RoiCalculatorPage />} />

    Do NOT place inside <PublicRoute> (which redirects authenticated users away).
    Do NOT place inside <ProtectedRoute> (which requires auth).
    Follow the exact same pattern as /pricing, /government, /contact.
  </action>
  <verify>
    <automated>cd /c/Users/glcar/prevailing-wage && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    TypeScript reports 0 new errors. The /roi path string appears exactly once in App.tsx. The lazy import for RoiCalculatorPage is present.
  </done>
</task>

</tasks>

<verification>
After both tasks:
  cd /c/Users/glcar/prevailing-wage && npx vitest run src/client/pages/RoiCalculatorPage.test.tsx
  cd /c/Users/glcar/prevailing-wage && npx tsc --noEmit 2>&1 | grep -v "workers.ts" | head -20

Spot-check manually:
  cd /c/Users/glcar/prevailing-wage && npm run dev
  Open http://localhost:5173/roi — page loads without auth
  Open http://localhost:5173/roi?projects=10&workers=50 — sliders pre-filled to 10 and 50
</verification>

<success_criteria>
- calcRoi() unit tests all pass in vitest
- No TypeScript errors introduced by App.tsx change
- /roi is accessible without login
- URL params ?projects and ?workers pre-fill sliders
- Sliders update annualHours and annualSavings live
- Email form submits POST /api/roi-leads (server returns 404 until Plan 02 ships — that is expected)
- Zero hardcoded hex values in RoiCalculatorPage.tsx
</success_criteria>

<output>
After completion, create .planning/phases/100-roi-calculator-page/100-01-SUMMARY.md
</output>
