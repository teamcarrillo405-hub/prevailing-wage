---
phase: 93-phase-b-watchdog-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/93-phase-b-watchdog-gate/93-SCORE.md
autonomous: true
requirements:
  - COMP-06
  - COMP-07
  - COMP-08
  - INT-01
  - INT-02
  - STATE-14
  - STATE-15

must_haves:
  truths:
    - "Every Phase B acceptance criterion is verified by an automated command"
    - "A SCORE.md file exists with a pass/fail row per criterion and a numeric total"
    - "The numeric score is calculated deterministically from pass counts"
    - "GATE_PASS or GATE_FAIL is declared based on score >= 8.75"
  artifacts:
    - path: ".planning/phases/93-phase-b-watchdog-gate/93-SCORE.md"
      provides: "Evidence record with per-criterion verdicts and final score"
      contains: "GATE_PASS or GATE_FAIL"
  key_links:
    - from: "93-SCORE.md"
      to: "phases 88-92 artifacts"
      via: "grep/test/vitest commands"
      pattern: "PASS|FAIL"
---

<objective>
Run the Phase B Watchdog Gate: execute every acceptance criterion check for phases 88–92,
compute a score (each criterion worth 1.0 point, 10 total), and write SCORE.md declaring
GATE_PASS (score >= 8.75) or GATE_FAIL with remediation pointers.

Purpose: Gate enforcement — Phase 94 must not begin until this score is on record.
Output: 93-SCORE.md with a final verdict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/88-live-samgov-wd-fetch/88-VALIDATION.md
@.planning/phases/89-dol-2024-rule-updates/89-VALIDATION.md
@.planning/phases/90-procore-timesheet-sync/90-VALIDATION.md
@.planning/phases/91-minnesota-certified-payroll/91-VALIDATION.md
@.planning/phases/92-virginia-certified-payroll/92-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run all automated gate checks and collect raw results</name>
  <files>.planning/phases/93-phase-b-watchdog-gate/93-SCORE.md</files>
  <action>
Run each check below in sequence from the repo root (`cd /c/Users/glcar/prevailing-wage`).
Record PASS or FAIL for each criterion. Do NOT interpret ambiguous output as a pass.

**Phase 88 — Live SAM.gov WD Fetch (COMP-06, COMP-07)**

C1 — Weekly cron expression (Sunday 03:00 UTC) is in index.ts:
```
grep -c "0 3 \* \* 0" src/server/index.ts
```
PASS if count >= 1. (Confirms the cron was changed from monthly '0 2 1 * *' to weekly.)

C2 — wdRevisionLog schema export and revision diff insertion exist:
```
grep -c "wdRevisionLog" src/server/db/schema.ts &&
grep -c "wdRevisionLog" src/server/services/wdolSync.ts
```
PASS if both counts >= 1. (Schema export + diff log insert in sync service.)

Also check stale-WD banner on ProjectDetailPage (COMP-06 UI):
```
grep -c "StaleWdBanner\|lastFetchedAt\|days ago" src/client/pages/ProjectDetailPage.tsx
```
PASS if count >= 2. (Banner component defined and rendered with ROADMAP text.)

Count C1 as PASS only if the cron AND banner checks both pass.

**Phase 89 — DOL 2024 Rule Updates (COMP-08)**

C3 — WH347_FORM_REVISION constant set to 'Jan. 2025' in wh347Generator.ts:
```
grep -c "WH347_FORM_REVISION" src/server/services/wh347Generator.ts
```
PASS if count >= 1.

C4 — 30% deduction-ratio check in complianceService.ts + PayrollWeekDetailPage warning:
```
grep -c "deduction-ratio\|deductionRatio" src/server/services/complianceService.ts &&
grep -c "30%\|CIVIL_PENALTY_PER_VIOLATION" src/client/pages/ProjectDetailPage.tsx
```
PASS if both counts >= 1. (Compliance check added; civil penalty constant present in UI.)

**Phase 90 — Procore Timesheet Sync (INT-01, INT-02)**

C5 — procoreService.ts exports connect/token management functions:
```
grep -c "getProcoreConnection\|saveProcoreTokens\|deleteProcoreTokens\|getValidProcoreToken" src/server/services/procoreService.ts
```
PASS if count >= 3. (At least 3 of the 4 required function exports present.)

C6 — Procore routes registered in integrations.ts + IntegrationsPage has Procore tile:
```
grep -c "procore/connect\|procore/callback\|procore/status" src/server/routes/integrations.ts &&
grep -c "Procore\|procore" src/client/pages/IntegrationsPage.tsx
```
PASS if both counts >= 2.

**Phase 91 — Minnesota Certified Payroll (STATE-14)**

C7 — mnPdfGenerator.ts exports fillMnCertifiedPayroll + export route has mn-dli endpoint:
```
grep -c "fillMnCertifiedPayroll" src/server/services/mnPdfGenerator.ts &&
grep -c "mn-dli" src/server/routes/export.ts
```
PASS if both counts >= 1.

C8 — MN entry in STATE_FORMS registry on PayrollWeekDetailPage:
```
grep -c "MN\|mn-dli\|mnPdf\|Minnesota" src/client/pages/PayrollWeekDetailPage.tsx
```
PASS if count >= 1.

**Phase 92 — Virginia Certified Payroll (STATE-15)**

C9 — vaPdfGenerator.ts exports fillVaCertifiedPayroll + export route has va-doli endpoint:
```
grep -c "fillVaCertifiedPayroll" src/server/services/vaPdfGenerator.ts &&
grep -c "va-doli" src/server/routes/export.ts
```
PASS if both counts >= 1.

C10 — VA entry in STATE_FORMS registry on PayrollWeekDetailPage:
```
grep -c "VA\|va-doli\|vaPdf\|Virginia" src/client/pages/PayrollWeekDetailPage.tsx
```
PASS if count >= 1.

**Migration file integrity check (not a scored criterion)**

Phases 88, 90, and 91 each planned a migration 0055 with different names. Determine
which one was actually committed and flag any that are missing as remediation notes:
```
ls src/server/db/migrations/0055_* 2>/dev/null
ls src/server/db/migrations/0056_* 2>/dev/null
```
Record what exists. This is informational — do not deduct points, but note in SCORE.md Notes.

**Full test suite (gate integrity check)**

Run:
```
npx vitest run --exclude ".worktrees/**" 2>&1 | tail -5
```
Not a scored criterion. If the suite has failures (not just todos/skips), set suite_ok: false
and deduct 0.5 from the final score.

**TypeScript check (gate integrity check)**

Run:
```
npx tsc --noEmit 2>&1 | grep -v "workers.ts" | grep "error TS" | head -5
```
If there are TS errors beyond the pre-existing workers.ts implicit-any, deduct 0.5.
  </action>
  <verify>
    <automated>cd /c/Users/glcar/prevailing-wage && echo "Raw check pass — proceed to Task 2 to write SCORE.md"</automated>
  </verify>
  <done>All 10 criterion checks have been run and raw PASS/FAIL results are recorded in working memory, ready for SCORE.md authoring.</done>
</task>

<task type="auto">
  <name>Task 2: Write 93-SCORE.md with verdicts and final declaration</name>
  <files>.planning/phases/93-phase-b-watchdog-gate/93-SCORE.md</files>
  <action>
Using the raw results collected in Task 1, write the 93-SCORE.md file with this exact structure:

```markdown
---
phase: 93-phase-b-watchdog-gate
scored_at: {ISO timestamp}
score_target: 8.75
---

# Phase B Watchdog Gate — Score Report

## Criteria Results

| ID  | Phase | Requirement | Description | Result | Points |
|-----|-------|-------------|-------------|--------|--------|
| C1  | 88 | COMP-06 | Weekly cron (0 3 * * 0 UTC) + StaleWdBanner present | PASS/FAIL | 1.0/0.0 |
| C2  | 88 | COMP-07 | wdRevisionLog in schema + revision diff insert in wdolSync | PASS/FAIL | 1.0/0.0 |
| C3  | 89 | COMP-08 | WH347_FORM_REVISION constant in wh347Generator | PASS/FAIL | 1.0/0.0 |
| C4  | 89 | COMP-08 | deduction-ratio check in complianceService + CIVIL_PENALTY_PER_VIOLATION in ProjectDetailPage | PASS/FAIL | 1.0/0.0 |
| C5  | 90 | INT-01 | procoreService.ts exports 3+ OAuth token functions | PASS/FAIL | 1.0/0.0 |
| C6  | 90 | INT-02 | Procore routes in integrations.ts + Procore tile in IntegrationsPage | PASS/FAIL | 1.0/0.0 |
| C7  | 91 | STATE-14 | mnPdfGenerator exports fillMnCertifiedPayroll + mn-dli route in export.ts | PASS/FAIL | 1.0/0.0 |
| C8  | 91 | STATE-14 | MN entry in STATE_FORMS on PayrollWeekDetailPage | PASS/FAIL | 1.0/0.0 |
| C9  | 92 | STATE-15 | vaPdfGenerator exports fillVaCertifiedPayroll + va-doli route in export.ts | PASS/FAIL | 1.0/0.0 |
| C10 | 92 | STATE-15 | VA entry in STATE_FORMS on PayrollWeekDetailPage | PASS/FAIL | 1.0/0.0 |

## Integrity Checks (deductions)

| Check | Result | Deduction |
|-------|--------|-----------|
| Full test suite (all tests green) | PASS/FAIL | 0.0 / -0.5 |
| TypeScript (no new errors beyond workers.ts implicit-any) | PASS/FAIL | 0.0 / -0.5 |

## Migration File Inventory

| File | Status |
|------|--------|
| src/server/db/migrations/0055_*.sql | FOUND / MISSING |
| src/server/db/migrations/0056_*.sql | FOUND / MISSING |

Note: Phases 88, 90, and 91 each planned a migration at index 55 with different file names.
Only one can occupy index 55 in _journal.json. Record actual migration filenames here.
This does not affect score unless the migration critical to a scored criterion is absent.

## Score Calculation

- Base score: {sum of passing criteria} / 10
- Deductions: {sum of integrity deductions}
- **Final score: {base - deductions} / 10**

## Verdict

<!-- Replace exactly one of the two lines below -->
**GATE_PASS** — Score {X.XX} >= 8.75. Phase 94 may begin.
**GATE_FAIL** — Score {X.XX} < 8.75. Remediation required before Phase 94.

## Failed Criteria (if any)

{For each FAIL row, one bullet with the file path and a specific fix description}

## Raw Command Evidence

| ID  | Command | Output Summary |
|-----|---------|----------------|
| C1  | grep cron + grep StaleWdBanner | {actual output} |
| C2  | grep wdRevisionLog schema + sync | {actual output} |
| C3  | grep WH347_FORM_REVISION | {actual output} |
| C4  | grep deduction-ratio + CIVIL_PENALTY | {actual output} |
| C5  | grep procoreService exports | {actual output} |
| C6  | grep Procore routes + IntegrationsPage | {actual output} |
| C7  | grep fillMnCertifiedPayroll + mn-dli | {actual output} |
| C8  | grep MN STATE_FORMS | {actual output} |
| C9  | grep fillVaCertifiedPayroll + va-doli | {actual output} |
| C10 | grep VA STATE_FORMS | {actual output} |

## Notes

- Scored: {date}
- Test suite result: {pass count} passed, {skip count} skipped, {fail count} failed
- TypeScript: {clean / known workers.ts implicit-any only / new errors found}
- Migration index 55 collision: {which file name actually occupies idx 55 in _journal.json}
- Manual-only criteria deferred: Procore OAuth end-to-end with live credentials, MN/VA form
  visual inspection against official templates — cannot be verified without live sessions
```

Scoring rules:
- 10 criteria × 1.0 point each = 10.0 maximum
- Integrity deductions applied after summing criteria
- Minimum score to declare GATE_PASS: 8.75
- Score is a single decimal-rounded number (e.g., 9.00, 8.50)
  </action>
  <verify>
    <automated>cd /c/Users/glcar/prevailing-wage && test -f .planning/phases/93-phase-b-watchdog-gate/93-SCORE.md && grep -q "GATE_PASS\|GATE_FAIL" .planning/phases/93-phase-b-watchdog-gate/93-SCORE.md && grep -q "Final score" .planning/phases/93-phase-b-watchdog-gate/93-SCORE.md && echo "93-SCORE.md OK"</automated>
  </verify>
  <done>93-SCORE.md is written, all 10 rows have PASS or FAIL, the final score is calculated, and exactly one of GATE_PASS or GATE_FAIL is declared.</done>
</task>

</tasks>

<verification>
Gate passes when:
1. 93-SCORE.md exists in `.planning/phases/93-phase-b-watchdog-gate/`
2. All 10 criteria rows have a verdict
3. Final score >= 8.75 → GATE_PASS declared
4. Full test suite is green (no new failures)
5. No new TypeScript errors beyond the pre-existing workers.ts implicit-any

Gate fails when:
- Final score < 8.75 → GATE_FAIL declared with remediation bullets pointing to the
  specific file and fix for each failed criterion
</verification>

<success_criteria>
- 93-SCORE.md exists with 10 scored rows
- Final numeric score computed and displayed
- GATE_PASS or GATE_FAIL declared unambiguously
- If GATE_FAIL: each failing criterion has a specific file path and fix description
- If GATE_PASS: Phase 94 entry in ROADMAP.md may be unlocked
- Migration index 55 collision documented in Notes (phases 88, 90, 91 each planned an idx 55 migration)
</success_criteria>

<output>
After completion, create `.planning/phases/93-phase-b-watchdog-gate/93-01-SUMMARY.md` with:
- Final gate verdict (GATE_PASS / GATE_FAIL)
- Final score
- Count of passing criteria out of 10
- Any failing criteria listed by ID
- Test suite result (X passing, Y failing)
- Whether Phase 94 is unblocked
- Note on migration idx 55 collision resolution
</output>
