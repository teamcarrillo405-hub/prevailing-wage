---
phase: 84-dependabot-uptime-monitoring
plan: 02
type: execute
wave: 2
depends_on:
  - 84-01
files_modified:
  - src/client/pages/LandingPage.tsx
autonomous: false
requirements:
  - SEC-10

user_setup:
  - service: better-stack
    why: "Uptime monitoring and public status page for SOC 2 SEC-10 evidence. Free tier (10 monitors, 3-min checks, 1 status page). No explicit commercial restriction as of April 2026."
    env_vars: []
    dashboard_config:
      - task: "Create Better Stack account at https://betterstack.com/uptime"
        location: "betterstack.com → Sign Up (free)"
      - task: "Create a new monitor: URL = https://prevailingwage.app/api/health, check every 3 minutes, HTTP keyword 'ok' to confirm healthy response"
        location: "Better Stack Dashboard → Monitors → New Monitor"
      - task: "Create a public status page: any subdomain (e.g., status), associate the /api/health monitor"
        location: "Better Stack Dashboard → Status Pages → New Status Page"
      - task: "Copy the status page public URL (e.g., https://prevwage.betteruptime.com)"
        location: "Better Stack Dashboard → Status Pages → your page → Public URL"
      - task: "Update the STATUS_PAGE_URL constant in src/client/pages/LandingPage.tsx with the real subdomain URL"
        location: "src/client/pages/LandingPage.tsx — search for TODO: set after Better Stack setup"
      - task: "Create the 'dependencies' label in GitHub repo Settings → Labels (color: #0075ca) before first Dependabot run"
        location: "GitHub repo → Settings → Labels → New label"

must_haves:
  truths:
    - "LandingPage footer contains a 'System Status' link pointing to the Better Stack status page URL"
    - "LandingPage footer contains an iframe badge from Better Stack (betteruptime.com/badge)"
    - "The STATUS_PAGE_URL constant is defined with a TODO comment for the subdomain to be filled in after account setup"
    - "All existing LandingPage footer links still render correctly"
  artifacts:
    - path: "src/client/pages/LandingPage.tsx"
      provides: "Footer with System Status link + Better Stack iframe badge"
      contains: "STATUS_PAGE_URL"
  key_links:
    - from: "LandingFooter (src/client/pages/LandingPage.tsx)"
      to: "Better Stack status page"
      via: "anchor href={STATUS_PAGE_URL} and iframe src={STATUS_PAGE_URL + '/badge?theme=dark'}"
      pattern: "STATUS_PAGE_URL"
---

<objective>
Add the Better Stack "System Status" link and embeddable iframe badge to the LandingPage footer.

Purpose: SEC-10 compliance — public status page URL linked from the app footer, providing SOC 2 availability evidence and user-visible uptime transparency.

Output:
- `src/client/pages/LandingPage.tsx` updated: `LandingFooter` function gains a `STATUS_PAGE_URL` constant (with TODO placeholder), a "System Status" anchor in the nav links row, and a Better Stack iframe badge between the nav links and the copyright line.

Human setup required: Better Stack account must be created and `/api/health` monitor configured before the `STATUS_PAGE_URL` placeholder can be replaced with the real subdomain URL. See `user_setup` in frontmatter.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/84-dependabot-uptime-monitoring/84-RESEARCH.md
@.planning/phases/84-dependabot-uptime-monitoring/84-VALIDATION.md
@.planning/phases/84-dependabot-uptime-monitoring/84-01-SUMMARY.md
</context>

<interfaces>
<!-- Current LandingFooter structure — extracted from src/client/pages/LandingPage.tsx lines 1013-1051 -->
<!-- Executor must read this file before modifying to confirm no drift from this snapshot. -->

```tsx
// src/client/pages/LandingPage.tsx lines 1013-1051 (current state)
function LandingFooter() {
  const footerLinks = [
    { label: 'Pricing', to: '/pricing' },
    { label: 'Case Studies', to: '/case-studies' },
    { label: 'Contact', to: '/contact' },
    { label: 'Reviews', to: '/reviews' },
    { label: 'Security Policy', to: '/security' },
    { label: 'API Docs', to: '/api-docs' },
    { label: 'Log In', to: '/login' },
  ];

  return (
    <footer className="bg-nav-dark text-gray-400 py-10 px-6">
      <div className="max-w-5xl mx-auto">
        <div className="flex flex-col sm:flex-row justify-between items-start sm:items-center gap-6 mb-6">
          <span className="text-white font-headline text-sm">
            HCC Prevailing Wage
          </span>
          <div className="flex flex-wrap gap-x-6 gap-y-2 text-sm">
            {footerLinks.map(({ label, to }) => (
              <Link
                key={label}
                to={to}
                className="text-gray-400 hover:text-brand-gold transition-colors"
              >
                {label}
              </Link>
            ))}
          </div>
        </div>
        <p className="text-xs text-gray-600 text-center border-t border-white/5 pt-6">
          Davis-Bacon and Related Acts compliance software for federal construction contractors.
          &copy; 2026 PrevWage. All rights reserved.
        </p>
      </div>
    </footer>
  );
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add STATUS_PAGE_URL constant, System Status link, and Better Stack badge to LandingFooter</name>
  <files>src/client/pages/LandingPage.tsx</files>
  <read_first>
    - src/client/pages/LandingPage.tsx lines 1010-1055 (confirm current LandingFooter matches snapshot above before editing)
  </read_first>
  <action>
Edit `src/client/pages/LandingPage.tsx` to update the `LandingFooter` function. Make these three targeted changes:

**Change 1:** Add `STATUS_PAGE_URL` constant immediately before the `LandingFooter` function declaration (before `function LandingFooter()`):

```tsx
// TODO: set after Better Stack account setup — replace YOUR-SUBDOMAIN with the actual subdomain
// chosen during account creation at https://betterstack.com/uptime
const STATUS_PAGE_URL = 'https://YOUR-SUBDOMAIN.betteruptime.com';
```

**Change 2:** In the `footerLinks` map `<div>`, add a "System Status" `<a>` anchor immediately AFTER the closing `})}` of the `footerLinks.map(...)` block. It must be a sibling `<a>` (not an internal router `<Link>`) because it is an external URL:

```tsx
<a
  href={STATUS_PAGE_URL}
  target="_blank"
  rel="noopener noreferrer"
  className="text-gray-400 hover:text-brand-gold transition-colors"
>
  System Status
</a>
```

The full nav links `<div>` after the change:
```tsx
<div className="flex flex-wrap gap-x-6 gap-y-2 text-sm">
  {footerLinks.map(({ label, to }) => (
    <Link
      key={label}
      to={to}
      className="text-gray-400 hover:text-brand-gold transition-colors"
    >
      {label}
    </Link>
  ))}
  <a
    href={STATUS_PAGE_URL}
    target="_blank"
    rel="noopener noreferrer"
    className="text-gray-400 hover:text-brand-gold transition-colors"
  >
    System Status
  </a>
</div>
```

**Change 3:** Add the Better Stack iframe badge `<div>` between the end of the nav row `<div className="flex flex-col ...">` and the copyright `<p>`. Insert it so the footer's inner `<div>` reads:

```tsx
<div className="max-w-5xl mx-auto">
  {/* nav row */}
  <div className="flex flex-col sm:flex-row justify-between items-start sm:items-center gap-6 mb-6">
    ...existing content + System Status anchor...
  </div>
  {/* Better Stack embeddable status badge */}
  <div className="mb-4">
    <iframe
      src={`${STATUS_PAGE_URL}/badge?theme=dark`}
      width="250"
      height="30"
      frameBorder="0"
      scrolling="no"
      title="Service status"
      style={{ colorScheme: 'none' }}
    />
  </div>
  {/* copyright */}
  <p className="text-xs text-gray-600 text-center border-t border-white/5 pt-6">
    Davis-Bacon and Related Acts compliance software for federal construction contractors.
    &copy; 2026 PrevWage. All rights reserved.
  </p>
</div>
```

**React JSX notes (per RESEARCH.md):**
- Use `frameBorder` (camelCase) not `frameborder` — JSX requires camelCase HTML attributes
- Use `style={{ colorScheme: 'none' }}` — prevents Tailwind dark mode from breaking badge rendering
- The `src` uses a template literal: `${STATUS_PAGE_URL}/badge?theme=dark`

**Do NOT:**
- Change the `footerLinks` array entries
- Modify the footer's className attributes
- Add any animation or framer-motion
- Use hardcoded badge URL — the STATUS_PAGE_URL constant must be the single source of truth
  </action>
  <verify>
    <automated>grep -c "STATUS_PAGE_URL" /c/Users/glcar/prevailing-wage/src/client/pages/LandingPage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep "STATUS_PAGE_URL" src/client/pages/LandingPage.tsx` matches at least 3 times (const definition + href + iframe src)
    - `grep "YOUR-SUBDOMAIN.betteruptime.com" src/client/pages/LandingPage.tsx` matches once (the placeholder constant)
    - `grep "TODO: set after Better Stack" src/client/pages/LandingPage.tsx` matches once (TODO comment)
    - `grep "System Status" src/client/pages/LandingPage.tsx` matches once (link label)
    - `grep "noopener noreferrer" src/client/pages/LandingPage.tsx` matches at least once (security attr on external link)
    - `grep "frameBorder" src/client/pages/LandingPage.tsx` matches once (camelCase, not frameborder)
    - `grep "colorScheme" src/client/pages/LandingPage.tsx` matches once (dark mode fix)
    - `grep "badge?theme=dark" src/client/pages/LandingPage.tsx` matches once (badge iframe src)
    - `npx tsc --noEmit` exits 0 (no TypeScript errors introduced)
    - `npx vitest run --reporter=verbose 2>&1 | tail -5` shows all tests passing (no regressions)
  </acceptance_criteria>
  <done>
`LandingFooter` function in `src/client/pages/LandingPage.tsx` has:
1. `STATUS_PAGE_URL` constant with `YOUR-SUBDOMAIN` placeholder and TODO comment
2. "System Status" `<a>` link in the footer nav links row pointing to `STATUS_PAGE_URL`
3. Better Stack iframe badge `<div>` between the nav row and the copyright line, using `${STATUS_PAGE_URL}/badge?theme=dark` as src
All existing footerLinks unchanged. TypeScript compiles clean. Vitest suite passes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — Better Stack setup and badge visual confirm</name>
  <action>Human steps required: (1) Create Better Stack account and monitor at https://betterstack.com/uptime targeting https://prevailingwage.app/api/health at 3-minute intervals. (2) Create public status page and copy its subdomain URL. (3) Update STATUS_PAGE_URL constant in src/client/pages/LandingPage.tsx with real subdomain. (4) Create GitHub 'dependencies' label (color #0075ca) in repo Settings → Labels. (5) Verify footer badge renders in browser.</action>
  <verify>Visual browser check: navigate to http://localhost:4099, scroll to footer, confirm System Status link and Better Stack badge iframe are visible. Click System Status link — status page opens in new tab.</verify>
  <done>Better Stack account created, /api/health monitor active at 3-minute intervals, public status page accessible, STATUS_PAGE_URL constant updated with real subdomain, GitHub dependencies label created, footer badge renders correctly in browser.</done>
  <what-built>
Plan 84-02 Task 1 added the STATUS_PAGE_URL constant (placeholder), "System Status" link, and Better Stack iframe badge to LandingFooter. The placeholder URL must be replaced with the real Better Stack subdomain after account setup.
  </what-built>
  <how-to-verify>
**Step 1 — Create Better Stack account and monitor:**
1. Go to https://betterstack.com/uptime and sign up for the free tier
2. Create a new monitor:
   - URL: `https://prevailingwage.app/api/health`
   - Check interval: 3 minutes (or 5 — whichever free tier allows)
   - Keyword confirmation (optional): `"ok"` (confirms healthy JSON response)
3. Create a public status page and associate the `/api/health` monitor
4. Copy the public status page URL (format: `https://YOUR-SUBDOMAIN.betteruptime.com`)

**Step 2 — Update the placeholder in code:**
In `src/client/pages/LandingPage.tsx`, find the line:
```tsx
const STATUS_PAGE_URL = 'https://YOUR-SUBDOMAIN.betteruptime.com';
```
Replace `YOUR-SUBDOMAIN` with the actual subdomain from Step 1.

**Step 3 — Create the GitHub label:**
In GitHub repo → Settings → Labels → New label:
- Name: `dependencies`
- Color: `#0075ca`
(Required before first Dependabot run or PR label application will fail.)

**Step 4 — Visual verification in browser:**
1. Run `npm run dev` if not already running
2. Navigate to `http://localhost:4099` (or the dev port)
3. Scroll to the footer
4. Confirm "System Status" link is visible in the footer nav links row
5. Confirm the Better Stack status badge iframe renders below the nav links row
6. Click "System Status" — it should open the Better Stack status page in a new tab

**Expected:** Footer nav links unchanged + "System Status" link added + iframe badge visible.
  </how-to-verify>
  <resume-signal>Type "approved" once the badge renders and the status page URL is updated, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
After Task 1 (automated):

```bash
# TypeScript compile check
cd /c/Users/glcar/prevailing-wage && npx tsc --noEmit 2>&1 | head -20

# Grep checks
grep -c "STATUS_PAGE_URL" src/client/pages/LandingPage.tsx   # must be >= 3
grep "System Status" src/client/pages/LandingPage.tsx         # must match
grep "frameBorder" src/client/pages/LandingPage.tsx           # must match (camelCase)
grep "TODO: set after Better Stack" src/client/pages/LandingPage.tsx  # must match

# Existing suite
npx vitest run --reporter=verbose 2>&1 | tail -5
```

After human checkpoint (manual):
- Better Stack account created and `/api/health` monitor active
- Status page URL confirmed accessible publicly
- `STATUS_PAGE_URL` constant updated with real subdomain
- GitHub `dependencies` label created
- Footer badge renders in browser at dev URL
</verification>

<success_criteria>
1. `src/client/pages/LandingPage.tsx` contains `STATUS_PAGE_URL` constant with placeholder (or real URL after human setup) and TODO comment
2. Footer has "System Status" `<a>` link pointing to `STATUS_PAGE_URL` with `target="_blank" rel="noopener noreferrer"`
3. Footer has `<iframe src={...STATUS_PAGE_URL.../badge?theme=dark} frameBorder="0" style={{colorScheme:'none'}}>` badge
4. TypeScript compiles without errors
5. Vitest suite passes (zero regressions)
6. (Post-human-setup) Better Stack monitors `https://prevailingwage.app/api/health` at ≤5-minute intervals and the public status page is accessible
</success_criteria>

<output>
After completion, create `.planning/phases/84-dependabot-uptime-monitoring/84-02-SUMMARY.md`
</output>
