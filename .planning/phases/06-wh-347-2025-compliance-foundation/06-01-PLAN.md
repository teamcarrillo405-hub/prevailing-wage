---
phase: 06-wh-347-2025-compliance-foundation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/routes/workers.test.ts
  - tests/services/wh347.test.ts
autonomous: true
requirements: [WH347-01, WH347-02]

must_haves:
  truths:
    - "Running the test suite returns failing (red) tests for programName support on worker classifications"
    - "Running the test suite returns failing (red) tests for multi-page WH-347 generation"
    - "Running the test suite returns failing (red) tests for certApprentices boolean derivation"
    - "All existing passing tests remain green after stub additions"
  artifacts:
    - path: "tests/routes/workers.test.ts"
      provides: "Stub tests for programName field on classification create endpoint"
    - path: "tests/services/wh347.test.ts"
      provides: "Stub tests for multi-page PDF and certApprentices boolean — appended to existing file"
  key_links:
    - from: "tests/routes/workers.test.ts"
      to: "src/server/routes/workers.ts"
      via: "supertest POST /api/projects/:id/workers/:id/classifications"
      pattern: "programName"
    - from: "tests/services/wh347.test.ts"
      to: "src/server/services/wh347Generator.ts"
      via: "fillWh347() called with 9-worker fixture"
      pattern: "getPageCount.*4"
---

<objective>
Create failing test stubs before any implementation exists. These stubs define the behavioral contract that Plans 02 and 03 must satisfy.

Purpose: Nyquist compliance — every implementation task in this phase has an automated verify command pointing to a pre-existing test stub.
Output: Two test files with red stubs. Existing passing tests are not broken.
</objective>

<execution_context>
@C:/Users/glcar/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/glcar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-wh-347-2025-compliance-foundation/06-RESEARCH.md
@.planning/phases/06-wh-347-2025-compliance-foundation/06-VALIDATION.md

<interfaces>
<!-- Key interfaces from the files being tested. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From tests/services/wh347.test.ts (existing, APPEND only — do not replace):
- Uses: fillWh347(), WH347_FIELDS, Wh347Data from wh347Generator.js
- Pattern: FIXTURE constant at top, beforeAll() to load template + call fillWh347
- Import: `import { fillWh347, WH347_FIELDS, type Wh347Data } from '../../src/server/services/wh347Generator.js'`
- Template: `path.join(process.cwd(), 'assets', 'wh347-official-2025.pdf')`

From tests/routes/payroll.test.ts (use as pattern for new workers.test.ts):
- Pattern: supertest(app), registerAndLogin(), createProject(), cookie auth
- beforeAll sets: JWT_SECRET and NODE_ENV=test
- Imports: `import supertest from 'supertest'`, `import { app } from '../../src/server/index.js'`

From src/server/routes/workers.ts — current CreateClassificationSchema:
```typescript
const CreateClassificationSchema = z.object({
  tradeCode: z.string().min(1).max(50),
  tradeDescription: z.string().min(1).max(200),
  laborType: z.enum(['journeyworker', 'apprentice', 'foreman']),
  apprenticePercent: z.number().int().min(0).max(100).optional(),
})
// NOTE: programName field does NOT exist yet — stubs test for it
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create workers route test stub with programName cases</name>
  <files>tests/routes/workers.test.ts</files>
  <behavior>
    - Test stub A: POST /classifications with laborType='apprentice' and programName='ABC Joint Apprenticeship' → 201, response body contains programName field
    - Test stub B: GET /workers returns classifications that include programName field (null when not set)
    - These tests MUST fail (red) before Plan 02 is implemented — programName does not exist in schema yet
  </behavior>
  <action>
Create `tests/routes/workers.test.ts` as a NEW file. Follow the exact pattern from `tests/routes/payroll.test.ts`:

1. `beforeAll` sets `JWT_SECRET` and `NODE_ENV=test`
2. Helper functions: `registerAndLogin(suffix)`, `createProject(cookie)`, `createWorkerWithClassification(cookie, projectId)` (same pattern as payroll.test.ts — can copy the helpers)
3. New helper: `createApprenticeWithProgramName(cookie, projectId, programName)` — POSTs to `/api/projects/:projectId/workers` to create worker, then POSTs to `/api/projects/:projectId/workers/:workerId/classifications` with `{ tradeCode: 'ELEC', tradeDescription: 'Electrician', laborType: 'apprentice', apprenticePercent: 80, programName: 'IBEW Apprenticeship Program' }`
4. Test stub A (describe 'POST /classifications programName'): Call `createApprenticeWithProgramName`; `expect(res.status).toBe(201)`; `expect(res.body.data.classification.programName).toBe('IBEW Apprenticeship Program')` — this will FAIL until Plan 02 adds the column
5. Test stub B (describe 'GET /workers programName field'): Create an apprentice worker WITHOUT programName; `GET /api/projects/:projectId/workers`; `expect(workers[0].classifications[0]).toHaveProperty('programName')` — will FAIL until Plan 02 adds the column

Do NOT skip or `.todo` these tests — they must actually run and fail on missing field.
  </action>
  <verify>
    <automated>cd C:/Users/glcar/prevailing-wage && npx vitest run tests/routes/workers.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>Test file exists. Tests run (not skipped). Tests fail with a schema/field error (not a syntax error). All other tests unaffected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add multi-page and certApprentices stubs to existing wh347.test.ts</name>
  <files>tests/services/wh347.test.ts</files>
  <behavior>
    - Stub C: fillWh347() with 9-worker fixture produces a PDF with exactly 4 pages (2 page sets)
    - Stub D: fillWh347() with 9-worker fixture shows "Page 1 of 2" text content accessible (page count check proxy)
    - These tests MUST fail until Plan 03 implements multi-page chunking
  </behavior>
  <action>
APPEND to the existing `tests/services/wh347.test.ts` file — do NOT replace or rewrite it. Add a new `describe('multi-page WH-347', ...)` block after the existing `describe('wh347Generator', ...)` block.

The new describe block needs:
1. A 9-worker fixture constant `FIXTURE_9_WORKERS: Wh347Data` — same structure as FIXTURE but with `workers` array containing 9 entries (copy worker 1 nine times with `entryNo` 1–9, different `workerName` values for each)
2. A `let filledBytes9: Uint8Array` at describe scope
3. `beforeAll(async () => { filledBytes9 = await fillWh347(FIXTURE_9_WORKERS, templateBytes); })` — NOTE: `templateBytes` is defined at the outer file scope in the existing `beforeAll`; the inner `beforeAll` can reference it since JS closures allow this, BUT vitest runs describe-level `beforeAll` after outer `beforeAll`, so this is safe
4. Stub test: `it('produces 4 pages for 9 workers (2 page sets)', async () => { const doc = await PDFDocument.load(filledBytes9); expect(doc.getPageCount()).toBe(4); })` — FAILS until Plan 03 removes the 8-worker hard cap

Also APPEND a new `describe('certApprentices boolean', ...)` block that tests the generator contract separately — note this is about the compliance data structure, not export.ts logic:
5. Stub test: `it('accepts certApprentices: false in compliance object', async () => { const data = { ...FIXTURE, compliance: { ...FIXTURE.compliance, certApprentices: false } }; const bytes = await fillWh347(data, templateBytes); const doc = await PDFDocument.load(bytes); expect(doc.getPageCount()).toBe(2); })` — this test PASSES now (it's a contract test, not a failing stub). This documents that the generator accepts false without error.

After appending, run the suite — only the 4-page test should be red. The certApprentices contract test should be green.
  </action>
  <verify>
    <automated>cd C:/Users/glcar/prevailing-wage && npx vitest run tests/services/wh347.test.ts 2>&1 | tail -25</automated>
  </verify>
  <done>Multi-page stub test runs and fails (page count is 2, not 4). certApprentices contract test passes. All prior tests in the file remain green.</done>
</task>

</tasks>

<verification>
After both tasks:

```bash
cd C:/Users/glcar/prevailing-wage && npx vitest run 2>&1 | tail -30
```

Expected: All previously passing tests still pass. New stubs in workers.test.ts fail. Multi-page stub in wh347.test.ts fails. No unexpected failures.
</verification>

<success_criteria>
- tests/routes/workers.test.ts exists with programName stubs that fail
- tests/services/wh347.test.ts has 4-page stub appended that fails
- Full vitest suite: previously passing tests still green, only new stubs are red
- No syntax errors in either file (vitest can parse and run both)
</success_criteria>

<output>
After completion, create `.planning/phases/06-wh-347-2025-compliance-foundation/06-01-SUMMARY.md`
</output>
