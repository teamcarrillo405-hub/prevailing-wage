---
phase: 101-customer-testimonials-video
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/client/pages/TestimonialsPage.tsx
  - src/client/App.tsx
autonomous: true
requirements: [TRUST-05, TRUST-06]

must_haves:
  truths:
    - "User can navigate to /testimonials and read 3 contractor quotes"
    - "Each quote shows a name, company, project context, and avatar initials"
    - "A YouTube video embed is visible on the page"
    - "A 'Download Case Study PDF' link is visible and downloadable"
  artifacts:
    - path: "src/client/pages/TestimonialsPage.tsx"
      provides: "Testimonials + video + PDF download page"
      min_lines: 120
  key_links:
    - from: "src/client/App.tsx"
      to: "/testimonials"
      via: "React Router Route + lazy import"
      pattern: "path=\"/testimonials\""
---

<objective>
Create a public /testimonials page that displays 3 contractor social-proof quotes, a YouTube video embed, and a PDF case study download link.

Purpose: TRUST-05 (customer testimonials) and TRUST-06 (video + case study) require dedicated credibility content for sales and SEO.
Output: TestimonialsPage.tsx + route registration in App.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<!-- Style reference: LandingPage.tsx uses bg-nav-dark, brand-gold, font-headline, Card, CheckCircle, dark testimonial carousel cards. Match that exact token set. TestimonialCarousel already exists in LandingPage as a subcomponent — do NOT duplicate; create a standalone full page version. -->

@src/client/pages/LandingPage.tsx
@src/client/App.tsx
</context>

<interfaces>
<!-- App.tsx lazy-import pattern (match exactly): -->
```typescript
const TestimonialsPage = React.lazy(() => import('./pages/TestimonialsPage').then(m => ({ default: m.TestimonialsPage })));
```

<!-- Public route placement — add after /pricing and before /government: -->
```tsx
<Route path="/testimonials" element={<TestimonialsPage />} />
```

<!-- Testimonial shape (reuse from LandingPage.tsx line 76-109): -->
```typescript
interface Testimonial {
  quote: string;
  name: string;
  company: string;
  project: string;
  initials: string;
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Build TestimonialsPage with quotes, video embed, PDF download</name>
  <files>src/client/pages/TestimonialsPage.tsx</files>
  <action>
Create `src/client/pages/TestimonialsPage.tsx` exporting `TestimonialsPage`.

Structure:
1. **Nav** — same minimal nav pattern as PricingPage.tsx (PricingNav): dark bg-nav-dark bar, "HCC Prevailing Wage" gold headline link to "/", Log In link to "/login".

2. **Hero** — `<section className="bg-nav-dark text-white py-16 px-6 text-center">`, h1 "What Contractors Are Saying", subtitle "Real results from verified users managing Davis-Bacon compliance."

3. **Testimonials grid** — 3 cards using TESTIMONIALS array (copy exact data from LandingPage.tsx TESTIMONIALS const). Each card: `<div className="bg-white rounded-2xl p-8 shadow-md border border-gray-100">`. Inside: large left-quote decoration in brand-gold (opacity-20), `<blockquote>` with italic quote text in gray-700, footer with gold initials avatar circle (w-10 h-10 bg-brand-gold text-nav-dark font-bold rounded-full), name/company/project text. Layout: `grid grid-cols-1 md:grid-cols-3 gap-6 max-w-5xl mx-auto`.

4. **Video section** — `<section className="bg-nav-dark py-20 px-6">`. Embed YouTube iframe for a real HCC demo video. Since no confirmed video ID exists yet, use placeholder ID `dQw4w9WgXcQ` with a clear `// TODO: replace with real video ID` comment. Iframe: `width="560" height="315" src="https://www.youtube.com/embed/VIDEOID" title="HCC Prevailing Wage Demo" frameBorder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowFullScreen`. Wrap in `<div className="max-w-3xl mx-auto aspect-video rounded-2xl overflow-hidden">`. Section heading: "See It in Action" white font-headline.

5. **PDF case study download** — `<section className="bg-gray-50 py-16 px-6 text-center">`. Heading "Download the Full Case Study". Subtext "HCC I-405 Corridor project: 4 hours saved per week, clean DOL audit". Download link: `<a href="/case-studies/hcc" className="...">Download PDF Case Study</a>` styled as a secondary button (border border-nav-dark text-nav-dark px-8 py-3 rounded-lg font-semibold hover:bg-gray-100 transition-colors). NOTE: the link goes to the existing /case-studies/hcc route (CaseStudyPage already exists — no new PDF file needed).

6. **Footer** — identical to PricingFooter (copy the links array pattern from PricingPage.tsx lines 580-613).

All design tokens: bg-nav-dark, text-brand-gold, bg-brand-gold, font-headline, font-body. No hardcoded hex values.
  </action>
  <verify>
    <automated>cd /c/Users/glcar/prevailing-wage && npx tsc --noEmit --project tsconfig.json 2>&1 | grep -i "TestimonialsPage" || echo "No TestimonialsPage TS errors"</automated>
  </verify>
  <done>TestimonialsPage.tsx exists, exports TestimonialsPage, compiles cleanly, contains 3 testimonial cards, a YouTube iframe, and a PDF/case study download link.</done>
</task>

<task type="auto">
  <name>Task 2: Register /testimonials route in App.tsx</name>
  <files>src/client/App.tsx</files>
  <action>
Add lazy import and route for TestimonialsPage in App.tsx.

1. Add lazy import after the RoiCalculatorPage import (line ~55):
```typescript
const TestimonialsPage = React.lazy(() => import('./pages/TestimonialsPage').then(m => ({ default: m.TestimonialsPage })));
```

2. Add public route after `/roi` route (line ~136):
```tsx
<Route path="/testimonials" element={<TestimonialsPage />} />
```

Do NOT add to PublicRoute wrapper (it redirects authenticated users — testimonials should be accessible to everyone including authenticated users browsing the marketing site). Place alongside /api-docs, /case-studies, /pricing, /roi in the "Public pages — no auth required" block.

Also add "Testimonials" to the footer links array in LandingPage.tsx (line ~1021, footerLinks array) so the marketing footer links to /testimonials.
  </action>
  <verify>
    <automated>cd /c/Users/glcar/prevailing-wage && grep -n "testimonials" src/client/App.tsx && grep -n "testimonials" src/client/pages/LandingPage.tsx</automated>
  </verify>
  <done>App.tsx has lazy import + Route for /testimonials. LandingPage.tsx footer includes Testimonials link. npx tsc --noEmit passes with 0 errors on new files.</done>
</task>

</tasks>

<verification>
```bash
cd /c/Users/glcar/prevailing-wage && npx tsc --noEmit 2>&1 | tail -5
```
TypeScript must report 0 new errors. The pre-existing errors on workers.ts lines 108/115 are acceptable.

```bash
cd /c/Users/glcar/prevailing-wage && grep -c "TestimonialsPage" src/client/App.tsx
```
Must return 2 (lazy import + Route).
</verification>

<success_criteria>
- /testimonials route exists and resolves to TestimonialsPage
- Page shows 3 testimonial cards with quotes, names, companies, initials avatars
- YouTube iframe is present (placeholder ID with TODO comment)
- Case study download link points to /case-studies/hcc
- Footer links to /testimonials from LandingPage
- No new TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/101-customer-testimonials-video/101-01-SUMMARY.md` with:
- Files modified
- Key decisions (video placeholder ID chosen, linked to existing CaseStudyPage)
- Verification results
</output>
