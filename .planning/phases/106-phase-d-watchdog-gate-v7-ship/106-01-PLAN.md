---
phase: 106-phase-d-watchdog-gate-v7-ship
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/106-phase-d-watchdog-gate-v7-ship/106-SCORE.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - TRUST-05
  - TRUST-06
  - ENT-01
  - ENT-02
  - AI-01
  - AI-02
  - REPT-06
  - OPS-01

must_haves:
  truths:
    - "Every Phase D acceptance criterion is verified by an automated command"
    - "A SCORE.md file exists with a pass/fail row per criterion and a numeric total"
    - "The numeric score is calculated deterministically from pass counts and deductions"
    - "GATE_PASS or GATE_FAIL is declared based on score >= 9.2"
    - "If GATE_PASS: git tag v7.0.0 is created and ROADMAP.md marks all Phase D phases complete"
    - "If GATE_FAIL: each failing criterion has a specific file path and fix description"
    - "LCPtracker feature gap audit is documented in SCORE.md"
  artifacts:
    - path: ".planning/phases/106-phase-d-watchdog-gate-v7-ship/106-SCORE.md"
      provides: "Evidence record with per-criterion verdicts, LCPtracker gap audit, and final score"
      contains: "GATE_PASS or GATE_FAIL"
  key_links:
    - from: "106-SCORE.md"
      to: "phases 101-105 artifacts"
      via: "grep/test/vitest commands"
      pattern: "PASS|FAIL"
    - from: "GATE_PASS verdict"
      to: "git tag v7.0.0"
      via: "git tag -a command"
      pattern: "v7.0.0"
---

<objective>
Run the Phase D Watchdog Gate: execute every acceptance criterion check for phases 101–105,
compute a score (each criterion worth 1.0 point, 10 total), run integrity checks (test suite +
TypeScript), conduct the LCPtracker feature gap audit, and write SCORE.md declaring
GATE_PASS (score >= 9.2) or GATE_FAIL.

If GATE_PASS: create git tag v7.0.0 and mark all Phase D phases complete in ROADMAP.md.

Purpose: Final gate enforcement — v7.0 must not ship until the score is on record.
Output: 106-SCORE.md with final verdict, git tag v7.0.0 (on GATE_PASS), updated ROADMAP.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/106-phase-d-watchdog-gate-v7-ship/106-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run all 10 gate checks, LCPtracker audit, and integrity checks</name>
  <files>.planning/phases/106-phase-d-watchdog-gate-v7-ship/106-SCORE.md</files>
  <action>
Run each check below in sequence from the repo root (`cd /c/Users/glcar/prevailing-wage`).
Record PASS or FAIL for each criterion. Do NOT interpret ambiguous output as a pass.

**Phase 101 — Customer Testimonials + Video (TRUST-05, TRUST-06)**

C1 — Testimonials page exists as a routed React component:
```
find src/client/pages -name "*estimonial*" -o -name "*Testimonial*" 2>/dev/null | head -5
grep -r "testimonial\|Testimonial" src/client/App.tsx src/client/main.tsx 2>/dev/null | head -5
```
PASS if a TestimonialsPage (or equivalent) file exists AND is registered in the router.

C2 — Video embed present in the testimonials page or landing page:
```
grep -r "youtube\|vimeo\|<video\|iframe.*src\|VideoEmbed\|video-embed" src/client/pages/ 2>/dev/null | grep -v ".test." | head -10
```
PASS if count >= 1 (video iframe or video component present in a client page).

**Phase 102 — Enterprise Pricing + SSO Foundation (ENT-01, ENT-02)**

C3 — Enterprise tier present on PricingPage:
```
grep -r "Enterprise\|enterprise" src/client/pages/PricingPage.tsx 2>/dev/null | head -5
```
PASS if count >= 1.

C4 — sso_configs table in DB schema:
```
grep -c "sso_configs\|ssoConfigs" src/server/db/schema.ts
```
PASS if count >= 1.

**Phase 103 — AI Classification Assist (AI-01, AI-02)**

C5 — /api/ai/classify route registered:
```
grep -r "ai/classify\|ai-classify" src/server/routes/ 2>/dev/null | head -5
```
PASS if at least one route file registers the classify endpoint.

C6 — Claude API integration present (Anthropic SDK or raw fetch to claude API):
```
grep -r "anthropic\|claude\|@anthropic-ai" src/server/ 2>/dev/null | grep -v ".test." | grep -v "node_modules" | head -10
```
PASS if count >= 1 (SDK import or API call present in server code).

**Phase 104 — Advanced Audit Analytics (REPT-06)**

C7 — Pivot table component exists:
```
find src/client -name "*ivot*" -o -name "*Pivot*" 2>/dev/null | head -5
grep -r "PivotTable\|pivot-table\|pivotTable" src/client/ 2>/dev/null | grep -v ".test." | head -5
```
PASS if a pivot table component file exists OR is referenced in a page.

C8 — CSV export route for audit analytics:
```
grep -r "audit.*csv\|csv.*audit\|analytics.*export\|export.*analytics" src/server/routes/ 2>/dev/null | head -5
grep -r "audit-analytics\|pivot.*export\|export.*pivot" src/server/routes/ 2>/dev/null | head -5
```
PASS if count >= 1 (at least one CSV export endpoint for audit/analytics data).

**Phase 105 — Growth Dashboard Admin (OPS-01)**

C9 — /admin/growth page exists:
```
find src/client/pages -name "*rowth*" -o -name "*Growth*" -o -name "*dmin*" -o -name "*Admin*" 2>/dev/null | head -10
grep -r "admin/growth\|AdminGrowth\|GrowthDashboard" src/client/ 2>/dev/null | grep -v ".test." | head -5
```
PASS if an admin growth page file exists OR is routed at /admin/growth.

C10 — Admin auth guard protecting the growth dashboard:
```
grep -r "isAdmin\|adminOnly\|AdminRoute\|admin.*guard\|requireAdmin\|role.*admin" src/server/routes/ src/client/ 2>/dev/null | grep -v ".test." | head -10
```
PASS if count >= 1 (admin-only guard/middleware applied to protect the growth route).

**Full test suite (gate integrity check)**

Run:
```
npx vitest run --exclude ".worktrees/**" 2>&1 | tail -5
```
Record the test counts (passed / failed / todo). If failures > 0, deduct 0.5 from final score.
Not a scored criterion.

**TypeScript check (gate integrity check)**

Run:
```
npx tsc --noEmit 2>&1 | grep -v "workers.ts" | grep "error TS" | head -10
```
If there are TS errors beyond the pre-existing workers.ts implicit-any, deduct 0.5.
Note: the stripeService.ts Stripe version string error from Phase B gate is a KNOWN pre-existing
error — do NOT deduct if it is the only error found outside workers.ts.

**LCPtracker Feature Gap Audit**

LCPtracker (https://lcptracker.com) is the leading certified payroll compliance platform for
public works contractors. Conduct a gap analysis by reviewing LCPtracker's published feature
list and marketing copy against this codebase's capabilities.

Key LCPtracker claimed features to check against our implementation:
1. Electronic certified payroll submission to agencies — check: do we have CA eCPR XML + WA PWIA XML?
   `ls src/server/services/*Xml* src/server/services/*ecpr* src/server/services/*pwia* 2>/dev/null`
2. Automated prevailing wage rate lookup — check: SAM.gov WD fetch + weekly cron?
   `grep -c "wdolSync\|fetchWageData\|samgov" src/server/services/wdolSync.ts`
3. Multi-state certified payroll forms — check: how many states do we support?
   `ls src/server/services/*PdfGenerator.ts 2>/dev/null | wc -l`
4. Payroll import from accounting software — check: QuickBooks, ADP, Gusto, Paychex, Sage?
   `grep -r "quickbooks\|adp\|gusto\|paychex\|sage" src/server/routes/ 2>/dev/null | head -5`
5. DBE/MBE/WBE subcontractor tracking — check: Phase 71-72?
   `find src/server -name "*dbe*" -o -name "*Dbe*" 2>/dev/null | head -5`
6. Mobile field access — check: PWA/service worker + GPS clock-in?
   `find src/client/public -name "*.json" 2>/dev/null | head -3`
7. Davis-Bacon apprenticeship ratio enforcement — check: Phase 70?
   `grep -c "apprentice.*ratio\|ratio.*apprentice\|COMP-03" src/server/services/complianceService.ts`
8. Real-time compliance dashboards — check: Phase 73?
   `find src/client/pages -name "*Compliance*" -o -name "*Dashboard*" 2>/dev/null | head -5`

Document each gap as: AHEAD / PARITY / BEHIND with one-line explanation.
Record audit results for inclusion in SCORE.md.
  </action>
  <verify>
    <automated>cd /c/Users/glcar/prevailing-wage && echo "Raw checks complete — proceed to Task 2 to write SCORE.md"</automated>
  </verify>
  <done>All 10 criterion checks have been run, LCPtracker gap audit is complete, and raw PASS/FAIL results plus audit findings are recorded in working memory, ready for SCORE.md authoring.</done>
</task>

<task type="auto">
  <name>Task 2: Write 106-SCORE.md with verdicts and final declaration</name>
  <files>.planning/phases/106-phase-d-watchdog-gate-v7-ship/106-SCORE.md</files>
  <action>
Using the raw results collected in Task 1, write the 106-SCORE.md file with this exact structure:

```markdown
---
phase: 106-phase-d-watchdog-gate-v7-ship
scored_at: {ISO timestamp}
score_target: 9.2
---

# Phase D Watchdog Gate — Score Report

## Criteria Results

| ID  | Phase | Requirement | Description | Result | Points |
|-----|-------|-------------|-------------|--------|--------|
| C1  | 101 | TRUST-05 | TestimonialsPage exists and is registered in router | PASS/FAIL | 1.0/0.0 |
| C2  | 101 | TRUST-06 | Video embed present in testimonials or landing page | PASS/FAIL | 1.0/0.0 |
| C3  | 102 | ENT-01 | Enterprise tier on PricingPage | PASS/FAIL | 1.0/0.0 |
| C4  | 102 | ENT-02 | sso_configs table in DB schema | PASS/FAIL | 1.0/0.0 |
| C5  | 103 | AI-01 | /api/ai/classify route registered | PASS/FAIL | 1.0/0.0 |
| C6  | 103 | AI-02 | Claude API integration present in server code | PASS/FAIL | 1.0/0.0 |
| C7  | 104 | REPT-06 | Pivot table component exists | PASS/FAIL | 1.0/0.0 |
| C8  | 104 | REPT-06 | CSV export route for audit analytics | PASS/FAIL | 1.0/0.0 |
| C9  | 105 | OPS-01 | /admin/growth page exists | PASS/FAIL | 1.0/0.0 |
| C10 | 105 | OPS-01 | Admin auth guard protecting growth dashboard | PASS/FAIL | 1.0/0.0 |

## Integrity Checks (deductions)

| Check | Result | Deduction |
|-------|--------|-----------|
| Full test suite (all tests green) | PASS/FAIL — {X} passed, {Y} failed | 0.0 / -0.5 |
| TypeScript (no new errors beyond workers.ts implicit-any + known stripeService.ts) | PASS/FAIL | 0.0 / -0.5 |

## Score Calculation

- Base score: {sum of passing criteria} / 10
- Deductions: {sum of integrity deductions}
- **Final score: {base - deductions} / 10**

## Verdict

<!-- Replace exactly one of the two lines below -->
**GATE_PASS** — Score {X.XX} >= 9.2. v7.0.0 tag created. Phase D complete.
**GATE_FAIL** — Score {X.XX} < 9.2. Remediation required before v7.0 ship.

## Failed Criteria (if any)

{For each FAIL row, one bullet with the file path and a specific fix description}

## LCPtracker Feature Gap Audit

Scored: {date}. LCPtracker is the leading competing platform for public works certified payroll compliance.

| # | LCPtracker Feature | Our Status | Evidence |
|---|-------------------|-----------|----------|
| 1 | Electronic certified payroll submission | {AHEAD/PARITY/BEHIND} | {file or note} |
| 2 | Automated prevailing wage rate lookup | {AHEAD/PARITY/BEHIND} | {file or note} |
| 3 | Multi-state certified payroll forms | {AHEAD/PARITY/BEHIND} | {count} state PDFs |
| 4 | Payroll import from accounting software | {AHEAD/PARITY/BEHIND} | {providers listed} |
| 5 | DBE/MBE/WBE subcontractor tracking | {AHEAD/PARITY/BEHIND} | {file or note} |
| 6 | Mobile field access (PWA) | {AHEAD/PARITY/BEHIND} | {file or note} |
| 7 | Davis-Bacon apprenticeship ratio enforcement | {AHEAD/PARITY/BEHIND} | {file or note} |
| 8 | Real-time compliance dashboards | {AHEAD/PARITY/BEHIND} | {file or note} |

**Summary:** {N} AHEAD, {M} PARITY, {K} BEHIND

{If any BEHIND items: one bullet per item with recommended remediation}

## Raw Command Evidence

| ID  | Command | Output Summary |
|-----|---------|----------------|
| C1  | find TestimonialsPage + grep router | {actual output} |
| C2  | grep video embed | {actual output} |
| C3  | grep Enterprise PricingPage | {actual output} |
| C4  | grep sso_configs schema | {actual output} |
| C5  | grep ai/classify route | {actual output} |
| C6  | grep anthropic server | {actual output} |
| C7  | find PivotTable component | {actual output} |
| C8  | grep audit CSV export route | {actual output} |
| C9  | find GrowthDashboard page | {actual output} |
| C10 | grep admin auth guard | {actual output} |

## Notes

- Scored: {date}
- Test suite result: {X} passed, {Y} todo, {Z} failed
- TypeScript: {clean / known errors only / new errors found}
- Known pre-existing TS errors (not penalized): workers.ts implicit-any; stripeService.ts Stripe version string
- Manual-only criteria deferred: SSO Okta connect flow end-to-end (requires live Okta dev account),
  AI classification latency < 3s (requires live ANTHROPIC_API_KEY + network call),
  visual inspection of testimonials page and video playback — cannot be verified without live session
```

Scoring rules:
- 10 criteria x 1.0 point each = 10.0 maximum
- Integrity deductions applied after summing criteria
- Minimum score to declare GATE_PASS: 9.2 (higher bar than Phase B's 8.75)
- Score is a single decimal-rounded number (e.g., 9.00, 9.50, 10.0)
- stripeService.ts Stripe version string error is documented as pre-existing — do NOT deduct for it
  </action>
  <verify>
    <automated>cd /c/Users/glcar/prevailing-wage && test -f .planning/phases/106-phase-d-watchdog-gate-v7-ship/106-SCORE.md && grep -q "GATE_PASS\|GATE_FAIL" .planning/phases/106-phase-d-watchdog-gate-v7-ship/106-SCORE.md && grep -q "Final score" .planning/phases/106-phase-d-watchdog-gate-v7-ship/106-SCORE.md && echo "106-SCORE.md OK"</automated>
  </verify>
  <done>106-SCORE.md is written, all 10 rows have PASS or FAIL, the LCPtracker gap audit table is populated, the final score is calculated, and exactly one of GATE_PASS or GATE_FAIL is declared.</done>
</task>

<task type="auto">
  <name>Task 3: Ship v7.0.0 (on GATE_PASS only) — tag + ROADMAP update</name>
  <files>.planning/ROADMAP.md</files>
  <action>
**Only execute this task if Task 2 declared GATE_PASS. If GATE_FAIL, stop here and surface the
failing criteria for remediation. Do not create the tag on a GATE_FAIL.**

**Step 1 — Create the annotated git tag:**
```
cd /c/Users/glcar/prevailing-wage
git tag -a v7.0.0 -m "v7.0.0 — prevailing-wage milestone complete"
```
Verify: `git tag | grep v7.0.0` returns `v7.0.0`.

**Step 2 — Update ROADMAP.md Phase D progress table:**

Find the v7.0 Progress table section and update these rows from "Not started" to "Complete"
with today's date (2026-04-27):

```
| 101. Customer Testimonials + Video | v7.0 | 2/2 | Complete | 2026-04-27 |
| 102. Enterprise Pricing + SSO Foundation | v7.0 | 2/2 | Complete | 2026-04-27 |
| 103. AI Classification Assist | v7.0 | 3/3 | Complete | 2026-04-27 |
| 104. Advanced Audit Analytics | v7.0 | 2/2 | Complete | 2026-04-27 |
| 105. Growth Dashboard (Admin) | v7.0 | 2/2 | Complete | 2026-04-27 |
| 106. Phase D Watchdog Gate + v7.0 Ship | v7.0 | 1/1 | Complete | 2026-04-27 |
```

**Step 3 — Mark Phase D checkbox items complete in ROADMAP.md:**

In the Phase D section under "v7.0 Industry Leadership (Phases 83–106)", update each
`- [ ]` line for phases 101-106 to `- [x]`.

**Step 4 — Update Phase D acceptance criteria block:**

Under "WATCHDOG GATE: Score >= 9.2/10 required to ship v7.0 milestone.", add on a new line:
```
**GATE_PASS declared {date} — Score {X.XX}/10 — v7.0.0 tagged.**
```

**Step 5 — Commit the ROADMAP update:**
```
cd /c/Users/glcar/prevailing-wage
git add .planning/ROADMAP.md .planning/phases/106-phase-d-watchdog-gate-v7-ship/106-SCORE.md
git commit -m "chore(106): Phase D Watchdog GATE_PASS — v7.0.0 ship"
```
  </action>
  <verify>
    <automated>cd /c/Users/glcar/prevailing-wage && git tag | grep -q "v7.0.0" && echo "v7.0.0 tag exists" && grep -q "106.*Complete" .planning/ROADMAP.md && echo "ROADMAP updated"</automated>
  </verify>
  <done>git tag v7.0.0 exists in the repo. ROADMAP.md phases 101–106 all show Complete with date 2026-04-27. v7.0 milestone is officially shipped.</done>
</task>

</tasks>

<verification>
Gate passes when:
1. 106-SCORE.md exists in `.planning/phases/106-phase-d-watchdog-gate-v7-ship/`
2. All 10 criteria rows have a verdict (PASS or FAIL)
3. LCPtracker gap audit table is populated with all 8 rows
4. Final score >= 9.2 → GATE_PASS declared, v7.0.0 tagged, ROADMAP updated
5. Full test suite is green (no new failures)
6. No new TypeScript errors beyond pre-existing workers.ts + stripeService.ts

Gate fails when:
- Final score < 9.2 → GATE_FAIL declared with remediation bullets pointing to the
  specific file and fix for each failed criterion
- v7.0.0 tag is NOT created until GATE_PASS is declared
</verification>

<success_criteria>
- 106-SCORE.md exists with 10 scored rows and LCPtracker audit table
- Final numeric score computed and displayed
- GATE_PASS or GATE_FAIL declared unambiguously
- If GATE_FAIL: each failing criterion has a specific file path and fix description
- If GATE_PASS: git tag v7.0.0 exists; ROADMAP.md phases 101-106 marked Complete
- LCPtracker gap audit identifies AHEAD/PARITY/BEHIND for 8 feature dimensions
</success_criteria>

<output>
After completion, create `.planning/phases/106-phase-d-watchdog-gate-v7-ship/106-01-SUMMARY.md` with:
- Final gate verdict (GATE_PASS / GATE_FAIL)
- Final score (X.XX / 10)
- Count of passing criteria out of 10
- Any failing criteria listed by ID with fix description
- Test suite result (X passing, Y failing)
- LCPtracker audit summary (N AHEAD, M PARITY, K BEHIND)
- Whether v7.0.0 tag was created
- Whether ROADMAP.md was updated
</output>
