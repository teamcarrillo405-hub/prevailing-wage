---
phase: 64-soc2-logging-page-polish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/client/pages/ProjectDetailPage.tsx
  - src/client/pages/PayrollListPage.tsx
  - src/client/pages/PayrollWeekDetailPage.tsx
autonomous: false
requirements: [DESIGN-01]

must_haves:
  truths:
    - "ProjectDetailPage header area visually matches landing page card quality (elevated shadow, bold section separation)"
    - "PayrollListPage week rows use Card with shadow-card-elevated and status badge color tokens"
    - "PayrollWeekDetailPage download buttons are grouped in a sticky bottom action bar without changing their conditional rendering logic"
  artifacts:
    - path: "src/client/pages/ProjectDetailPage.tsx"
      provides: "Premium card treatment on project header and stats sections"
      contains: "shadow-card-elevated"
    - path: "src/client/pages/PayrollListPage.tsx"
      provides: "Elevated card rows with compliant/violation/submitted badge colors"
      contains: "shadow-card-elevated"
    - path: "src/client/pages/PayrollWeekDetailPage.tsx"
      provides: "Sticky bottom bar grouping all download buttons"
      contains: "sticky bottom-0"
  key_links:
    - from: "src/client/pages/PayrollWeekDetailPage.tsx"
      to: "download button conditional structure"
      via: "sticky wrapper div preserving all existing state-gated conditionals"
      pattern: "sticky bottom-0"
---

<objective>
Apply premium design treatment to the three highest-traffic internal pages — ProjectDetailPage, PayrollListPage, and PayrollWeekDetailPage — to match landing page card quality.

Purpose: Users spend the most time on these three pages; bringing them to landing-page visual quality increases perceived product polish and sets the tone for the SOC 2 presentation.
Output: Surgical Tailwind class upgrades on three existing files. No component API changes, no logic changes, no file rewrites.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md

<interfaces>
<!-- Design tokens available in Tailwind CSS v4 config: -->
shadow-card-elevated = 0 8px 24px rgba(0,0,0,0.12)   — use class: shadow-card-elevated
Status badge color mapping (use with Badge component variant prop or className):
  compliant  → emerald  (class: bg-emerald-100 text-emerald-800 or variant="compliant")
  violations → crimson  (class: bg-red-100 text-red-800 or variant="violation")
  submitted  → gold     (class: bg-amber-100 text-amber-800 or variant="submitted")

<!-- Component primitives (already imported in all three files): -->
Card       — wraps content; accepts className prop for extra Tailwind
Badge      — status chip; accepts variant or className
Button     — action button; variant="primary"|"secondary"|"ghost"
PageHeader — page title area
EmptyState — zero-state placeholder

<!-- ProjectDetailPage.tsx key facts: -->
- File is ~1151 lines. SURGICAL EDITS ONLY — do not rewrite.
- Already imports: Card, Badge, Button, PageHeader, EmptyState, useToast
- Project header section is around lines 600–700 (search for <PageHeader or the outer card wrapper)
- WorkflowProgress component is defined in the file (lines 74–110)
- STATUS_BADGE is imported from cprStatus lib and already used

<!-- PayrollListPage.tsx key facts: -->
- Already imports Card, Badge, Button
- Week rows are rendered in a .map() loop (search for weeks.map)
- Each week row currently uses Card but may lack elevated shadow
- Badge already used for isFinal and submittedAt status — upgrade colors to design tokens

<!-- PayrollWeekDetailPage.tsx key facts: -->
- Download buttons are at approximately lines 1036–1108 (confirmed from grep)
- Buttons are inside a <div> container within a flex row
- They are state-gated with conditionals: weekId &&, isCA &&, isWA &&, isNY &&, isIL &&
- Do NOT alter any conditional expression — only wrap the outer div in a sticky bar
- The sticky bar must NOT cover page content — add bottom padding (pb-24) to the main content area
- Existing button group div is inside a flex container around line 1108: </div> closes it
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade ProjectDetailPage.tsx — elevated card header and section cards</name>
  <files>src/client/pages/ProjectDetailPage.tsx</files>
  <action>
    Read the file before editing. Make SURGICAL changes only — do not rewrite sections, do not remove existing JSX.

    **Target areas (search for these strings to find exact locations):**

    1. **Project header card** — find the outermost Card wrapping the project name/metadata section (around the PageHeader or the first major Card after the loading guard). Add `className="shadow-card-elevated"` to that Card. If it already has a className, append the class: `className="existing-classes shadow-card-elevated"`.

    2. **Stats/info cards** — find any Card components that display contract type, funding type, award date, or status (likely 2–4 small stat cards). Add `className="shadow-card-elevated"` to each.

    3. **WorkflowProgress steps** — the step number circles use `border-gray-300`. Upgrade incomplete steps to use `border-brand-navy/30` instead of `border-gray-300`. The complete step color (`bg-status-compliant`) is already correct — do not change it.

    4. **Section dividers** — find any `<hr>` or `border-t border-gray-200` divider between major sections. Upgrade to `border-t border-brand-navy/10`.

    **Do NOT:**
    - Change any onClick, mutation, or data-fetching logic
    - Rewrite the WorkflowProgress component — change only the className strings
    - Add new components not already imported
    - Modify the SubcontractorCPR section structure (it has complex state)
    - Change file length by more than ~15 lines (this is a style-only pass)
  </action>
  <verify>
    <automated>grep -c "shadow-card-elevated" src/client/pages/ProjectDetailPage.tsx</automated>
  </verify>
  <done>At least 2 occurrences of `shadow-card-elevated` in the file. `pnpm tsc --noEmit` passes. No new TypeScript errors.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade PayrollListPage.tsx — elevated week rows and semantic badge colors</name>
  <files>src/client/pages/PayrollListPage.tsx</files>
  <action>
    Read the file before editing. Make surgical changes only.

    **Target areas:**

    1. **Week row cards** — find the `weeks.map(...)` loop (around line 200+). Each iteration renders a Card for a payroll week. Add `className="shadow-card-elevated hover:shadow-lg transition-shadow"` to each week's Card element.

    2. **Status badges — submitted** — find Badge elements that check `week.submittedAt`. Add or upgrade className to use gold tokens: `className="bg-amber-100 text-amber-800 font-medium"`. If Badge uses a variant prop, set `variant="submitted"` if that variant exists, otherwise use className directly.

    3. **Status badges — amendment** — find Badge elements for `week.amendmentNumber`. Style as: `className="bg-blue-100 text-blue-800 font-medium"`.

    4. **Status badges — final** — find Badge for `week.isFinal`. Style as: `className="bg-emerald-100 text-emerald-800 font-medium"` (compliant/green = "this is locked in").

    5. **New week button** — find the primary "New Payroll Week" or equivalent Button. If it uses `variant="secondary"`, upgrade to `variant="primary"` so it stands out as the primary action.

    6. **Empty state** — if EmptyState is used (no weeks yet), confirm it has a descriptive subtitle. If the subtitle is missing or generic, add: `subtitle="Start by clicking 'New Payroll Week' above."` as a prop.

    **Do NOT:**
    - Change any modal logic (copy week modal has complex state — surgical CSS only)
    - Change the Card component's API or add new props that don't exist
    - Alter the `handleNewWeekClick`, `handleChooseCopy`, or `handlePreview` functions
  </action>
  <verify>
    <automated>grep -c "shadow-card-elevated\|amber-100\|emerald-100" src/client/pages/PayrollListPage.tsx</automated>
  </verify>
  <done>At least 3 occurrences of the target class names. `pnpm tsc --noEmit` passes. No new TypeScript errors.</done>
</task>

<task type="auto">
  <name>Task 3: Upgrade PayrollWeekDetailPage.tsx — sticky download action bar</name>
  <files>src/client/pages/PayrollWeekDetailPage.tsx</files>
  <action>
    Read the file before editing. This is the most delicate task — the download buttons have complex state-gated conditionals that must not be altered.

    **The goal:** Wrap the existing download button group in a sticky bottom bar so users can always see export actions without scrolling. Do not change any button's conditional logic, onClick, disabled state, or label.

    **Step 1 — Find the download button group container.**
    Search for the div that contains the first `{weekId && (<Button ... onClick={handleDownloadClick}>Download WH-347</Button>)}` (around line 1036). This div is likely a `<div className="flex gap-2 flex-wrap ...">` or similar flex container ending around line 1108.

    **Step 2 — Replace the outer wrapper div** of that button group from:
    ```tsx
    <div className="flex gap-2 flex-wrap ...">
      {/* all the conditional buttons */}
    </div>
    ```
    To a sticky bottom bar:
    ```tsx
    <div className="sticky bottom-0 z-10 bg-white border-t border-brand-navy/10 px-6 py-3 -mx-6 mt-8">
      <div className="flex gap-2 flex-wrap items-center">
        {/* ALL existing conditional buttons unchanged */}
      </div>
    </div>
    ```

    The `-mx-6` pulls the bar edge-to-edge relative to the page padding. Adjust the negative margin to match whatever horizontal padding the page content uses (check the Layout component's content padding — it is likely `px-6` or `px-8`; match accordingly).

    **Step 3 — Add bottom padding to the main content area** so the sticky bar does not cover the last content rows. Find the main content wrapper div (likely a `<div className="max-w-... mx-auto ...">` or the Layout's content slot) and add `pb-24` to it.

    **Step 4 — Do NOT change anything inside the button group.** Every single `{weekId && ...}`, `{isCA && ...}`, `{isWA && ...}`, `{isNY && ...}`, `{isIL && ...}`, `{stateFormConfig && ...}` conditional must remain byte-for-byte identical. Only the outer wrapper div changes.

    **Critical guards:**
    - Do NOT rewrite any conditional expression
    - Do NOT change button variant, onClick, disabled, or label
    - Do NOT move buttons outside the sticky bar
    - Do NOT add new buttons
    - The file is ~1350+ lines — change count should be under 10 lines net
  </action>
  <verify>
    <automated>grep -n "sticky bottom-0" src/client/pages/PayrollWeekDetailPage.tsx</automated>
  </verify>
  <done>
    - `grep "sticky bottom-0" src/client/pages/PayrollWeekDetailPage.tsx` returns 1 match
    - `grep -c "handleDownloadClick\|handleCaDownloadClick\|handleWaDownloadClick\|handleStateFormDownload" src/client/pages/PayrollWeekDetailPage.tsx` returns same count as before editing (no handlers removed)
    - `pnpm tsc --noEmit` passes
  </done>
</task>

</tasks>

<verification>
Visual verification required — automated checks confirm structure but not rendered quality.

Browser verification steps:
1. Navigate to any project's detail page (`/projects/:id`). The project metadata card should have visible depth (elevated shadow). The workflow step circles should use `border-brand-navy/30` for incomplete steps.
2. Navigate to `/projects/:id/payroll`. Week rows should show clear card elevation on hover. Status badges should be amber (submitted), emerald (final), or blue (amendment) — not default gray.
3. Navigate to `/projects/:id/payroll/:weekId`. Scroll to the bottom — the download buttons should float above the page content in a white sticky bar with a subtle top border. Scroll back up — the buttons should still be visible at the bottom. All download buttons should still function identically.
4. Confirm no console errors in all three pages.
</verification>

<success_criteria>
- `grep "shadow-card-elevated" src/client/pages/ProjectDetailPage.tsx` returns 2+ matches
- `grep "shadow-card-elevated" src/client/pages/PayrollListPage.tsx` returns 1+ matches
- `grep "sticky bottom-0" src/client/pages/PayrollWeekDetailPage.tsx` returns exactly 1 match
- `pnpm tsc --noEmit` passes with zero new errors across all three files
- No conditional download button logic in PayrollWeekDetailPage.tsx was altered (diff confirms only wrapper div changed)
- Browser shows elevated card quality on all three pages with no visual regressions
</success_criteria>

<output>
After completion, create `.planning/phases/64-soc2-logging-page-polish/64-03-SUMMARY.md` with:
- Screenshots or descriptions of before/after for each page
- Exact line ranges changed in each file
- Any design token names that did not resolve (fallback used)
- Any deviations from this plan
</output>
