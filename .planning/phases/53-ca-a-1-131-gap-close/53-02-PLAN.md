---
phase: 53-ca-a-1-131-gap-close
plan: 02
type: execute
wave: 2
depends_on:
  - 53-01
files_modified:
  - src/server/services/a1131Generator.ts
autonomous: false
requirements:
  - CA-02

must_haves:
  truths:
    - "Clicking the CA button shows the CSLB/WC disclosure modal before any PDF download occurs"
    - "The downloaded A-1-131 PDF shows correct contractor name, address, CSLB license, payroll number, and week ending date in the header"
    - "Per-worker rows display correct hours (ST/OT/DT), hourly rate, gross wages, and deductions at readable coordinates"
    - "The certification page (page 2) shows contractor name, payroll description, and date — not blank"
    - "SDI and other deduction values are legible and do not visually overlap adjacent columns"
    - "DT rows (if present) fall within the worker block boundary — not overlapping the next worker's section"
  artifacts:
    - path: "src/server/services/a1131Generator.ts"
      provides: "Coordinate constants verified against live rendered PDF"
      contains: "HEADER"
  key_links:
    - from: "Browser CA download button"
      to: "showCaDisclosure modal"
      via: "handleCaDownloadClick()"
      pattern: "stateFormConfig\\.route === 'a1131'"
    - from: "a1131Generator.ts HEADER constants"
      to: "PDF field positions"
      via: "CTM landscape drawing"
      pattern: "lx.*ly"
---

<objective>
Formally execute the browser verification of the CA A-1-131 PDF that was specified as Phase 24 Task 3 but never performed. Apply any coordinate corrections discovered during visual inspection.

Purpose: CA-02 requires human-eye verification that all PDF field coordinates are correct. Unit tests confirm the PDF is valid %PDF; only browser rendering confirms visual placement against the official A-1-131 form grid.

Output:
- Human-verified confirmation that all 5 coordinate sections are correctly placed (or documented corrections applied to a1131Generator.ts)
- CA-02 formally marked complete
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-ca-a-1-131-gap-close/53-RESEARCH.md
@.planning/phases/53-ca-a-1-131-gap-close/53-VALIDATION.md
@.planning/phases/53-ca-a-1-131-gap-close/53-01-SUMMARY.md

<interfaces>
<!-- Coordinate constants in a1131Generator.ts — reference for corrections if needed. -->

Current HEADER constants (landscape lx/ly after CTM on page 1):
```typescript
contractorName:  { lx: 315, ly: 535 }
cslbLicense:     { lx: 618, ly: 535 }
address:         { lx: 770, ly: 535 }
payrollNo:       { lx: 283, ly: 514 }
weekEndingDate:  { lx: 454, ly: 514 }
wcPolicyNumber:  { lx: 675, ly: 497 }
projectNo:       { lx: 866, ly: 514 }
projectLocation: { lx: 856, ly: 497 }
```

Current worker row Y formula:
```typescript
// stY = 391 - (workerIdx * 92)
// otY = stY - 47
// dtY = stY - 94   ← NOTE: 2pt into next worker's block (block spacing is 92)
```

Current cert page (page 2) — absolute portrait coords (NO CTM on page 2):
```typescript
contractor name: x=80,  y=630
payroll desc:    x=375, y=588
date:            x=108, y=451
```

Correction pattern (if any coordinate is off):
- Increase lx → moves text RIGHT
- Decrease lx → moves text LEFT
- Increase ly → moves text UP
- Decrease ly → moves text DOWN
- For page 2: increase y → moves text UP, increase x → moves text RIGHT
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start dev server and create CA test project with payroll data</name>
  <files></files>
  <action>
    Start the dev server in the background:
    ```bash
    npm run dev
    ```
    Server runs on port 4099.

    If a CA project with at least one payroll entry already exists in the dev database, use it. Otherwise create one:
    1. Navigate to http://localhost:4099
    2. Log in with the dev account
    3. Create a project with state = CA, fill in CSLB License Number (e.g., "B-123456") and WC Policy Number (e.g., "WC-2024-001")
    4. Add at least 1 worker with a classification
    5. Create a payroll week and enter hours for the worker (include ST, OT, and DT hours if possible to test all row types)
    6. Note the payroll week URL (contains the weekId)

    Then navigate to PayrollWeekDetailPage for that CA payroll week. Confirm the "Download CA A-1-131" button is present.
  </action>
  <verify>
    <automated>npx vitest run tests/services/a1131.test.ts</automated>
  </verify>
  <done>
    - Dev server is running on port 4099
    - A CA payroll week with at least 1 worker entry exists and is accessible
    - Unit tests still pass (no regressions from Plan 01 changes)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plan 53-01 fixed the CA button routing bug and added the audit log. The dev server is running with a CA payroll week ready.

    This checkpoint verifies:
    1. The modal routing fix works (button → disclosure modal, not direct download)
    2. The A-1-131 PDF field coordinates are correct on both pages
    3. The audit log entry is created on download
  </what-built>
  <how-to-verify>
    **Step 1 — Verify disclosure modal routing:**
    1. Go to http://localhost:4099 and navigate to the CA payroll week
    2. Click "Download CA A-1-131"
    3. CONFIRM: A modal appears with CSLB/WC disclosure language (not an immediate file download)
    4. Click the confirm button in the modal
    5. CONFIRM: The A-1-131 PDF downloads successfully

    **Step 2 — Open PDF and inspect page 1 (worker data page):**
    Open the downloaded PDF in your browser or PDF viewer. Compare against the official form layout.

    Check these 5 sections:

    A. **Header row (top of form):**
       - Contractor name appears in the "Name of Contractor or Sub-Contractor" box
       - CSLB license appears in the license field
       - Address appears in the address field
       - Payroll number in its box, week ending date in its box
       - WC policy number and project location visible in lower header row

    B. **Per-worker rows:**
       - Worker name and trade classification appear on the left
       - Day hour columns (M/T/W/T/F/S/S) show correct values
       - Total hours, hourly rate, and gross wages columns are readable
       - ST row, OT row (if OT hours present) visible within each worker block

    C. **Deduction columns (right side of form):**
       - Federal tax, state tax, SDI deduction values are readable
       - Values do not visually bleed into adjacent columns

    D. **DT rows (if you entered DT hours):**
       - DT row appears within the worker's block — NOT overlapping the next worker's header row
       - If DT row overlaps: note the pixel distance of the overlap

    E. **Fringe section:**
       - Fringe credit value is visible (drawn at otY position)

    **Step 3 — Open PDF and inspect page 2 (certification page):**
    - Contractor name appears at the top of the certification statement
    - Payroll description text is visible
    - Date field shows the correct week-ending date
    - CONFIRM: Page 2 is NOT a blank gray page (if blank, note this)

    **Step 4 — Check audit log:**
    1. Go to the project's Activity page (Project → Activity)
    2. CONFIRM: A "ca_pdf.downloaded" entry appears for the week just downloaded

    **Respond with one of:**
    - "approved" — all sections correct, no coordinate issues found
    - A list of which sections had issues and what you observed (e.g., "contractor name is 20px too far right", "cert page is blank", "DT row overlaps next worker")
  </how-to-verify>
  <resume-signal>Type "approved" or describe any coordinate issues found</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Apply coordinate corrections (if needed) and run final test suite</name>
  <files>src/server/services/a1131Generator.ts</files>
  <action>
    IF the checkpoint returned "approved" with no issues:
    - Skip coordinate changes entirely
    - Run the final test suite to confirm clean state
    - Proceed to output step

    IF the checkpoint identified coordinate issues:
    - Open src/server/services/a1131Generator.ts
    - Adjust the relevant constants in HEADER, COL, or getWorkerRowLY() based on the feedback:
      - Text too far right → decrease lx
      - Text too far left → increase lx
      - Text too high → decrease ly
      - Text too low → increase ly
      - DT overlapping next worker → change blockSpacing from 92 to 96 (expand) OR change subRowSpacing from 47 to 44 (compress)
      - Cert page blank → the absolute portrait coordinates need adjustment (increase y to move UP; typical cert field range is y=400–700)
    - After each adjustment, run: `npx vitest run tests/services/a1131.test.ts`
    - Tests must remain green (coordinate changes do not affect test assertions which check PDF byte validity, not exact field positions)

    CRITICAL: Do NOT change the CTM setup (`concatTransformationMatrix` call) or the pushGraphicsState/popGraphicsState wrapping — these are architecturally correct. Adjust only numeric constant values.

    After corrections (or with no corrections needed):
    Run full CA test suite:
    ```bash
    npx vitest run tests/services/a1131.test.ts tests/routes/export.test.ts
    ```
  </action>
  <verify>
    <automated>npx vitest run tests/services/a1131.test.ts tests/routes/export.test.ts</automated>
  </verify>
  <done>
    - All 7 a1131 unit tests pass
    - All export route integration tests pass
    - Any coordinate corrections documented in the SUMMARY.md
    - CA-02 formally verified complete
  </done>
</task>

</tasks>

<verification>
Final verification after all tasks complete:

```bash
npx vitest run tests/services/a1131.test.ts tests/routes/export.test.ts
npx tsc --noEmit
```

Both must be green. The checkpoint approval (Step 2 above) is the authoritative CA-02 gate.
</verification>

<success_criteria>
- User clicked CA download button and saw the eCPR disclosure modal (not a direct download)
- All 5 PDF sections verified: header, per-worker rows, deduction columns, DT rows (if applicable), cert page
- Cert page 2 is NOT blank — contractor name, payroll description, and date are visible
- Audit log shows ca_pdf.downloaded entry on the project Activity page
- Any coordinate corrections applied to a1131Generator.ts and tests still pass
- CA-02 status updated to [x] in REQUIREMENTS.md
</success_criteria>

<output>
After completion, create `.planning/phases/53-ca-a-1-131-gap-close/53-02-SUMMARY.md` using the summary template.

Also update `.planning/REQUIREMENTS.md` line for CA-02: change `- [ ] **CA-02**` to `- [x] **CA-02**`.
</output>
