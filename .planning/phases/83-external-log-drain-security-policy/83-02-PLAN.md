---
phase: 83-external-log-drain-security-policy
plan: 02
type: execute
wave: 2
depends_on:
  - "83-01"
files_modified:
  - SECURITY_POLICY.md
  - src/client/pages/SecurityPolicyPage.tsx
autonomous: true
requirements:
  - SEC-08

must_haves:
  truths:
    - "SECURITY_POLICY.md exists at repo root and is committed to git"
    - "SECURITY_POLICY.md is consistent with the existing /security React page (same email, same SLA values)"
    - "SECURITY_POLICY.md content matches what /.well-known/security.txt advertises (security@prevailingwage.app contact, /security-policy URL)"
    - "Required SECURITY_POLICY.md sections present: Supported Versions, Reporting a Vulnerability, Response SLA, Responsible Disclosure, Scope"
    - "SecurityPolicyPage.tsx (the /security route) renders a 72h ack SLA value matching SECURITY_POLICY.md"
    - "All 724 existing tests continue to pass"
  artifacts:
    - path: "SECURITY_POLICY.md"
      provides: "GitHub-displayed security policy + SOC 2 auditor evidence document"
      contains: "## Reporting a Vulnerability"
    - path: "src/client/pages/SecurityPolicyPage.tsx"
      provides: "User-facing /security page with SLA values aligned to SECURITY_POLICY.md"
      contains: "72 hours"
  key_links:
    - from: "SECURITY_POLICY.md"
      to: "security@prevailingwage.app"
      via: "Reporting a Vulnerability section contact line"
      pattern: "security@prevailingwage\\.app"
    - from: "src/client/pages/SecurityPolicyPage.tsx"
      to: "SECURITY_POLICY.md SLA values"
      via: "Same-text 72h acknowledgement and 7-day resolution promises"
      pattern: "72 hours"
    - from: "/.well-known/security.txt (already served by index.ts)"
      to: "https://prevailingwage.app/security-policy"
      via: "Existing Phase 80 wiring — references the policy URL that SecurityPolicyPage.tsx satisfies"
      pattern: "security-policy"
---

<objective>
Create `SECURITY_POLICY.md` at the repo root and reconcile the existing `SecurityPolicyPage.tsx` /security route so all three security artifacts (markdown file, React page, /.well-known/security.txt) advertise the SAME contact email and the SAME 72-hour acknowledgement SLA. Closes SOC 2 CC7/CC9 evidence gap SEC-08.

Purpose: GitHub auto-renders `SECURITY_POLICY.md` on the repo's "Security" tab — auditors and good-faith researchers expect this file at root. The existing /security React page already satisfies the "served at /security" success criterion but its SLA copy says "48 hours" and its contact email is `security@prevwage.app` — both inconsistent with the spec (72h ack) and with `.well-known/security.txt` (which uses `security@prevailingwage.app`). One source of truth is required.

Output: New `SECURITY_POLICY.md` at repo root + minimal copy edits to `SecurityPolicyPage.tsx` to align SLA wording and contact email.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/83-external-log-drain-security-policy/83-RESEARCH.md
@.planning/phases/83-external-log-drain-security-policy/83-01-SUMMARY.md
@src/client/pages/SecurityPolicyPage.tsx
@src/server/index.ts

<interfaces>
<!-- /.well-known/security.txt — ALREADY SERVED by src/server/index.ts (Phase 80, lines 97-107). -->
<!-- This is the canonical source for both contact email and policy URL. -->
<!-- Do NOT modify index.ts in this plan. -->
```
Contact: mailto:security@prevailingwage.app
Expires: 2027-01-01T00:00:00.000Z
Preferred-Languages: en
Policy: https://prevailingwage.app/security-policy
```

<!-- Existing SecurityPolicyPage.tsx (lines 167-185) — Responsible Disclosure section currently says: -->
<!--   "We commit to acknowledging your report within 48 hours and providing -->
<!--    a remediation timeline within 7 business days." -->
<!--   Email link: <a href="mailto:security@prevwage.app"> -->
<!-- BOTH must be updated to match SECURITY_POLICY.md (72h ack, security@prevailingwage.app). -->

<!-- The phase goal explicitly specifies: -->
<!--   - reporting email: teamcarrillo405@gmail.com -->
<!-- BUT .well-known/security.txt advertises security@prevailingwage.app. -->
<!-- Resolution (per Research Pitfall 5 — all three files must agree): -->
<!--   Use security@prevailingwage.app as the PRIMARY listed contact (matches /.well-known/security.txt -->
<!--   which is already deployed and indexed). teamcarrillo405@gmail.com listed as the BACKUP / -->
<!--   personal escalation in the Reporting section so the spec's "reporting email" is honored without -->
<!--   contradicting the live security.txt advertisement. This keeps a single .well-known source -->
<!--   of truth and a single GitHub-displayed file. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create SECURITY_POLICY.md at repo root</name>
  <files>SECURITY_POLICY.md</files>
  <read_first>
    - src/server/index.ts (lines 97-107 — confirm /.well-known/security.txt content; we mirror its email + policy URL)
    - src/client/pages/SecurityPolicyPage.tsx (full file — note current sections: Data Encryption, Data in Transit, Infrastructure, Access Controls, SOC 2 status, Responsible Disclosure, Data Retention)
    - .planning/phases/83-external-log-drain-security-policy/83-RESEARCH.md (Section "SECURITY_POLICY.md Structure" — canonical GitHub format)
  </read_first>
  <action>
    Create a new file `SECURITY_POLICY.md` at the REPO ROOT (NOT in `.planning/`, NOT in `src/`, NOT in `docs/`) with the following content. This file is the GitHub-rendered policy + SOC 2 evidence artifact.

    File content (write exactly):

    ```markdown
    # Security Policy

    PrevWage takes security seriously. This document describes which versions are supported, how to report vulnerabilities, and what response you can expect from us.

    ## Supported Versions

    PrevWage is a single-tenant SaaS application. Only the version currently deployed at `https://prevailingwage.app` is supported. There is no end-of-life schedule for older versions because customers always run the latest deployment.

    | Version | Supported |
    | ------- | --------- |
    | Production (`prevailingwage.app` — latest deploy) | Yes |
    | Self-hosted forks | No — community responsibility |
    | Pre-release / staging branches | No |

    ## Reporting a Vulnerability

    Please report security vulnerabilities **privately**. Do not open a public GitHub issue, post in social media, or share details with third parties before we have had a reasonable opportunity to address the report.

    **Primary contact (preferred):** [security@prevailingwage.app](mailto:security@prevailingwage.app)

    **Escalation contact (if no acknowledgement within 72 hours):** [teamcarrillo405@gmail.com](mailto:teamcarrillo405@gmail.com)

    The same primary contact is also published in [`.well-known/security.txt`](https://prevailingwage.app/.well-known/security.txt) for automated discovery.

    Please include:

    - A clear description of the vulnerability and its impact
    - Step-by-step reproduction instructions (or proof-of-concept)
    - Affected URL(s), endpoint(s), or component(s)
    - Any logs, screenshots, or supporting material
    - Whether you would like to be publicly credited after remediation

    PGP-encrypted reports are welcome. PGP key fingerprint: *to be published* — request via the primary contact and we will send the key out-of-band.

    ## Response SLA

    | Phase | Target |
    | ----- | ------ |
    | Acknowledgement of receipt | Within **72 hours** of report |
    | Initial triage and severity classification | Within 7 calendar days |
    | Remediation plan with target fix date | Within 14 calendar days |
    | High / Critical fix deployed | Within 30 calendar days |
    | Coordinated public disclosure (if applicable) | Negotiated with reporter |

    Acknowledgements come from the primary contact. If you have not received an acknowledgement within 72 hours, please email the escalation contact.

    ## Responsible Disclosure

    We follow a coordinated disclosure model:

    1. You report the issue privately via the primary contact.
    2. We acknowledge within 72 hours.
    3. We confirm the issue, assign a severity, and share a remediation timeline.
    4. We deploy a fix to production.
    5. We coordinate the public disclosure date with you, including a credit line if you opt in.
    6. We will not pursue legal action against good-faith security researchers who:
       - Make a reasonable effort to avoid privacy violations and service disruption
       - Do not exfiltrate, modify, or destroy customer data
       - Do not access more data than necessary to demonstrate the issue
       - Give us a reasonable opportunity to remediate before public disclosure

    ## Scope

    **In scope:**

    - The production application at `https://prevailingwage.app` and its API surface
    - The publicly accessible `/upload/:token` subcontractor portal
    - The public REST API at `/v1/*` (Bearer-token authenticated)
    - The marketing pages and public-facing routes (`/`, `/about`, `/security`, `/api-docs`, `/case-studies/*`)
    - First-party JavaScript bundles served by the application

    **Out of scope:**

    - Third-party services we depend on (Render, Resend, Stripe, Sentry, Better Stack) — please report directly to those vendors
    - Denial-of-service or volumetric attacks
    - Social engineering of staff or customers
    - Physical attacks against infrastructure
    - Findings from automated scanners that do not demonstrate a real-world impact
    - Self-XSS that requires a victim to paste attacker-controlled content into the browser console
    - Missing security headers without a demonstrated exploit chain

    ## Recognition

    We do not currently operate a paid bug bounty program. We do publish a public Hall of Fame for researchers who responsibly disclose qualifying issues — opt in via the primary contact.

    ---

    *Last updated: April 2026.*
    *Mirrored at [https://prevailingwage.app/security](https://prevailingwage.app/security) (rendered React page) and [`.well-known/security.txt`](https://prevailingwage.app/.well-known/security.txt) (machine-readable contact).*
    ```

    Key constraints:
    - File path is EXACTLY `SECURITY_POLICY.md` at repo root (uppercase + underscore + `.md`). GitHub auto-detects this exact filename for the Security tab.
    - Primary contact MUST be `security@prevailingwage.app` (matches the already-deployed `.well-known/security.txt` from Phase 80 — Research Pitfall 5: all three artifacts must agree).
    - Escalation contact `teamcarrillo405@gmail.com` honors the spec's "reporting email" without contradicting the live security.txt.
    - SLA values: 72 hours ack, 7 days triage, 14 days remediation plan, 30 days fix (per phase spec).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const p='SECURITY_POLICY.md';if(!fs.existsSync(p))process.exit(1);const c=fs.readFileSync(p,'utf8');const need=['## Supported Versions','## Reporting a Vulnerability','## Response SLA','## Responsible Disclosure','## Scope','security@prevailingwage.app','teamcarrillo405@gmail.com','72 hours'];for(const n of need){if(!c.includes(n)){console.error('MISSING:',n);process.exit(2)}}console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File `SECURITY_POLICY.md` exists at the repo root (sibling to `package.json`, `.env.example`, `README.md`)
    - File contains heading `## Supported Versions`
    - File contains heading `## Reporting a Vulnerability`
    - File contains heading `## Response SLA`
    - File contains heading `## Responsible Disclosure`
    - File contains heading `## Scope`
    - File contains the literal string `security@prevailingwage.app`
    - File contains the literal string `teamcarrillo405@gmail.com`
    - File contains the literal string `72 hours`
    - File does NOT introduce any new code dependencies (pure markdown)
  </acceptance_criteria>
  <done>
    Markdown file exists at repo root with all five required sections, both contact addresses, and the 72-hour SLA. GitHub will auto-render this on the repo Security tab on next push.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Reconcile SecurityPolicyPage.tsx SLA copy + contact email with SECURITY_POLICY.md</name>
  <files>src/client/pages/SecurityPolicyPage.tsx</files>
  <read_first>
    - src/client/pages/SecurityPolicyPage.tsx (FULL file — Responsible Disclosure section is at lines 163-186; "Last updated" line is at lines 205-208)
    - SECURITY_POLICY.md (the file just created in Task 1 — values to mirror)
  </read_first>
  <action>
    The existing `/security` React page satisfies the "served at /security" criterion but two values disagree with the new SECURITY_POLICY.md and with `.well-known/security.txt`. Make the MINIMAL textual edits below — do NOT restructure the page or change any styling.

    Edit 1 — Responsible Disclosure paragraph (currently around line 170-174):
    Find this exact text:
    ```
    If you discover a security vulnerability in PrevWage, please report it to our
              security team. We commit to acknowledging your report within 48 hours and providing
              a remediation timeline within 7 business days.
    ```
    Replace with:
    ```
    If you discover a security vulnerability in PrevWage, please report it to our
              security team. We commit to acknowledging your report within 72 hours and providing
              a remediation timeline within 14 calendar days.
    ```
    (The 72-hour SLA value is the load-bearing change. Do NOT add an anchor link to SECURITY_POLICY.md here — the real GitHub repo URL is not yet known, and adding a placeholder href ships a broken link to production. Omit the link entirely; users can find SECURITY_POLICY.md via GitHub.)

    Edit 2 — Security Contact mailto (currently around lines 181-183):
    Find both occurrences of `security@prevwage.app` (one inside the `<a href="mailto:...">` href, one as the link text, AND a third occurrence in the "Last updated" paragraph at line 207).
    Replace ALL THREE with `security@prevailingwage.app`.

    Use a single Find/Replace on the file: `security@prevwage.app` -> `security@prevailingwage.app` (3 replacements expected).

    Constraints:
    - Do NOT change page layout, Tailwind classes, lucide-react icons, or section ordering.
    - Do NOT add new imports.
    - Do NOT remove any of the existing 8 sections (Data Encryption, Data in Transit, Infrastructure, Access Controls, SOC 2 status, Responsible Disclosure, Data Retention, Last updated).
    - The page MUST still pass any existing snapshot/render test — no structural changes.

    After editing, run the full test suite to confirm no regression:
    ```bash
    npx vitest run --exclude ".worktrees/**" --exclude ".claude/worktrees/**"
    ```
    Expected: 724 passing, same as Plan 01 baseline.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('src/client/pages/SecurityPolicyPage.tsx','utf8');if(c.includes('security@prevwage.app')){console.error('STILL HAS OLD EMAIL');process.exit(1)}if(!c.includes('72 hours')){console.error('MISSING 72 HOURS SLA');process.exit(2)}if(!c.includes('security@prevailingwage.app')){console.error('MISSING NEW EMAIL');process.exit(3)}console.log('OK')"</automated>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
    <automated>npx vitest run --exclude ".worktrees/**" --exclude ".claude/worktrees/**"</automated>
  </verify>
  <acceptance_criteria>
    - `src/client/pages/SecurityPolicyPage.tsx` contains the literal string `72 hours`
    - `src/client/pages/SecurityPolicyPage.tsx` contains the literal string `security@prevailingwage.app`
    - `src/client/pages/SecurityPolicyPage.tsx` does NOT contain the literal string `security@prevwage.app` (zero occurrences)
    - `src/client/pages/SecurityPolicyPage.tsx` does NOT contain the literal string `48 hours`
    - The exported component `SecurityPolicyPage` still exists with the same name
    - TypeScript compiles clean (`npx tsc --noEmit -p tsconfig.json` exits 0)
    - Full vitest suite exits 0 with the same passing count as Plan 01 final state (724 passing, 42 todo)
  </acceptance_criteria>
  <done>
    /security page advertises the same SLA (72h) and same primary contact (security@prevailingwage.app) as SECURITY_POLICY.md and as /.well-known/security.txt. Three artifacts now agree.
  </done>
</task>

</tasks>

<verification>
Phase 83 SEC-08 acceptance:
- `ls SECURITY_POLICY.md` (at repo root) succeeds
- `grep -c "## Reporting a Vulnerability" SECURITY_POLICY.md` returns 1
- `grep -c "72 hours" SECURITY_POLICY.md` returns at least 1
- `grep -c "teamcarrillo405@gmail.com" SECURITY_POLICY.md` returns at least 1
- `grep -c "security@prevwage.app" src/client/pages/SecurityPolicyPage.tsx` returns 0 (no longer present)
- `grep -c "security@prevailingwage.app" src/client/pages/SecurityPolicyPage.tsx` returns at least 1
- `npx vitest run --exclude ".worktrees/**" --exclude ".claude/worktrees/**"` exits 0 with 724 passing
- The /security route in `src/client/App.tsx` is UNCHANGED (already registered)

Combined Phase 83 final state:
- `grep -l "@logtail/pino" package.json src/server/logger.ts` returns both files (Plan 01)
- `ls SECURITY_POLICY.md` succeeds (Plan 02)
- All four success criteria from the phase goal are observable
</verification>

<success_criteria>
- SECURITY_POLICY.md exists at repo root with the five required sections
- SECURITY_POLICY.md primary contact = security@prevailingwage.app (matches /.well-known/security.txt)
- SECURITY_POLICY.md escalation contact = teamcarrillo405@gmail.com (honors phase spec)
- SECURITY_POLICY.md ack SLA = 72 hours
- SecurityPolicyPage.tsx /security route SLA copy says 72 hours (was 48)
- SecurityPolicyPage.tsx contact email is security@prevailingwage.app (was security@prevwage.app)
- All 724 tests still pass
- TypeScript compilation clean (zero errors)
- /.well-known/security.txt UNCHANGED (was correct in Phase 80)
</success_criteria>

<output>
After completion, create `.planning/phases/83-external-log-drain-security-policy/83-02-SUMMARY.md` documenting:
- Confirmed file path of SECURITY_POLICY.md (repo root)
- All three security-artifact email addresses now identical (security@prevailingwage.app)
- Test count before/after (must match)
- Note that GitHub's repo Security tab will auto-render this file on next push to default branch
- Confirmation that PGP key fingerprint section is intentionally a placeholder ("to be published") — future enhancement
</output>
