---
phase: 94-offline-payroll-entry-queue
plan: 03
type: execute
wave: 3
depends_on:
  - 94-01
  - 94-02
files_modified:
  - src/client/lib/offlineQueue.ts
  - src/client/components/ui/OfflineBanner.tsx
autonomous: false
requirements:
  - MOB-16
  - MOB-17

must_haves:
  truths:
    - "window 'online' event flushes both the generic offlineQueue and the payroll-specific queue"
    - "OfflineBanner shows combined pending count (generic + payroll queues) in its badge"
    - "Reconnect flush handles 409 from server by calling markSyncedElsewhere — not re-queuing"
    - "Human can verify offline entry flow end-to-end in browser DevTools"
  artifacts:
    - path: "src/client/components/ui/OfflineBanner.tsx"
      provides: "Unified sync badge showing total pending count across both queues"
      contains: "getPendingCount from payrollQueue"
    - path: "src/client/lib/offlineQueue.ts"
      provides: "processQueue unchanged — existing replay path not broken"
  key_links:
    - from: "src/client/components/ui/OfflineBanner.tsx"
      to: "src/client/lib/payrollQueue.ts"
      via: "getPendingCount import"
      pattern: "getPendingCount"
    - from: "OfflineBanner handleOnline"
      to: "useOfflineEntryMutation flush"
      via: "processQueue() drains generic queue; payroll-queue is drained per-wizard-instance on reconnect"
      pattern: "processQueue"
---

<objective>
Wire the reconnect flush path end-to-end and update OfflineBanner to show the combined pending count (generic requests + payroll entries). Add a human-verify checkpoint for the full offline flow.

Purpose: MOB-16 requires replay-on-reconnect and MOB-17 requires 409 conflict resolution. Both are implemented in code by this plan, but browser behavior (DevTools throttle, badge count, optimistic UI) requires human verification.

Output: Updated OfflineBanner with unified count, confirmed flush path, and a human verification step covering the complete offline payroll entry → reconnect → sync flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@src/client/components/ui/OfflineBanner.tsx
@src/client/lib/offlineQueue.ts
@src/client/lib/payrollQueue.ts
</context>

<interfaces>
<!-- From payrollQueue.ts (94-01): -->
```typescript
export async function getPendingCount(): Promise<number>;
// Returns count of entries with status='pending'
```

<!-- From offlineQueue.ts (existing): -->
```typescript
export async function getQueueLength(): Promise<number>;
export async function processQueue(): Promise<void>;
```

<!-- OfflineBanner current OfflineBadge pattern: -->
```typescript
// Polls getQueueLength() every 10s + on online/offline events
// Shows amber pill with count, hides when 0
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update OfflineBanner to show unified pending count</name>
  <files>src/client/components/ui/OfflineBanner.tsx</files>
  <action>
In `OfflineBanner.tsx`, update the `OfflineBadge` component:

1. Import `getPendingCount` from `../../lib/payrollQueue`.
2. In the `refresh()` async function inside the useEffect, call both:
   ```typescript
   const [genericLen, payrollLen] = await Promise.all([
     getQueueLength(),
     getPendingCount(),
   ]);
   if (active) setQueueLen(genericLen + payrollLen);
   ```
3. Update the badge `title` and `aria-label` to say:
   - `"${queueLen} item${queueLen === 1 ? '' : 's'} pending sync"`
   (Previously it said "change" — "item" is more accurate for mixed queue types.)

No other changes to OfflineBanner.tsx. The `OfflineBanner` component (offline/back-online banner) is unchanged.

Do NOT add a second polling interval. The single 10s interval covers both queues after this change.
  </action>
  <verify>
    <automated>cd /c/Users/glcar/prevailing-wage && npx tsc --noEmit 2>&1 | grep -i "OfflineBanner\|error TS" | head -10</automated>
  </verify>
  <done>OfflineBadge imports getPendingCount. Badge count is sum of generic + payroll queue lengths. Zero TS errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Full offline payroll entry queue flow (Phase 94):
    - payrollQueue.ts IDB CRUD layer (plan 94-01)
    - useOfflineEntryMutation hook with offline/online detection (plan 94-02)
    - Unified OfflineBanner badge count (this plan)
    - Reconnect flush via window 'online' event (existing OfflineBanner + useOfflineEntryMutation.flush)
  </what-built>
  <how-to-verify>
    1. Start dev server: `cd /c/Users/glcar/prevailing-wage && npm run dev`
    2. Open http://localhost:4200, log in, open a project with workers, navigate to New Payroll Week.
    3. Complete Step 1 (roster — select workers). This creates the week on the server while online.
    4. Open DevTools → Network tab → set throttle to "Offline".
    5. In Step 2 (hours grid), enter hours for one worker (e.g., 8 monSt).
    6. Verify: the save-status indicator shows "Queued for sync" (amber badge) instead of "Saving...".
    7. Check DevTools → Application → IndexedDB → payroll-queue → entries: one entry should appear with status='pending'.
    8. Check the nav header: OfflineBadge should show "1" (amber pill).
    9. Set network throttle back to "No throttle" (online).
    10. Verify: OfflineBanner shows "Back online — syncing..." green flash.
    11. Within 5 seconds, verify: the payroll-queue entries store is empty (entry was flushed).
    12. Navigate to the Payroll Week Detail page for this week: the entry should appear with correct hours.
    13. OfflineBadge should show nothing (count = 0).

    409 Conflict test (optional — requires a second browser/incognito):
    - Submit the same entry from a second browser tab before flushing from the offline tab.
    - When the offline tab comes back online, the entry with matching (weekId, workerId, classificationId) should get a 409.
    - Verify: the IDB entry status changes to 'synced-elsewhere', and the wizard shows "Synced by another device" (red badge).
  </how-to-verify>
  <resume-signal>Type "approved" if both online and offline paths work as described. Describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
cd /c/Users/glcar/prevailing-wage && npx tsc --noEmit 2>&1 | head -10
</verification>

<success_criteria>
- OfflineBanner badge shows sum of both queue lengths
- Zero TS errors across all modified files
- Human verifies: offline entry shows "Queued for sync", IDB entry visible in DevTools, flush on reconnect sends entry to server, week detail page shows the entry after flush
- Human verifies: 409 conflict path sets status='synced-elsewhere' and shows "Synced by another device" badge
</success_criteria>

<output>
After completion, create `.planning/phases/94-offline-payroll-entry-queue/94-03-SUMMARY.md`
</output>
