---
phase: 41-ny-state-forms
plan: 01
slug: migration-payroll-query-patch
type: execute
wave: 1
status: draft
goal: Add nyMpwrSubmittedAt column to payrollWeeks and patch getPayrollEntriesWithWorkerDetails to return nysRegisteredApprentice
requirements: [STATE-03, STATE-05, NFR-01, NFR-05]
files_modified:
  - src/server/db/migrations/0024_ny_mpwr_submission.sql
  - src/server/db/migrations/meta/_journal.json
  - src/server/db/schema.ts
  - src/server/services/payrollService.ts
autonomous: true
depends_on: []

must_haves:
  truths:
    - "payrollWeeks table has ny_mpwr_submitted_at column available at runtime"
    - "getPayrollEntriesWithWorkerDetails returns nysRegisteredApprentice for each entry"
    - "getPayrollEntriesWithWorkerDetails returns nyMpwrSubmittedAt for payroll week queries"
  artifacts:
    - path: "src/server/db/migrations/0024_ny_mpwr_submission.sql"
      provides: "ALTER TABLE adding ny_mpwr_submitted_at column"
      contains: "ALTER TABLE payroll_weeks ADD COLUMN ny_mpwr_submitted_at TEXT"
    - path: "src/server/db/schema.ts"
      provides: "Drizzle schema definition for nyMpwrSubmittedAt"
      contains: "nyMpwrSubmittedAt"
    - path: "src/server/services/payrollService.ts"
      provides: "nysRegisteredApprentice in select of getPayrollEntriesWithWorkerDetails"
      contains: "nysRegisteredApprentice"
  key_links:
    - from: "src/server/db/schema.ts"
      to: "src/server/db/migrations/0024_ny_mpwr_submission.sql"
      via: "schema column matches migration column name"
      pattern: "nyMpwrSubmittedAt.*ny_mpwr_submitted_at"
    - from: "src/server/services/payrollService.ts"
      to: "src/server/db/schema.ts"
      via: "workers.nysRegisteredApprentice referenced in select"
      pattern: "workers\\.nysRegisteredApprentice"
---

<objective>
Add the missing nyMpwrSubmittedAt column to the payrollWeeks table via migration 0024 and patch getPayrollEntriesWithWorkerDetails to include nysRegisteredApprentice from workers table.

Purpose: Plans 02-05 depend on this column existing in schema and the nysRegisteredApprentice field being available in payroll entry queries. Without this, the MPWR XML generator cannot read the apprentice flag, and the NY submit route has no column to write to.

Output: Migration file, updated schema, patched payroll service query.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-ny-state-forms/41-RESEARCH.md

@src/server/db/schema.ts
@src/server/db/migrations/meta/_journal.json
@src/server/services/payrollService.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 0024 and update Drizzle schema</name>
  <files>src/server/db/migrations/0024_ny_mpwr_submission.sql, src/server/db/migrations/meta/_journal.json, src/server/db/schema.ts</files>
  <read_first>
    - src/server/db/migrations/meta/_journal.json (understand current idx and entry format)
    - src/server/db/schema.ts (find payrollWeeks table definition, confirm caEcprSubmittedAt/waLniSubmittedAt pattern)
    - src/server/db/migrations/0023_ny_schema.sql (understand previous migration format)
  </read_first>
  <action>
1. Create `src/server/db/migrations/0024_ny_mpwr_submission.sql` with content:
   ```sql
   ALTER TABLE payroll_weeks ADD COLUMN ny_mpwr_submitted_at TEXT;
   ```
   Single statement — no `--> statement-breakpoint` needed for single-statement migrations. But if CLAUDE.md or existing pattern requires the breakpoint even for single statements, include it: `ALTER TABLE payroll_weeks ADD COLUMN ny_mpwr_submitted_at TEXT;--> statement-breakpoint` — check 0023 migration format and match exactly.

2. Update `src/server/db/migrations/meta/_journal.json`: add entry at idx 20 with tag `0024_ny_mpwr_submission`, breakpoints: true. Follow the exact JSON structure of existing entries (idx, when, tag, breakpoints fields).

3. Update `src/server/db/schema.ts`: in the `payrollWeeks` table definition, add `nyMpwrSubmittedAt: text('ny_mpwr_submitted_at')` — place it adjacent to `caEcprSubmittedAt` and `waLniSubmittedAt` for consistency.
  </action>
  <verify>
    <automated>cd C:/Users/glcar/prevailing-wage && npx tsc --noEmit</automated>
  </verify>
  <done>Migration file exists at 0024, registered in _journal.json at idx 20, schema.ts has nyMpwrSubmittedAt on payrollWeeks, TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Patch getPayrollEntriesWithWorkerDetails to include nysRegisteredApprentice</name>
  <files>src/server/services/payrollService.ts</files>
  <read_first>
    - src/server/services/payrollService.ts (read the full getPayrollEntriesWithWorkerDetails function — find the select object and understand all current fields; find where workers table is joined)
    - src/server/db/schema.ts (confirm workers.nysRegisteredApprentice column definition)
  </read_first>
  <action>
1. In `getPayrollEntriesWithWorkerDetails`, locate the select object. Add `nysRegisteredApprentice: workers.nysRegisteredApprentice` to the select — this is an additive change. The workers table is already joined in this query so no new JOIN is needed.

2. Also ensure `nyMpwrSubmittedAt: payrollWeeks.nyMpwrSubmittedAt` is accessible where payroll week data is queried. Check if this function returns week-level data or if there is a separate `getPayrollWeek` function. If `getPayrollWeek` already returns all columns via select *, no change needed there. If it uses explicit select, add `nyMpwrSubmittedAt`.

3. Do NOT modify any existing field mappings — this is purely additive.
  </action>
  <verify>
    <automated>cd C:/Users/glcar/prevailing-wage && npx tsc --noEmit</automated>
  </verify>
  <done>getPayrollEntriesWithWorkerDetails returns nysRegisteredApprentice for each entry row. TypeScript compiles clean. No existing tests broken.</done>
</task>

</tasks>

<verification>
npx tsc --noEmit
grep "nysRegisteredApprentice" src/server/services/payrollService.ts
grep "nyMpwrSubmittedAt" src/server/db/schema.ts
</verification>

<success_criteria>
- Migration 0024_ny_mpwr_submission.sql exists with ALTER TABLE ADD COLUMN
- _journal.json has entry at idx 20
- schema.ts payrollWeeks includes nyMpwrSubmittedAt
- payrollService.ts getPayrollEntriesWithWorkerDetails select includes nysRegisteredApprentice
- TypeScript compiles clean with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/41-ny-state-forms/41-01-SUMMARY.md`
</output>
