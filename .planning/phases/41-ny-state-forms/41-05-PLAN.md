---
phase: 41-ny-state-forms
plan: 05
slug: ny-mpwr-modal-ui
type: execute
wave: 4
status: draft
goal: Add 3-step NY MPWR submission modal to PayrollWeekDetailPage with download buttons and submit tracking
requirements: [STATE-05]
files_modified:
  - src/client/pages/PayrollWeekDetailPage.tsx
autonomous: false
depends_on: [41-04]

must_haves:
  truths:
    - "NY MPWR button only visible when project.state is NY"
    - "Step 1 collects and persists PRC Number and NYS Contractor Registration Number"
    - "Step 2 provides download buttons for PW-12 PDF and MPWR XML"
    - "Step 3 shows MPWR portal upload checklist with 30-day deadline reminder"
    - "Mark as Submitted button calls PATCH /api/payroll/weeks/:id/ny-submit and shows success badge"
    - "Modal resets to Step 1 on close (no stale state)"
  artifacts:
    - path: "src/client/pages/PayrollWeekDetailPage.tsx"
      provides: "NY MPWR 3-step submission modal"
      contains: "showNyMpwrModal"
  key_links:
    - from: "src/client/pages/PayrollWeekDetailPage.tsx"
      to: "/api/export/pw12/:weekId"
      via: "fetch in download handler"
      pattern: "api/export/pw12"
    - from: "src/client/pages/PayrollWeekDetailPage.tsx"
      to: "/api/export/ny-mpwr-xml/:weekId"
      via: "fetch in download handler"
      pattern: "api/export/ny-mpwr-xml"
    - from: "src/client/pages/PayrollWeekDetailPage.tsx"
      to: "/api/payroll/weeks/:id/ny-submit"
      via: "fetch PATCH on Mark as Submitted"
      pattern: "ny-submit"
---

<objective>
Add the NY MPWR 3-step submission modal to PayrollWeekDetailPage, gated to NY projects only, with download buttons for PW-12 PDF and MPWR XML, and a submit-to-portal checklist.

Purpose: This is the user-facing entry point for the entire NY submission workflow. Contractors open the modal, confirm their registration numbers, download both files, then mark the submission as complete.

Output: Updated PayrollWeekDetailPage.tsx with fully functional NY MPWR modal.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-ny-state-forms/41-RESEARCH.md
@.planning/phases/41-ny-state-forms/41-04-SUMMARY.md

@src/client/pages/PayrollWeekDetailPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add NY MPWR 3-step submission modal to PayrollWeekDetailPage</name>
  <files>src/client/pages/PayrollWeekDetailPage.tsx</files>
  <read_first>
    - src/client/pages/PayrollWeekDetailPage.tsx (FULL FILE — understand: isCA/isWA gating pattern, showWaCprModal/waCprStep state machine pattern, WA CPR modal JSX structure, download button fetch+blob+anchor click pattern, setCaEcprSubmitted/setWaLniSubmitted PATCH call pattern, Badge variant="success" for submitted state, modal close/reset handler pattern, project data access shape from TanStack Query)
  </read_first>
  <action>
Add the NY MPWR submission modal following the exact WA CPR modal pattern:

**State variables (add alongside existing WA/CA state):**
```typescript
const [showNyMpwrModal, setShowNyMpwrModal] = useState(false);
const [nyMpwrStep, setNyMpwrStep] = useState<1 | 2 | 3>(1);
const [nyPrcNumber, setNyPrcNumber] = useState('');
const [nysContractorRegNumber, setNysContractorRegNumber] = useState('');
```

**isNY constant (add alongside isCA/isWA):**
```typescript
const isNY = projectData?.data?.project?.state?.toUpperCase() === 'NY';
```

Initialize nyPrcNumber and nysContractorRegNumber from project data when modal opens (or via useEffect when project data loads):
```typescript
// When project data loads, seed the fields
useEffect(() => {
  if (projectData?.data?.project) {
    setNyPrcNumber(projectData.data.project.nyprcNumber || '');
    setNysContractorRegNumber(projectData.data.project.nysContractorRegNumber || '');
  }
}, [projectData?.data?.project]);
```

**NY MPWR button in action bar (alongside CA/WA buttons):**
- Only visible when `isNY && weekId`
- onClick: `setNyMpwrStep(1); setShowNyMpwrModal(true);`
- Label: "NY MPWR Submission"

**Modal (follows WA CPR modal JSX pattern):**

**Step 1 — Collect Fields:**
- Title: "Step 1: Confirm NY Registration Details"
- Text input for PRC Number (value: nyPrcNumber, onChange: setNyPrcNumber)
  - Hint text: "Project Registration Certificate number from NYSDOL"
- Text input for NYS Contractor Registration Number (value: nysContractorRegNumber, onChange: setNysContractorRegNumber)
  - Hint text: "Your NYS contractor registration number"
- "Save & Continue" button:
  - On click: PATCH project to persist nyprcNumber and nysContractorRegNumber (use existing project update endpoint)
  - Then: `setNyMpwrStep(2)`
- "Cancel" button: close modal + reset step

**Step 2 — Download Files:**
- Title: "Step 2: Download Submission Files"
- "Download PW-12 PDF" button:
  - Use the existing file-download helper pattern (fetch + blob + URL.createObjectURL + anchor click) — same as CA/WA download buttons on the page
  - URL: `/api/export/pw12/${weekId}`
- "Download MPWR XML" button:
  - Same download pattern
  - URL: `/api/export/ny-mpwr-xml/${weekId}`
- "Continue to Checklist" button: `setNyMpwrStep(3)`

**Step 3 — Checklist + Submit:**
- Title: "Step 3: Submit to NY MPWR Portal"
- Bullet list (checklist style):
  - "Upload the MPWR XML file to the NYSDOL MPWR portal (https://dol.ny.gov)"
  - "Submissions must be made within 30 days of the payroll week ending date"
  - "Keep the PW-12 PDF for your offline records"
  - "Verify work category names match the MPWR portal dropdown values"
- "Mark as Submitted to NY MPWR" button:
  - On click: `fetch(`/api/payroll/weeks/${weekId}/ny-submit`, { method: 'PATCH' })`
  - On success: close modal, show `Badge variant="success"` with submitted date (same pattern as CA/WA submission badges)
  - Invalidate TanStack Query to refresh week data showing nyMpwrSubmittedAt

**If already submitted (nyMpwrSubmittedAt is set on week data):**
- Show `Badge variant="success"` with date next to the NY MPWR button (same as CA/WA submitted badge pattern)

**Modal close handler (per research Pitfall 5 — prevent stale state):**
```typescript
const closeNyModal = () => {
  setShowNyMpwrModal(false);
  setNyMpwrStep(1);
};
```

**Design tokens only:** Use `bg-brand-gold`, `border-brand-gold`, `bg-surface-card`, etc. from existing design system. Never use hex colors directly.
  </action>
  <verify>
    <automated>cd C:/Users/glcar/prevailing-wage && npx tsc --noEmit</automated>
  </verify>
  <done>NY MPWR 3-step modal added to PayrollWeekDetailPage. Button only visible for NY projects. Step 1 collects PRC + reg numbers. Step 2 downloads PW-12 PDF and MPWR XML. Step 3 shows checklist and Mark as Submitted button. Modal resets on close. Uses design tokens only. TypeScript compiles clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify NY MPWR modal end-to-end</name>
  <files>src/client/pages/PayrollWeekDetailPage.tsx</files>
  <action>
Human verification of the NY MPWR submission flow. No code changes — visual and functional confirmation of the modal built in Task 1.

What was built: Complete NY MPWR submission flow with 3-step modal (field collection, file downloads, submit tracking), all wired to backend routes from Plan 04.

How to verify:
1. Navigate to PayrollWeekDetailPage for an NY project with payroll entries
2. Confirm "NY MPWR Submission" button is visible
3. Click it — Step 1 should show PRC Number and Contractor Reg Number fields (pre-filled if previously saved)
4. Enter/confirm values, click "Save and Continue"
5. Step 2: Click "Download PW-12 PDF" — confirm PDF downloads and opens correctly with contractor header and worker rows
6. Step 2: Click "Download MPWR XML" — confirm XML downloads with ProjectRollup root element
7. Click "Continue to Checklist"
8. Step 3: Confirm checklist items visible including 30-day deadline
9. Click "Mark as Submitted to NY MPWR" — confirm success badge appears with date
10. Close and reopen modal — confirm it resets to Step 1
11. Navigate to a CA project — confirm NO NY MPWR button visible

Resume signal: Type "approved" or describe issues to fix.
  </action>
  <verify>
    <automated>cd C:/Users/glcar/prevailing-wage && npx tsc --noEmit</automated>
  </verify>
  <done>User has confirmed the NY MPWR modal works end-to-end: 3 steps complete, files download, submit tracking works, state gating correct, modal resets on close.</done>
</task>

</tasks>

<verification>
npx tsc --noEmit
Manual: Open NY project PayrollWeekDetailPage, complete all 3 modal steps
</verification>

<success_criteria>
- NY MPWR button only visible on NY projects
- Step 1 persists PRC Number and Contractor Reg to project
- Step 2 downloads PW-12 PDF and MPWR XML successfully
- Step 3 shows checklist with 30-day deadline
- Mark as Submitted writes agency_submissions and shows success badge
- Modal resets on close
- No hex colors — design tokens only
- TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/41-ny-state-forms/41-05-SUMMARY.md`
</output>
