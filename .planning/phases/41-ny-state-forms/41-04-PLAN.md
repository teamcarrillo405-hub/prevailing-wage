---
phase: 41-ny-state-forms
plan: 04
slug: export-routes-and-tests
type: execute
wave: 4
status: draft
goal: Wire PW-12 and MPWR XML generators into export routes, add NY submit route, and write integration tests
requirements: [STATE-02, STATE-03, STATE-05, NFR-03]
files_modified:
  - src/server/routes/export.ts
  - src/server/routes/payroll.ts
  - src/server/services/payrollService.ts
  - tests/routes/export.test.ts
  - tests/routes/payroll.test.ts
autonomous: true
depends_on: [41-02, 41-03]

must_haves:
  truths:
    - "GET /api/export/pw12/:weekId returns a PDF for NY projects"
    - "GET /api/export/pw12/:weekId returns 400 for non-NY projects"
    - "GET /api/export/ny-mpwr-xml/:weekId returns XML for NY projects"
    - "GET /api/export/ny-mpwr-xml/:weekId returns 400 for non-NY projects"
    - "PATCH /api/payroll/weeks/:id/ny-submit sets nyMpwrSubmittedAt and writes agency_submissions row"
    - "All new routes call assertProjectAccess before data access (NFR-03)"
    - "Both export routes return 403 without project access"
  artifacts:
    - path: "src/server/routes/export.ts"
      provides: "PW-12 and MPWR XML export endpoints"
      contains: "pw12"
    - path: "src/server/routes/payroll.ts"
      provides: "NY submit PATCH endpoint"
      contains: "ny-submit"
    - path: "src/server/services/payrollService.ts"
      provides: "setNyMpwrSubmitted service function"
      contains: "setNyMpwrSubmitted"
    - path: "tests/routes/export.test.ts"
      provides: "Integration tests for NY export routes"
      contains: "pw12"
    - path: "tests/routes/payroll.test.ts"
      provides: "Integration test for NY submit route"
      contains: "ny-submit"
  key_links:
    - from: "src/server/routes/export.ts"
      to: "src/server/services/mpwrXmlGenerator.ts"
      via: "import generateMpwrXml"
      pattern: "generateMpwrXml"
    - from: "src/server/routes/export.ts"
      to: "src/server/services/pw12Generator.ts"
      via: "import fillPw12"
      pattern: "fillPw12"
    - from: "src/server/routes/payroll.ts"
      to: "src/server/services/payrollService.ts"
      via: "import setNyMpwrSubmitted"
      pattern: "setNyMpwrSubmitted"
    - from: "src/server/routes/export.ts"
      to: "assertProjectAccess"
      via: "authorization guard before data access"
      pattern: "assertProjectAccess"
---

<objective>
Wire the MPWR XML generator and PW-12 PDF generator into Express export routes, add the NY MPWR submit route to payroll.ts, and write integration tests covering state-gating, authorization, and the submit flow.

Purpose: These routes are the API surface that the UI modal (Plan 05) will call to download files and mark submissions.

Output: Two new export routes, one new submit route, integration tests for all three.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-ny-state-forms/41-RESEARCH.md
@.planning/phases/41-ny-state-forms/41-02-SUMMARY.md
@.planning/phases/41-ny-state-forms/41-03-SUMMARY.md

@src/server/routes/export.ts
@src/server/routes/payroll.ts
@src/server/services/payrollService.ts
@tests/routes/export.test.ts
@tests/routes/payroll.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add PW-12 and MPWR XML export routes</name>
  <files>src/server/routes/export.ts</files>
  <read_first>
    - src/server/routes/export.ts (FULL FILE — understand the existing A-1-131, ecpr-xml, wa-cpr-xml route patterns, assertProjectAccess usage, state gating pattern, audit log pattern, getPayrollEntriesWithWorkerDetails usage, import statements)
    - src/server/services/mpwrXmlGenerator.ts (confirm export name and input type — from Plan 02)
    - src/server/services/pw12Generator.ts (confirm export name and input type — from Plan 03)
    - src/server/index.ts (confirm export router registration — verify routes are on the export router already)
  </read_first>
  <action>
Add two new routes to `src/server/routes/export.ts`:

**Route 1: GET /api/export/pw12/:weekId**
1. Extract weekId from params, userId from req.user
2. Load payroll week via getPayrollWeek(weekId) — 404 if not found
3. Call assertProjectAccess(db, week.projectId, userId) — catches to 403 (NFR-03)
4. State gate: `if (project.state?.toUpperCase() !== 'NY')` return 400 with error "PW-12 is only available for New York projects"
5. Load entries via getPayrollEntriesWithWorkerDetails(weekId)
6. Map data to Pw12Input format: contractor from project fields, week from payroll week, entries from query results
7. Call `fillPw12(input)` — await the Promise
8. Set headers: `Content-Type: application/pdf`, `Content-Disposition: attachment; filename="pw12-${weekId}.pdf"`
9. Send `Buffer.from(filledPdf)` via `res.end()`
10. Best-effort audit log: dynamic import auditService, insertAuditLog with action `ny_pw12.downloaded`

**Route 2: GET /api/export/ny-mpwr-xml/:weekId**
1. Same auth + state gate pattern as PW-12 route
2. Load entries via getPayrollEntriesWithWorkerDetails(weekId)
3. Map data to MpwrXmlInput format: project fields (nyprcNumber, nysContractorRegNumber, name, contractorFein), week fields, entries with all required fields including nysRegisteredApprentice, daily hours, fringe fields
4. Call `generateMpwrXml(input)` — synchronous, returns string
5. Set headers: `Content-Type: application/xml`, `Content-Disposition: attachment; filename="mpwr-${weekId}.xml"`
6. Send XML string via `res.send(xml)`
7. Best-effort audit log with action `ny_mpwr_xml.downloaded`

Both routes follow the exact pattern of existing ecpr-xml and wa-cpr-xml routes. Import generateMpwrXml and fillPw12 at the top of the file.
  </action>
  <verify>
    <automated>cd C:/Users/glcar/prevailing-wage && npx tsc --noEmit</automated>
  </verify>
  <done>Two new export routes added: GET /pw12/:weekId and GET /ny-mpwr-xml/:weekId. Both use assertProjectAccess, state-gate to NY, and write audit logs. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add NY MPWR submit route to payroll.ts</name>
  <files>src/server/routes/payroll.ts, src/server/services/payrollService.ts</files>
  <read_first>
    - src/server/routes/payroll.ts (find the setCaEcprSubmitted / setWaLniSubmitted PATCH routes — understand exact pattern for request handling, assertProjectAccess, agency_submissions insert, audit log)
    - src/server/services/payrollService.ts (find setCaEcprSubmitted function — understand the exact update pattern with payrollWeeks.set and eq)
  </read_first>
  <action>
**In payrollService.ts:**
1. Add `setNyMpwrSubmitted(weekId: string): Promise<{ nyMpwrSubmittedAt: string }>` — follows setCaEcprSubmitted exactly:
   - `const now = new Date().toISOString()`
   - `db.update(payrollWeeks).set({ nyMpwrSubmittedAt: now, updatedAt: now }).where(eq(payrollWeeks.id, weekId))`
   - Return `{ nyMpwrSubmittedAt: now }`

**In payroll.ts:**
1. Add `PATCH /api/payroll/weeks/:id/ny-submit` — follows the ca-ecpr-submit / wa-lni-submit pattern exactly:
   - Extract id from params, userId from req.user
   - Load week via getPayrollWeek(id) — 404 if not found
   - assertProjectAccess(db, week.projectId, userId) — NFR-03
   - Call `setNyMpwrSubmitted(id)`
   - Write `agency_submissions` row: insert into agencySubmissions with `agency: 'NY_MPWR'`, weekId, projectId, submittedAt, submittedBy
   - Best-effort audit log: action `agency_submission.created`, meta `{ agency: 'NY_MPWR' }`
   - Return 200 with `{ nyMpwrSubmittedAt }` in response body
  </action>
  <verify>
    <automated>cd C:/Users/glcar/prevailing-wage && npx tsc --noEmit</automated>
  </verify>
  <done>PATCH /api/payroll/weeks/:id/ny-submit route added following CA/WA pattern. setNyMpwrSubmitted service function created. Writes agency_submissions row and audit log. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 3: Write integration tests for NY export and submit routes</name>
  <files>tests/routes/export.test.ts, tests/routes/payroll.test.ts</files>
  <read_first>
    - tests/routes/export.test.ts (understand existing test setup — how projects are seeded, how auth is mocked, how state-gating is tested for CA/WA routes)
    - tests/routes/payroll.test.ts (understand existing test setup — how PATCH submit routes are tested for CA/WA)
  </read_first>
  <action>
**In tests/routes/export.test.ts — add test cases:**

1. `it('GET /api/export/pw12/:weekId returns 400 for non-NY project')` — create/use a CA project, request pw12 route, expect 400 with error message containing "New York"

2. `it('GET /api/export/ny-mpwr-xml/:weekId returns 400 for non-NY project')` — same as above for MPWR XML route

3. `it('GET /api/export/pw12/:weekId returns 403 without project access')` — request as wrong user, expect 401 or 403 (match existing pattern for assertProjectAccess failures — NFR-03)

4. `it('GET /api/export/ny-mpwr-xml/:weekId returns 403 without project access')` — same for XML route

Follow the exact test setup patterns from existing A-1-131 and ecpr-xml test cases in the same file. Use the same project/week fixtures, just with state='NY' for positive cases and state='CA' for negative state-gate cases.

**In tests/routes/payroll.test.ts — add test case:**

5. `it('PATCH /api/payroll/weeks/:id/ny-submit sets nyMpwrSubmittedAt')` — create an NY project + week, call PATCH, assert 200 response with nyMpwrSubmittedAt field, then verify the week's nyMpwrSubmittedAt is set in DB (or response). Follow the ca-ecpr-submit / wa-lni-submit test pattern.
  </action>
  <verify>
    <automated>cd C:/Users/glcar/prevailing-wage && npx vitest run tests/routes/export.test.ts tests/routes/payroll.test.ts</automated>
  </verify>
  <done>5 new integration tests added and passing: pw12 400 for non-NY, mpwr-xml 400 for non-NY, pw12 403 without access, mpwr-xml 403 without access, ny-submit sets nyMpwrSubmittedAt.</done>
</task>

</tasks>

<verification>
npx vitest run tests/routes/export.test.ts tests/routes/payroll.test.ts
npx tsc --noEmit
</verification>

<success_criteria>
- GET /api/export/pw12/:weekId serves PDF for NY projects, 400 for non-NY
- GET /api/export/ny-mpwr-xml/:weekId serves XML for NY projects, 400 for non-NY
- PATCH /api/payroll/weeks/:id/ny-submit sets nyMpwrSubmittedAt and writes agency_submissions
- All routes call assertProjectAccess (NFR-03)
- All new integration tests pass
- TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/41-ny-state-forms/41-04-SUMMARY.md`
</output>
