---
phase: 12-app-shell-global-layout
plan: 03
type: execute
wave: 2
depends_on:
  - 12-02
files_modified:
  - src/client/pages/ProjectDetailPage.tsx
  - src/client/pages/PayrollWeekDetailPage.tsx
  - src/client/pages/WorkersPage.tsx
  - src/client/pages/PayrollListPage.tsx
  - src/client/components/shared/ProjectCard.tsx
autonomous: false
requirements:
  - SHELL-03

must_haves:
  truths:
    - "ProjectDetailPage project info section renders inside the Card primitive"
    - "PayrollWeekDetailPage week summary and entry table sections render inside Card primitives"
    - "WorkersPage individual worker blocks render inside Card primitives"
    - "PayrollListPage payroll weeks list container renders inside a Card primitive"
    - "ProjectCard hover border uses brand-gold token (not hardcoded #F5C518)"
    - "All card containers use consistent border radius and padding"
  artifacts:
    - path: "src/client/pages/ProjectDetailPage.tsx"
      provides: "Project detail info card using Card primitive"
      contains: "<Card"
    - path: "src/client/pages/PayrollWeekDetailPage.tsx"
      provides: "Week summary and entries table wrapped in Card primitives"
      contains: "<Card"
    - path: "src/client/pages/WorkersPage.tsx"
      provides: "Worker blocks using Card primitive"
      contains: "<Card"
    - path: "src/client/pages/PayrollListPage.tsx"
      provides: "Payroll weeks container using Card primitive"
      contains: "<Card"
    - path: "src/client/components/shared/ProjectCard.tsx"
      provides: "ProjectCard hover border using brand-gold token"
      contains: "hover:border-brand-gold"
  key_links:
    - from: "src/client/pages/PayrollWeekDetailPage.tsx"
      to: "src/client/components/ui/Card.tsx"
      via: "import Card"
      pattern: "Card padding none"
    - from: "src/client/pages/WorkersPage.tsx"
      to: "src/client/components/ui/Card.tsx"
      via: "import Card"
      pattern: "Card padding sm"
---

<objective>
Adopt the Card primitive across 4 protected pages and fix the ProjectCard hover token, completing SHELL-03.

Purpose: 8+ raw inline card patterns (bg-white border border-gray-200 rounded-lg p-6) exist across 5 files. Replacing them with Card enforces uniform spacing and border-radius tokens across all card-based sections, ensuring no page has divergent card styling.

Output: All inline card divs in scope replaced with Card primitives using appropriate padding variants. ProjectCard hover border uses brand-gold token. 181-test suite remains green. Human visual verification confirms consistent card appearance across pages.
</objective>

<execution_context>
@C:/Users/glcar/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/glcar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-app-shell-global-layout/12-RESEARCH.md

<interfaces>
<!-- Card component (created in Phase 11-01) -->
<!-- Import: import { Card } from '../components/ui/Card' -->

```typescript
interface CardProps {
  padding?: 'default' | 'sm' | 'none';
  // default = p-6
  // sm      = p-4
  // none    = no padding (use for table wrappers where cells control their own padding)
  className?: string;  // merged via cn() -- safe to add divide-y, mb-*, max-w-*, etc.
  children: React.ReactNode;
}
```

Target replacements (from research):

ProjectDetailPage.tsx:
  BEFORE: div className="bg-white border border-gray-200 rounded-lg p-6 max-w-lg"
  AFTER:  Card className="max-w-lg"

PayrollWeekDetailPage.tsx:
  BEFORE: div className="bg-white rounded-lg border border-gray-200 mb-6"  (week summary)
  AFTER:  Card padding="none" className="mb-6"

  BEFORE: div className="bg-white rounded-lg border border-gray-200 mb-6 py-12 text-center"  (empty state)
  AFTER:  Card padding="none" className="mb-6 py-12 text-center"

  BEFORE: div className="bg-white rounded-lg border border-gray-200"  (entries table)
  AFTER:  Card padding="none"

WorkersPage.tsx:
  BEFORE: div className="bg-white rounded-lg border border-gray-200 p-5"  (worker cards)
  AFTER:  Card padding="sm"

  BEFORE: div className="bg-white rounded-lg border border-gray-200 p-6"  (add worker form)
  AFTER:  Card

PayrollListPage.tsx:
  BEFORE: div className="bg-white rounded-lg border border-gray-200 divide-y divide-gray-100"
  AFTER:  Card padding="none" className="divide-y divide-gray-100"

ProjectCard.tsx (button element -- NOT wrapped in Card):
  BEFORE: hover:border-[#F5C518]
  AFTER:  hover:border-brand-gold
  Note: Only this hover border class changes. Full Card adoption is Phase 14 (PAGE-01).

EXCLUSIONS -- do NOT touch:
- DashboardPage modal (bg-white rounded-lg shadow-xl) -- modals need shadow-xl, not shadow-card
- LoginPage/RegisterPage -- public pages not in Layout, Phase 14 (PAGE-07) scope
- Any element not matching the bg-white border border-gray-200 rounded-lg card pattern
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adopt Card in ProjectDetailPage, PayrollWeekDetailPage, and ProjectCard</name>
  <files>src/client/pages/ProjectDetailPage.tsx, src/client/pages/PayrollWeekDetailPage.tsx, src/client/components/shared/ProjectCard.tsx</files>
  <action>
Read each file in full before editing.

ProjectDetailPage.tsx:
- Add import: import { Card } from '../components/ui/Card';
- Find the div with className="bg-white border border-gray-200 rounded-lg p-6 max-w-lg" (project info dl section) and replace with Card className="max-w-lg". Ensure the closing div becomes /Card.
- Do NOT remove the PageHeader added in Plan 02.

PayrollWeekDetailPage.tsx:
- Add import: import { Card } from '../components/ui/Card';
- Replace the 3 inline card divs:
  1. Week summary section div with mb-6 class: replace with Card padding="none" className="mb-6"
  2. Empty state container div with mb-6 py-12 text-center: replace with Card padding="none" className="mb-6 py-12 text-center"
  3. Entries table wrapper div (no extra classes): replace with Card padding="none"
- Use padding="none" for all three because they contain tables or have their own internal padding.

ProjectCard.tsx:
- Find hover:border-[#F5C518] and replace with hover:border-brand-gold.
- This is a button element -- do NOT add a Card wrapper (full restructure is Phase 14).
- Make ONLY this class change.

After editing all three files, run npm run test -- --run to confirm all 181 tests still pass.
  </action>
  <verify>
    <automated>npm run test -- --run</automated>
  </verify>
  <done>ProjectDetailPage project info section wrapped in Card. PayrollWeekDetailPage has 3 Card wrappers for week summary, empty state, and entries table. ProjectCard hover border uses hover:border-brand-gold. All 181 tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Adopt Card in WorkersPage and PayrollListPage</name>
  <files>src/client/pages/WorkersPage.tsx, src/client/pages/PayrollListPage.tsx</files>
  <action>
Read each file in full before editing.

WorkersPage.tsx:
- Add import: import { Card } from '../components/ui/Card';
- Find all divs with className="bg-white rounded-lg border border-gray-200 p-5" (individual worker cards, likely in a .map()) and replace each with Card padding="sm". Note: padding="sm" is p-4 which approximates p-5.
- Find div with className="bg-white rounded-lg border border-gray-200 p-6" (add worker form container) and replace with Card.
- Ensure all matching closing div tags become /Card.

PayrollListPage.tsx:
- Check existing imports to determine the correct relative path to components/ui/Card, then add the import.
- Find div with className="bg-white rounded-lg border border-gray-200 divide-y divide-gray-100" and replace with Card padding="none" className="divide-y divide-gray-100".

After editing both files, run npm run test -- --run to confirm all 181 tests still pass.
  </action>
  <verify>
    <automated>npm run test -- --run</automated>
  </verify>
  <done>WorkersPage worker blocks and add-worker form wrapped in Card primitives. PayrollListPage payroll weeks list wrapped in Card with divide-y utility. All 181 tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of Card adoption and phase sign-off</name>
  <action>Human visual verification of Card adoption across all pages -- see how-to-verify steps.</action>
  <verify>Human confirms consistent card styling on all pages per the how-to-verify checklist.</verify>
  <done>User types "approved" after confirming all card sections render consistently across pages.</done>
  <what-built>
    Card primitive adoption complete across 4 protected pages plus ProjectCard hover token fixed.
    - ProjectDetailPage: project info section uses Card
    - PayrollWeekDetailPage: week summary, empty state, entries table use Card
    - WorkersPage: worker blocks and add-worker form use Card
    - PayrollListPage: payroll weeks list uses Card with divide-y
    - ProjectCard: hover border uses brand-gold token
  </what-built>
  <how-to-verify>
    1. Start dev server: npm run dev from project root
    2. Log in and open Dashboard -- verify nav is dark (#1a1a1a) with gold bottom border (SHELL-01 from Plan 01)
    3. Verify "Projects" heading renders in Oswald as PageHeader (SHELL-02 from Plan 02)
    4. Open a project -- verify project info card has consistent border-radius and subtle shadow
    5. Navigate to Workers -- verify each worker block has consistent card styling (rounded corners, light border)
    6. Navigate to a payroll list -- verify the weeks container has card styling
    7. Open a payroll week detail -- verify week summary and entries table have consistent card borders
    8. Hover over a project card on Dashboard -- verify gold border appears on hover

    Expected: All card sections appear visually consistent -- same border radius, same border color (light gray), same padding proportions. No page has an ad-hoc div that looks different from the Card primitive.
  </how-to-verify>
  <resume-signal>Type "approved" to confirm visual verification passed, or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
After plan completes:
1. npm run test -- --run -- all 181 tests green
2. grep for "bg-white border border-gray-200 rounded-lg" in ProjectDetailPage.tsx, PayrollWeekDetailPage.tsx, WorkersPage.tsx, PayrollListPage.tsx -- returns empty (all patterns replaced)
3. grep for hover:border-[#F5C518] in ProjectCard.tsx -- returns empty
4. grep for hover:border-brand-gold in ProjectCard.tsx -- returns the token class
5. Human visual sign-off via checkpoint task
</verification>

<success_criteria>
All 8+ inline card patterns across 5 files replaced with Card primitive. Card adoption is complete for Phase 12 scope. Visual verification confirms consistent card styling across Dashboard, ProjectDetail, Workers, PayrollList, and PayrollWeekDetail pages.
</success_criteria>

<output>
After completion, create `.planning/phases/12-app-shell-global-layout/12-03-SUMMARY.md`
</output>
