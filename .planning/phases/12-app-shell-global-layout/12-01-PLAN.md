---
phase: 12-app-shell-global-layout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/client/components/shared/Layout.tsx
autonomous: true
requirements:
  - SHELL-01

must_haves:
  truths:
    - "Every protected page shows a dark (#1a1a1a) nav bar — no white or gray-900 nav visible"
    - "The nav bottom border displays gold (#F5C518) on every protected page"
    - "Hovering the 'HCC Prevailing Wage' nav link turns the text gold"
  artifacts:
    - path: "src/client/components/shared/Layout.tsx"
      provides: "Shared Layout with brand-token nav"
      contains: "bg-nav-dark border-brand-gold hover:text-brand-gold"
  key_links:
    - from: "src/client/components/shared/Layout.tsx"
      to: "src/client/index.css @theme"
      via: "Tailwind utility class resolution"
      pattern: "bg-nav-dark|border-brand-gold|hover:text-brand-gold"
---

<objective>
Migrate Layout.tsx from hardcoded hex values to design token utility classes.

Purpose: A single 3-class change in Layout.tsx propagates brand-correct dark nav (#1a1a1a) and gold accent (#F5C518) to all 8 protected pages simultaneously, satisfying SHELL-01 with no per-page edits.

Output: Layout.tsx using bg-nav-dark, border-brand-gold, hover:text-brand-gold — all tokens already in @theme from Phase 10.
</objective>

<execution_context>
@C:/Users/glcar/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/glcar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-app-shell-global-layout/12-RESEARCH.md

<interfaces>
<!-- Current Layout.tsx nav — read from codebase per research -->
<!-- Replace exactly these 3 class values: -->

Current nav (from research):
```tsx
<nav className="bg-gray-900 border-b-4 border-[#F5C518]">
  <Link className="font-headline text-xl text-white tracking-wide hover:text-[#F5C518] transition-colors">
```

Target nav (after changes):
```tsx
<nav className="bg-nav-dark border-b-4 border-brand-gold">
  <Link className="font-headline text-xl text-white tracking-wide hover:text-brand-gold transition-colors">
```

Tokens confirmed in src/client/index.css @theme (Phase 10):
- --color-nav-dark: #1a1a1a  →  bg-nav-dark
- --color-brand-gold: #F5C518  →  border-brand-gold, hover:text-brand-gold

Note: bg-gray-900 (#111827) vs bg-nav-dark (#1a1a1a) — nav-dark is brand-correct; slightly lighter but brand spec calls for #1a1a1a.
Note: Log Out button styling (text-gray-300 hover:text-white border-gray-600) does NOT change — it uses standard Tailwind grays, not brand tokens.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Layout.tsx nav to design tokens</name>
  <files>src/client/components/shared/Layout.tsx</files>
  <action>
Read src/client/components/shared/Layout.tsx in full.

Make exactly 3 class changes in the nav element:
1. `bg-gray-900` → `bg-nav-dark`
2. `border-[#F5C518]` → `border-brand-gold`
3. `hover:text-[#F5C518]` → `hover:text-brand-gold`

Do NOT change any other classes. The Log Out button (text-gray-300, hover:text-white, border-gray-600, hover:border-gray-400) is intentionally using standard Tailwind grays — leave it unchanged.

After editing, grep the file to confirm no remaining `[#F5C518]` or `bg-gray-900` instances remain.

Run `npm run test -- --run` from the project root to confirm all 181 tests still pass.
  </action>
  <verify>
    <automated>npm run test -- --run</automated>
  </verify>
  <done>Layout.tsx contains bg-nav-dark, border-brand-gold, hover:text-brand-gold — no hardcoded hex values remain in this file. All 181 tests pass.</done>
</task>

</tasks>

<verification>
After plan completes:
1. `npm run test -- --run` — all 181 tests green
2. `grep -n "F5C518\|gray-900" src/client/components/shared/Layout.tsx` — returns empty (no remaining hardcoded values)
3. `grep -n "nav-dark\|brand-gold" src/client/components/shared/Layout.tsx` — returns both token classes
</verification>

<success_criteria>
Layout.tsx uses design tokens for all brand values. Every protected page (Dashboard, ProjectDetail, Workers, PayrollEntry, PayrollList, PayrollWeekDetail, OtScenario, Reports) inherits dark nav and gold accent via the shared Layout without any per-page changes.
</success_criteria>

<output>
After completion, create `.planning/phases/12-app-shell-global-layout/12-01-SUMMARY.md`
</output>
