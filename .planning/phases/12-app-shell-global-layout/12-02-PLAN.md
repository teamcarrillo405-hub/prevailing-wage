---
phase: 12-app-shell-global-layout
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/client/pages/DashboardPage.tsx
  - src/client/pages/ProjectDetailPage.tsx
autonomous: true
requirements:
  - SHELL-02

must_haves:
  truths:
    - "DashboardPage shows 'Projects' as a page title rendered via PageHeader (h1, Oswald font)"
    - "ProjectDetailPage shows the project name as a page title rendered via PageHeader (h1, Oswald font)"
    - "DashboardPage 'New Project' button appears right-aligned in the PageHeader action slot"
  artifacts:
    - path: "src/client/pages/DashboardPage.tsx"
      provides: "Dashboard with PageHeader primitive for page title"
      contains: "PageHeader"
    - path: "src/client/pages/ProjectDetailPage.tsx"
      provides: "Project detail with PageHeader primitive for page title"
      contains: "PageHeader"
  key_links:
    - from: "src/client/pages/DashboardPage.tsx"
      to: "src/client/components/ui/PageHeader.tsx"
      via: "import { PageHeader }"
      pattern: "<PageHeader title=\"Projects\""
    - from: "src/client/pages/ProjectDetailPage.tsx"
      to: "src/client/components/ui/PageHeader.tsx"
      via: "import { PageHeader }"
      pattern: "<PageHeader title=\\{project\\.name\\}"
---

<objective>
Adopt the PageHeader primitive in DashboardPage and ProjectDetailPage to enforce the typography hierarchy pattern for SHELL-02.

Purpose: @layer base already sets Oswald on h1-h4 globally (Phase 10). The gap is that DashboardPage and ProjectDetailPage use raw h2 elements instead of the PageHeader primitive. Migrating to PageHeader upgrades the semantic level (h2 → h1) and enforces the design system pattern so future pages follow the established convention.

Output: DashboardPage and ProjectDetailPage import and use PageHeader for their top-level page title. The inline flex header pattern is removed from both pages.
</objective>

<execution_context>
@C:/Users/glcar/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/glcar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-app-shell-global-layout/12-RESEARCH.md

<interfaces>
<!-- PageHeader component (created in Phase 11-02) -->
<!-- Import: import { PageHeader } from '../components/ui/PageHeader' -->

```typescript
interface PageHeaderProps {
  title: string;              // Renders as h1 font-headline text-2xl text-text-primary
  subtitle?: string;          // Optional — p text-sm text-text-secondary mt-1
  action?: React.ReactNode;   // Optional — right-aligned, conditionally rendered
  className?: string;         // Optional — merged via cn()
}
// Container: flex items-center justify-between mb-6
```

<!-- Button component (created in Phase 11-01) -->
<!-- Import: import { Button } from '../components/ui/Button' -->
<!-- Default variant is primary (gold fill, text-gray-900) -->

<!-- DashboardPage current inline pattern (from research): -->
```tsx
// BEFORE (lines ~32-39 in DashboardPage.tsx):
<div className="flex items-center justify-between mb-8">
  <h2 className="font-headline text-2xl text-gray-900">Projects</h2>
  <button onClick={() => setShowForm(true)} className="bg-[#F5C518] text-gray-900 ...">
    New Project
  </button>
</div>

// AFTER:
<PageHeader
  title="Projects"
  action={
    <Button onClick={() => setShowForm(true)}>
      New Project
    </Button>
  }
/>
```

<!-- ProjectDetailPage current inline pattern (from research): -->
```tsx
// BEFORE (line ~60):
<h2 className="font-headline text-3xl text-gray-900 mb-2">{project.name}</h2>

// AFTER:
<PageHeader title={project.name} />
```

<!-- Note: mb-6 is the PageHeader spec value (not mb-8 from existing inline). This is correct — design system standard. -->
<!-- Note: ProjectDetailPage has no h1 elsewhere → safe to upgrade h2 → h1 via PageHeader. -->
<!-- Note: DashboardPage modal h3 (New Project modal title) is NOT PageHeader — leave it as-is. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adopt PageHeader in DashboardPage</name>
  <files>src/client/pages/DashboardPage.tsx</files>
  <action>
Read src/client/pages/DashboardPage.tsx in full.

Add imports at the top of the file:
```tsx
import { PageHeader } from '../components/ui/PageHeader';
import { Button } from '../components/ui/Button';
```

Find the inline flex header div containing the "Projects" h2 and "New Project" button (pattern: `flex items-center justify-between mb-8`). Replace the entire div with:
```tsx
<PageHeader
  title="Projects"
  action={
    <Button onClick={() => setShowForm(true)}>
      New Project
    </Button>
  }
/>
```

The Button onClick handler must use the same state setter as the original button (`setShowForm(true)`).

Do NOT touch: the modal dialog div (bg-white rounded-lg shadow-xl — this is NOT a PageHeader, leave it), the project list rendering, the empty state section, or any other code outside the header div.

Run `npm run test -- --run` to confirm all 181 tests still pass.
  </action>
  <verify>
    <automated>npm run test -- --run</automated>
  </verify>
  <done>DashboardPage imports and renders PageHeader with title="Projects" and a Button action slot. The inline flex header div and h2 are removed. All 181 tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Adopt PageHeader in ProjectDetailPage</name>
  <files>src/client/pages/ProjectDetailPage.tsx</files>
  <action>
Read src/client/pages/ProjectDetailPage.tsx in full.

Add import at the top of the file:
```tsx
import { PageHeader } from '../components/ui/PageHeader';
```

Find the h2 element used as the page title (pattern: `font-headline text-3xl text-gray-900`, rendering {project.name}). Replace it with:
```tsx
<PageHeader title={project.name} />
```

If the h2 has any adjacent subtitle text (e.g., project description or status), pass it as the subtitle prop instead of leaving it as a separate element.

Do NOT add a PageHeader import if one already exists — check imports first. Do NOT remove any ProjectDetail sub-nav links, workflow buttons, or other content.

Run `npm run test -- --run` to confirm all 181 tests still pass.
  </action>
  <verify>
    <automated>npm run test -- --run</automated>
  </verify>
  <done>ProjectDetailPage imports and renders PageHeader with title={project.name}. The raw h2 page title element is removed. All 181 tests pass.</done>
</task>

</tasks>

<verification>
After plan completes:
1. `npm run test -- --run` — all 181 tests green
2. `grep -n "PageHeader" src/client/pages/DashboardPage.tsx` — returns import + JSX usage
3. `grep -n "PageHeader" src/client/pages/ProjectDetailPage.tsx` — returns import + JSX usage
4. `grep -n "justify-between mb-8" src/client/pages/DashboardPage.tsx` — returns empty (old pattern removed)
</verification>

<success_criteria>
DashboardPage and ProjectDetailPage both use the PageHeader primitive for their top-level title. The inline flex header pattern is eliminated from both pages. Typography hierarchy pattern is established for Phase 14 to follow.
</success_criteria>

<output>
After completion, create `.planning/phases/12-app-shell-global-layout/12-02-SUMMARY.md`
</output>
