---
phase: 87-phase-a-watchdog-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/87-phase-a-watchdog-gate/87-SCORE.md
autonomous: true
requirements:
  - SEC-07
  - SEC-08
  - SEC-09
  - SEC-10
  - PERF-01
  - PERF-02
  - NOTIF-05
  - NOTIF-06

must_haves:
  truths:
    - "Every Phase A acceptance criterion is verified by an automated command"
    - "A SCORE.md file exists with a pass/fail row per criterion and a numeric total"
    - "The numeric score is calculated deterministically from pass counts"
    - "GATE_PASS or GATE_FAIL is declared based on score >= 8.55"
  artifacts:
    - path: ".planning/phases/87-phase-a-watchdog-gate/87-SCORE.md"
      provides: "Evidence record with per-criterion verdicts and final score"
      contains: "GATE_PASS or GATE_FAIL"
  key_links:
    - from: "87-SCORE.md"
      to: "phases 83-86 artifacts"
      via: "grep/test/vitest commands"
      pattern: "PASS|FAIL"
---

<objective>
Run the Phase A Watchdog Gate: execute every acceptance criterion check for phases 83–86,
compute a score (each criterion worth 1.0 point, 10 total), and write SCORE.md declaring
GATE_PASS (score >= 8.55) or GATE_FAIL with remediation pointers.

Purpose: Gate enforcement — Phase 88 must not begin until this score is on record.
Output: 87-SCORE.md with a final verdict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-external-log-drain-security-policy/83-VALIDATION.md
@.planning/phases/84-dependabot-uptime-monitoring/84-VALIDATION.md
@.planning/phases/85-full-text-search/85-VALIDATION.md
@.planning/phases/86-scheduled-report-emails/86-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run all automated gate checks and collect raw results</name>
  <files>.planning/phases/87-phase-a-watchdog-gate/87-SCORE.md</files>
  <action>
Run each check below in sequence inside the repo root (`cd /c/Users/glcar/prevailing-wage`).
Record PASS or FAIL for each criterion. Do NOT interpret ambiguous output as a pass.

**Phase 83 — External Log Drain + Security Policy (SEC-07, SEC-08)**

C1 — Pino + Logtail wiring present in server code:
```
grep -c "pinoHttp\|@logtail/pino" src/server/index.ts src/server/logger.ts
```
PASS if total count >= 2.

C2 — SECURITY_POLICY.md exists with 72-hour SLA text:
```
test -f SECURITY_POLICY.md && grep -q "72 hours" SECURITY_POLICY.md
```
PASS if exit code 0.

C3 — SecurityPolicyPage.tsx exists and is wired as a route:
```
test -f src/client/pages/SecurityPolicyPage.tsx && grep -q "security\|SecurityPolicy" src/client/App.tsx
```
PASS if exit code 0.

**Phase 84 — Dependabot + Uptime Monitoring (SEC-09, SEC-10)**

C4 — dependabot.yml exists with npm and github-actions ecosystems:
```
test -f .github/dependabot.yml && grep -q "npm" .github/dependabot.yml && grep -q "github-actions" .github/dependabot.yml
```
PASS if exit code 0.

C5 — LandingPage footer contains a status/uptime link:
```
grep -qi "status\|betterstack\|uptime" src/client/pages/LandingPage.tsx
```
PASS if exit code 0.

**Phase 85 — Full-Text Search (PERF-01, PERF-02)**

C6 — FTS5 migration file exists:
```
test -f src/server/db/migrations/0054_workers_fts.sql
```
PASS if exit code 0.

C7 — WorkersPage uses debounced search:
```
grep -c "useDebounce\|debounce" src/client/pages/WorkersPage.tsx
```
PASS if count >= 1.

C8 — DashboardPage retains client-side project filter:
```
grep -c "filteredProjects\|searchQuery" src/client/pages/DashboardPage.tsx
```
PASS if count >= 1.

**Phase 86 — Scheduled Report Emails (NOTIF-05, NOTIF-06)**

C9 — scheduledReports.ts exists and is imported in index.ts:
```
test -f src/server/jobs/scheduledReports.ts && grep -q "runScheduledReports\|scheduledReports" src/server/index.ts
```
PASS if exit code 0.

C10 — Unsubscribe endpoint + ProjectSettingsPage report prefs present:
```
grep -q "unsubscribe" src/server/routes/notifications.ts && grep -c "reportSchedule\|reportEmail" src/client/pages/ProjectSettingsPage.tsx
```
PASS if exit code 0 and count >= 1.

**Full test suite (gate integrity check)**

Run:
```
npx vitest run --exclude ".worktrees/**" 2>&1 | tail -5
```
This is not a scored criterion but is a hard prerequisite: if the suite has failures (not just todos/skips), set an overall "suite_ok: false" flag and deduct 0.5 from the final score.

**TypeScript check (gate integrity check)**

Run:
```
npx tsc --noEmit 2>&1 | grep -v "stripeService" | head -5
```
If there are TS errors beyond the known stripeService error, deduct 0.5. The stripeService.ts Stripe API version error is pre-existing and excluded.
  </action>
  <verify>
    <automated>test -f .planning/phases/87-phase-a-watchdog-gate/87-SCORE.md && grep -q "GATE_" .planning/phases/87-phase-a-watchdog-gate/87-SCORE.md</automated>
  </verify>
  <done>87-SCORE.md exists, contains a row for each of C1-C10, shows a final score, and declares GATE_PASS or GATE_FAIL.</done>
</task>

<task type="auto">
  <name>Task 2: Write 87-SCORE.md with verdicts and final declaration</name>
  <files>.planning/phases/87-phase-a-watchdog-gate/87-SCORE.md</files>
  <action>
Using the raw results collected in Task 1, write the 87-SCORE.md file with this exact structure:

```markdown
---
phase: 87-phase-a-watchdog-gate
scored_at: {ISO timestamp}
score_target: 8.55
---

# Phase A Watchdog Gate — Score Report

## Criteria Results

| ID | Phase | Requirement | Description | Result | Points |
|----|-------|-------------|-------------|--------|--------|
| C1  | 83 | SEC-07 | Pino + Logtail wired in server code | PASS/FAIL | 1.0/0.0 |
| C2  | 83 | SEC-08 | SECURITY_POLICY.md with 72h SLA | PASS/FAIL | 1.0/0.0 |
| C3  | 83 | SEC-08 | SecurityPolicyPage.tsx routed in App | PASS/FAIL | 1.0/0.0 |
| C4  | 84 | SEC-09 | dependabot.yml with npm + github-actions | PASS/FAIL | 1.0/0.0 |
| C5  | 84 | SEC-10 | LandingPage footer has status/uptime link | PASS/FAIL | 1.0/0.0 |
| C6  | 85 | PERF-01 | FTS5 migration 0054 exists | PASS/FAIL | 1.0/0.0 |
| C7  | 85 | PERF-02 | WorkersPage debounced search hook | PASS/FAIL | 1.0/0.0 |
| C8  | 85 | PERF-02 | DashboardPage client filter intact | PASS/FAIL | 1.0/0.0 |
| C9  | 86 | NOTIF-05 | scheduledReports.ts + index.ts wiring | PASS/FAIL | 1.0/0.0 |
| C10 | 86 | NOTIF-06 | Unsubscribe route + ProjectSettings UI | PASS/FAIL | 1.0/0.0 |

## Integrity Checks (deductions)

| Check | Result | Deduction |
|-------|--------|-----------|
| Full test suite (762 tests green) | PASS/FAIL | 0.0 / -0.5 |
| TypeScript (no new errors beyond stripeService) | PASS/FAIL | 0.0 / -0.5 |

## Score Calculation

- Base score: {sum of passing criteria} / 10
- Deductions: {sum of integrity deductions}
- **Final score: {base - deductions} / 10**

## Verdict

<!-- Replace exactly one of the two lines below -->
**GATE_PASS** — Score {X.XX} >= 8.55. Phase 88 may begin.
**GATE_FAIL** — Score {X.XX} < 8.55. Remediation required before Phase 88.

## Failed Criteria (if any)

{For each FAIL row, one bullet with file path and fix description}

## Notes

- Scored: {date}
- Test suite result: {pass count} passed, {skip count} skipped
- TypeScript: {clean / known stripeService error only / new errors found}
- Manual-only criteria deferred: Logtail live drain, Better Stack monitor, email delivery — cannot be verified without live credentials
```

Scoring rules:
- 10 criteria × 1.0 point each = 10.0 maximum
- Integrity deductions applied after summing criteria
- Minimum score to declare GATE_PASS: 8.55
- Score is a single decimal-rounded number (e.g., 9.00, 8.50)
  </action>
  <verify>
    <automated>grep -q "GATE_PASS\|GATE_FAIL" .planning/phases/87-phase-a-watchdog-gate/87-SCORE.md && grep -q "Final score" .planning/phases/87-phase-a-watchdog-gate/87-SCORE.md</automated>
  </verify>
  <done>87-SCORE.md is written, all 10 rows have PASS or FAIL, the final score is calculated, and exactly one of GATE_PASS or GATE_FAIL is declared.</done>
</task>

</tasks>

<verification>
Gate passes when:
1. 87-SCORE.md exists in `.planning/phases/87-phase-a-watchdog-gate/`
2. All 10 criteria rows have a verdict
3. Final score >= 8.55 → GATE_PASS declared
4. Full test suite is green (762 passing)
5. No new TypeScript errors beyond the known stripeService.ts issue

Gate fails when:
- Final score < 8.55 → GATE_FAIL declared with remediation bullets
</verification>

<success_criteria>
- 87-SCORE.md exists with 10 scored rows
- Final numeric score computed and displayed
- GATE_PASS or GATE_FAIL declared unambiguously
- If GATE_FAIL: each failing criterion has a specific file path and fix description
- If GATE_PASS: Phase 88 entry in ROADMAP.md may be unlocked
</success_criteria>

<output>
After completion, create `.planning/phases/87-phase-a-watchdog-gate/87-01-SUMMARY.md` with:
- Final gate verdict (GATE_PASS / GATE_FAIL)
- Final score
- Count of passing criteria out of 10
- Any failing criteria listed by ID
- Test suite result (X passing)
- Whether Phase 88 is unblocked
</output>
