import { describe, it } from 'vitest';

describe('generateVariancePdf', () => {
  it.todo('VAR-04: returns Uint8Array starting with %PDF magic bytes');
  it.todo('VAR-04: includes project name in header');
  it.todo('VAR-02: over-threshold rows appear with flag indicator in PDF');
});
