import { describe, it } from 'vitest';
describe('payrollService', () => {
  it.todo('createPayrollWeek inserts row and returns id');
  it.todo('upsertPayrollEntry saves daily ST/OT hours with rate snapshots');
  it.todo('getPayrollEntries returns all entries for a week with worker names');
});
