import { describe, it } from 'vitest';
describe('payroll routes', () => {
  it.todo('POST /api/payroll/weeks creates a new week for a project');
  it.todo('GET /api/payroll/weeks/:id returns week with entries');
  it.todo('PUT /api/payroll/entries/:id upserts daily hours for a worker');
  it.todo('GET /api/payroll/projects/:projectId/weeks lists all weeks');
});
